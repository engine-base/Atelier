# 未着手・保留の依頼一覧（バックログ）

> **この表が「まだやっていないこと」の唯一の信頼源。**
> `docs/gap-tracker.md` は完了記録なので、そこに無いものは「終わっていない」。
> 会話は長くなると要約されて古い依頼が消えるため、**依頼を受けたらまずここに書く**。
> 着手したら「対応中」、完了したら gap-tracker.md へ移して本表から削除する。

最終更新: 2026-08-20

## 着手予定（優先順）

**現在、着手予定の項目はありません。**（新しい依頼を受けたらここに追記する）

## 判断待ち（経営者の回答が必要）

| # | 項目 | 何を決める必要があるか |
|---|---|---|
| — | **Anthropic への書面確認** | ① サードパーティ製品が利用者の Claude Code を自動起動する形態の可否 ② 無人の夜間自動実行がパーソナルユースの範囲か ③ 法人利用で Pro/Max 可か。**事業の根幹なので口頭ではなく書面で** |
| — | **規約の法務レビューと再同意** | GAP-188 で規約に「各自の Claude 契約が必要」を明記したが、本文は法務レビュー前のドラフト。① 確定文言のレビュー ② 旧版に同意済みのユーザーへ再同意を求めるか（現状は導線なし） |
| GAP-196 | **日次ダイジェストの遡り生成をやるか** | GAP-193 で「何回分飛んだか」は履歴と画面に出るようにした（黙って消えなくなった）。**遡って作り直すのは未実装** — 集計 SQL が `now() - interval '24 hours'` のように現在時刻に固定されているため、3 回走らせても「同じ今日の内容」が 3 個できるだけ。本当に遡るには 6 種の runner と集計 SQL に「いつ時点で集計するか」を通す作業が要る。**やるかどうかの判断待ち**（3 日前のダイジェストを今さら作っても読まれない可能性が高く、「飛んだ回数が分かる」だけで足りるかもしれない） |

## 将来やること（今は不要と判断済み）

| # | 項目 | 今の状態 / いつ必要になるか |
|---|---|---|
| GAP-191 | **実行中のターンへ指示を直接差し込む（常駐プロセス）** | GAP-190 は `--session-id` / `--resume` による継続で、毎ターン新プロセスを起動する。会話・TODO の継続とプラン枠の節約は実現済み。**実行中のターンへ割り込む**（Claude Code のインタラクティブと同じ挙動）には 1 プロセスを跨ターンで保持する常駐化が要る。現状の追い足しは GAP-189 の「実行が終わったら次に流す」方式で、取りこぼしは無い |
| — | Bridge のセキュリティ強化 | 経営者判断「承認モードや bash は Claude もやってるしそのままでいい」。**今後強化はしていく**方針。クラウド由来の指示リスクは運営サーバーの防御で担保 |
| — | 外形監視（uptime） | サーバーが完全に落ちた場合は自前ログにも残らない |
| — | DB 接続プールの拡張 | 現在 1 台あたり 15 接続（2 台で 30）。**数百ユーザーを超えるあたりで見直し** |
| — | SSE 同時接続の上限見直し | 同時チャット 50 人で 2 台目が起動。それ以上は遅くなる（実測 p95 2.6 秒 @ 同時 100） |
| — | 意味検索・文字起こしをサーバーで動かす | 本番 Docker イメージに `--extra localrag` を入れていないためモデル未搭載。動かすなら VM を 1〜2GB へ（実測: 埋め込み 652MB〜1,554MB / whisper 302MB〜1,007MB） |
