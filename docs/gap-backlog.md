# 未着手・保留の依頼一覧（バックログ）

> **この表が「まだやっていないこと」の唯一の信頼源。**
> `docs/gap-tracker.md` は完了記録なので、そこに無いものは「終わっていない」。
> 会話は長くなると要約されて古い依頼が消えるため、**依頼を受けたらまずここに書く**。
> 着手したら「対応中」、完了したら gap-tracker.md へ移して本表から削除する。

最終更新: 2026-08-22

## 着手予定（優先順）

**現在、着手予定の項目はありません。**（新しい依頼を受けたらここに追記する）

## 判断待ち（経営者の回答が必要）

| # | 項目 | 何を決める必要があるか |
|---|---|---|
| — | **⚠️ Anthropic の規約に当たる可能性（2026-08-22 判明・再オープン）** | 「違反していなければ照会不要」という前提で一度は先送りとしたが、**実際に読んだところ前提が成り立たなかった**。Agent SDK のドキュメントに「**Unless previously approved, Anthropic does not allow third party developers to offer claude.ai login or rate limits for their products**」と明記されており、**Atelier の形はこれに当たると読むのが素直**。同じ文が「事前承認があれば別」と書いており、**承認を取りに行くのが正規ルート**として示されている。全文・引用・選択肢は `docs/anthropic-terms-review.md`。決めること: ① 承認を取りに行く ② API キー方式へ切り替える（費用構造が変わる） ③ 認識したうえで現状維持 ④ 定時実行だけ止める（緩和策・併用可） |


## 決定済み（2026-08-22 経営者判断）

| # | 項目 | 決定 | 何をするか |
|---|---|---|---|
| — | Anthropic への書面確認 | ~~今は出さない。大きくなってから~~ → **2026-08-22 に前提が崩れたため判断待ちへ戻した** | 「向こうが書いているものに違反していなければ不要」という前提だったが、実際に読むと**当たると読める明文があった**（`docs/anthropic-terms-review.md`）。読み取り記録は作成済み |
| — | 規約の法務レビュー | **弁護士には頼まない。AI 側で仕上げる** | GAP-208 として実施。**私は弁護士ではないため「法的に完璧」は保証できない**旨を伝えたうえでの経営者判断。条文（消費者契約法・特商法・個人情報保護法）に照らした点検と是正を行い、限界は正直に記録する |
| — | 混雑通知の宛先 | **通知は入れない。画面に出ていればよい** | メール/Slack への送信は設定しない。ただし現状 S-T01 の混雑表示は **その要求を処理した machine のプロセス内カウンタ**で、`capacity_events`（machine をまたいだ実績）は cron でしか使われていない。**運営画面に machine 横断の混雑実績を出す**必要がある（GAP-208 に同梱） |
| GAP-196 | 日次ダイジェストの遡り生成 | **やらない** | 中身はタスク数・フェーズ・直近24hの実行件数を並べた機械的な集計（AI 不使用）。遡って作る価値が低く、「何回飛んだか」が分かれば実害なしと判断 |

## 将来やること（今は不要と判断済み）

**2026-08-20: この表にあった 7 項目はすべて実装しました**（GAP-191 / 194 / 195 /
197 / 198 / 199 / 200）。新しく「今は不要」と判断したものが出たらここに追記する。

| # | 項目 | 今の状態 / いつ必要になるか |
|---|---|---|
| — | 同時チャットをさらに増やす | GAP-202 で **1 台 1000 人 / 2 台 2000 人**（実測）。それを超えたときの手は 2 つ: ① 上限を上げる（実測では 5000 人でも配達 p95 77ms でまだ余裕。ただし uvicorn / Fly プロキシ側が**未実測**なので、上げる前にそこを測る）② machine 数を 2 から増やす。**断った回数が S-T01 に出る**ので必要時期は数字で分かる |
| — | 順番待ちの列も一杯になったときの体験 | GAP-203 で **並ばせて自動で通す**ようにした（列は上限の 2 倍 = 1 台 2000 人まで）。そこを超えたときは今も「お断り」のまま（理由は日本語で画面に出る / 打った文章も消えない）。実測上 当面到達しない |
| — | ブラウザ⇔Bridge の直結（同じ PC のとき中継を飛ばす） | GAP-202 で待機がほぼ無料になったため**うまみがほぼ消えた**（残るのは表示がわずかに速いことだけ）。Bridge にローカルサーバーが無い / Safari の https→localhost 制約が未検証 / スマホでは使えず中継を捨てられない、の 3 点で**道を 2 本保守する代償に見合わない**可能性が高い。当面やらない |
| — | **Supabase の Supavisor 実検証**（3 台目に行くとき必須） | GAP-205 で**切り替えは設定だけ**にした（コード変更不要・自動判定・prepared statement 自動無効化・手順書つき）。ただし **Supavisor 本体の挙動は実 Supabase でしか検証できず未実施**。実接続上限と料金も**ダッシュボードで要確認**。3 台目が必要になったら `docs/scaling-runbook.md` の段階 3 |
| — | 見た目の模倣そのもの | GAP-204 のとおり**技術的に止められない**（配った HTML/CSS は必ず読める）。規約 第9条で禁止を明記したので、実際に模倣が起きたときに**法的に戦える状態**にはなっている。監視（類似サイトの発見）は未実装 |
| — | 流出検査 — 英語のみのプロンプト | GAP-206 で**登録漏れは CI が落とす**ようにした（`apps/api/src` を走査し、プロンプトの印を含むのに未登録なら fail）。残っているのは**英語のみで書かれたプロンプト**で、検査は日本語の長い文字列を印にしているため対象外。今は全プロンプトが日本語なので実害なし |
| — | **セッションが 8 時間で切れる（refresh が未配線）**（旧 GAP-210） | パスワードでサインインしても refresh token が発行されず（発行しているのは magic link 経路だけ）、`refreshAccessToken()` は実装もテストもあるが**アプリ内の呼び出し元が 0 件**。アクセストークンの寿命は 8 時間（`ATELIER_ACCESS_TOKEN_TTL_SECONDS=28800`）なので、**1 日 1 回パスワードを打ち直す**ことになる。戻り先は `?redirect=` で保持されるので作業は失われない。7/18 に TTL を 1h→8h に伸ばした対処（deploy #69）が入っており、**痛みに対して適正なサイズの対処が済んでいる**ため着手しない（2026-08-25 判断）。**やるとしたら**: API が HttpOnly の refresh cookie を発行（CORS は `allow_credentials=True` + オリジン限定なので成立）→ 期限前に黙って更新 → 期限切れ着地時は復帰を試みる。**注意**: 「TTL をもっと伸ばす」では代替できない。アクセストークンは署名だけで検証する JWT で**発行後に取り消せない**ため、伸ばすと GAP-209 のサインアウトが同じ期間効かなくなる。**引き金**: 8 時間を超えて連続作業する利用者が実際に出たとき / セッションを 8h より短くしたくなったとき | **2026-08-26 の通し (J11-02) で実測して裏付けた**: `POST /auth/signin` の応答は `refresh_token: None`。一方 magic-link 経路で発行した本物のトークンは正しく機能する — 1 回使うと回転して再利用は 401、`POST /auth/signout` (204) の後も 401。つまり**仕組みは動いていて、password サインインだけが繋がっていない**。セッション長は設計判断なので着手していない。
| — | **画面別 e2e 37 本が収集すらされていない**（2026-08-25 実測済み） | `/e2e/` 直下の 37 本は `playwright.config.ts` の `testDir: './tests'` の**外**にあり、CI どころか**ワークスペース自身の `pnpm test` でも収集されない**（`--list` で 2 ファイル 41 件のみ）。**実際に走らせて測った** (`.qa/e2e-audit-20260825`): 認証なし 34/37 失敗 → 認証を入れると 23/37 失敗。残る 23 本も**実ブラウザで 19 画面を 1 枚ずつ開いて確認し、全部正常に描画されていた** — 旧内部 URL (`/projects/s_b01` → `/projects`)・旧文言 (「プロジェクト一覧」→「プロジェクト」)・リネーム (`/client/s_l01` → `/portal/invitations`)・空状態の想定漏れ が原因で、**製品の不具合は 0 件**。よって**今 CI に載せない**（載せる前に 37 本を直す作業＝壊れていないものを直す作業になる）。**引き金**: 公開を決めたとき、または画面の URL・文言を大きく変えるとき（そのとき 37 本ごと現行に合わせる） |
| — | Bridge トークンを OS キーチェーンへ | 今は平文 JSON (mode 0600)。Electron の safeStorage へ移すには実機検証が要る (GAP-199 で明記) |
