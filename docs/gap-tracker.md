# Gap Tracker — design-audit で確定した未実装/未整備の正本

> CLAUDE.md ルール12 準拠: 「あとで」「placeholder」を口にした時点で登録する台帳。
> 各項目は解消 PR で本ファイルからチェックを付けて消し込む。黙って消さない。

## 機能 gap (バックエンド未実装のため UI から要素を撤去したもの)

| ID | 内容 | 現状 | 解消に必要なもの | 起票日 |
|---|---|---|---|---|
| ~~GAP-001~~ ✅解消 (2026-08-04) | S-E01 チャット添付 | **実装済**: ① POST /chat/attachments/upload-url — 署名付きアップロード URL (Supabase Storage 契約、bucket `chat-attachments/{thread_id}/{uuid}/{sanitized}`、MIME 画像/PDF/テキスト/MD/CSV/ZIP 限定 415・10MB 超 413・viewer 403・不可視スレッド 404・storage 未設定 503) ② SSE stream body の attachments (≤10 件) → user message の chat_messages.attachments jsonb (既存列) に永続。**他スレッド配下の storage_path は 422** (可視性バイパス拒否) ③ GET /chat/messages/{id}/attachments/{index}/url — RLS 可視メッセージの署名付き DL URL (範囲外/越境 404) ④ S-E01 コンポーザに「添付」復元 — 複数選択/送信前チップ (個別削除)/client 側事前検証/アップロード失敗時は送信中止/メッセージに添付チップ → 署名付き URL を新タブで開く。pytest 5 (503/415/413/403/404/422 越境/jsonb 永続/署名 DL) + vitest 3 + 実操作監査 .audit-e01-attach.mjs 3 連続 ALL PASS (実 PDF PUT → 実バイト保存 → DB 突合 → 署名 URL 実 GET バイト一致) | — | 2026-07-18 |
| ~~GAP-002~~ ✅解消 (2026-08-04) | S-E01 /コマンド | **実装済** (S-E01 のモック要素はこれで全て実装済): コマンド体系 3 種 — ① `/要約` = 会話の要点整理 (決定事項/未解決の論点/次のアクション) を依頼する実文面に置換して SSE 送信 (LLM 実行) ② `/決定 <内容>` = POST /chat/threads/{id}/commands (command=decision) — 実 decisions 行 (decided_by=対話相手 AI 社員、with_user=true) ③ `/タスク化 <タイトル>` = 同 API (command=task) — 実 tasks 行 (category=チャット起票、triage、見積未実施の暫定 1h を description に明示)。サーバーはコマンド原文 (user) + 実行結果 (system) をスレッド永続 + audit chat_command.executed。viewer 403 / 不可視 404 / 空白 args・未対応 command 422。UI はモックの「/コマンド」(zap) パレット (usage+説明) → 挿入 → 送信時解釈、未対応 /xxx は何も実行せず inline error。pytest 3 + vitest 3 + 実操作監査 .audit-e01-commands.mjs 3 連続 ALL PASS (実 decisions/tasks 行 + user/system 永続 + audit ×2 を DB 突合) | — | 2026-07-18 |
| ~~GAP-003~~ ✅解消 (2026-08-03) | 確定事項の「ピン留め」操作 (モック現物は S-E01 主力決定カードの .decision-pin-btn) | **実装済**: decisions.pinned 列 (migration t-d-99z、冪等) + PATCH /decisions/{id} {pinned} + 一覧 order by pinned desc + S-E01 主力決定カードのトグル UI (aria-pressed)。pytest 回帰 + 実操作監査 .audit-e01-pin.mjs 3 連続 ALL PASS (DB 突合往復) | — | 2026-07-18 |
| ~~GAP-004~~ ✅解消 (2026-08-03) | phases への担当 AI 社員割当 | **実装済**: phases.assigned_employee_ids uuid[] (migration t-d-99zb) + PATCH /workflow/phases/{id} (丸ごと置換、他 WS 社員は 422 で拒否) + S-F02 割当 UI (チップ/外す/追加 select、楽観更新) + S-F01 ヘッダーアバターは実割当優先 (未割当は従来集計フォールバック)。**実操作監査が実バグを検出**: 社員候補が無フィルタで他 WS の同名社員を混ぜていた → project→workspace フィルタ修正。pytest 2 + vitest 1 + 監査 3 連続 ALL PASS | — | 2026-07-18 |
| ~~GAP-005~~ ✅解消 (2026-08-03) | S-B02 の「未解決コメント」KPI | **実装済**: GET /comments/unresolved-count?project_id= (workflow_output/mock/task/acceptance_criteria の 4 種 target を project へ逆引きし open のみ集計、RLS 可視分のみ)。S-B02 KPI をモック本来の「未解決コメント」実カウントに差替え (確定/未確認は sub 表示へ)。pytest (解決/削除で減算・越境 0) + 実操作監査 .audit-b02-unresolved.mjs 3 連続 ALL PASS | — | 2026-07-18 |
| ~~GAP-006~~ ✅解消 (2026-08-03) | S-I01 の「依存グラフ」ビュー | **実装済**: tasks.dependencies は API 契約是正 (57f14f5) で公開済だったため描画側を新設 — 表示トグル「依存グラフ」(モック .seg 準拠) + 層別 DAG (層 = 前提からの最長距離、SVG ベジェ辺 + 矢印、ノードは S-I02 への実リンク、循環依存は層 0 フォールバック + バッジ明示)。vitest 4 件 + 実操作監査 .audit-i01-deps.mjs 3 連続 ALL PASS (シード 3 タスク・辺 3 本・詳細遷移) | — | 2026-07-18 |
| ~~GAP-007~~ ✅解消 (2026-08-01) | TopBar の通知ベル (通知センター) | **実装済**: T-UC-36 監査で TopBar ベル (未処理承認待ち件数バッジ + aria-label) → 通知センター /t-uc-36 実リンクを実装。通知の実体は approval_inbox (e2e 通しでプロデューサ欠落も是正済 — complete_task が挿入・approve/reject が resolve)。journey J-17 で「pending=1 がベルバッジ+通知センターに実表示」を 4 回の全 PASS 実走で確認 | — | 2026-07-18 |
| ~~GAP-008~~ ✅解消 (2026-08-03) | S-C02 の「活動履歴」タブ + 「最近の活動」カード | **実装済**: GET /ai-employees/{id}/activities — tasks(担当)/decisions(decided_by)/task_executions(担当タスク)/chat_threads(対話相手) の 4 源を union して新しい順 (RLS 可視分のみ、越境は社員自体 404)。S-C02 に活動履歴タブ (種別バッジ+件数、最近の活動カード)。pytest 4 件 (fixture は WS 自動シードと両立) + vitest + 実操作監査 .audit-c02-activities.mjs 3 連続 ALL PASS | — | 2026-07-19 |
| ~~GAP-009~~ ✅解消 (2026-08-04) | S-C02 の「画像アップロード」(アイコン画像) | **実装済** (S-C02 のモック要素はこれで全て実装済): ① POST /ai-employees/{id}/icon-upload-url — 署名付きアップロード URL (meetings と同じ Supabase Storage 契約、bucket `avatars/ai-employees/{id}/{uuid}/{sanitized}`、MIME png/jpeg/webp 限定 415・512KB 超 413・storage 未設定 503) ② PATCH icon=storage_path で確定 (icon 列は lucide 名 / storage path 両対応) ③ GET /ai-employees/{id}/icon-url — 署名付き閲覧 URL (path でないときは 409) ④ S-C02 に「画像アップロード」復元 — client 側事前検証 → 実 PUT → PATCH → ヘッダ/プレビューに `<img>` 実描画、「頭文字に戻す」で解除、503 は誠実表示。**実 Supabase Storage は env (ATELIER_SUPABASE_ADMIN_API_URL / SERVICE_ROLE_KEY) 設定のみで稼働** (dev 監査は storage スタブで実バイト往復)。pytest 8 + vitest 3 + 実操作監査 .audit-c02-icon.mjs 3 連続 ALL PASS (実 PNG PUT → DB 突合 → naturalWidth>0 実描画) | — | 2026-07-19 |
| ~~GAP-010~~ ✅解消 (2026-08-04) | S-K01 の「グラフ」ビュー | **実装済**: GET /knowledge/graph — ノード (RLS 可視・参照回数順最大 120、超過は truncated 明示) + 実エッジ (parent_id 階層 / タグ共起。**推測エッジなし**)。UI は SVG 円形レイアウト (決定的配置・外部グラフライブラリなし)、実線=階層/破線=タグ共起、scope 別配色+凡例、ノードクリック → 実 GET /knowledge/{id} → ノートビュー遷移。R-T08 越境 0 件は pytest で検証。pytest 2 + vitest 2 + 実操作監査 .audit-k01-graph.mjs 3 連続 ALL PASS (実 JWT API 突合 + SVG 実描画 + クリック遷移) | — | 2026-07-19 |
| ~~GAP-011~~ ✅解消 (2026-08-04) | S-K01 の「Obsidian で開く / Vault に書出」 | **実装済** (S-K01 のモック要素はこれで全て実装済): ① GET /knowledge/vault-export — RLS 可視分のみを Obsidian Vault 形式 zip (`<scope>/<category>/<title>.md` + YAML frontmatter、衝突は連番回避、X-Vault-Nodes) ② 「Obsidian Vault に書出」= 認証付き fetch → zip DL (書出中表示+失敗 inline error) ③ 「Obsidian で開く」= 選択ノートの obsidian://new?name+content 実 URI (未選択時 非描画)。pytest 2 (zip 構成/frontmatter + R-T08 越境 0 件) + vitest 2 + 実操作監査 .audit-k01-obsidian.mjs 3 連続 ALL PASS (実 DL → unzip → DB 突合、zip 件数 = DB 可視ノード数) | — | 2026-07-19 |
| ~~GAP-012~~ ✅解消 (2026-08-04) | S-K01 の「バックリンク」セクション | **実装済**: knowledge_references テーブル (migration t-d-99zc — RLS は knowledge_nodes の可視性をサブクエリ継承、insert/update も可視ナレッジ限定) + チャット SSE の RAG 消費時に (knowledge, chat_thread) を upsert 永続化 (再参照は reference_count++ に畳む) + GET /knowledge/{id}/references (referrer_title を chat_threads/tasks/decisions から実解決、RLS 不可視・削除済の参照元は除外。無題スレッドは「無題スレッド」fallback) + S-K01 バックリンク節 (件数見出し・種別ラベル・文脈・n 回、参照 0 は「まだ参照されていません」の誠実表示)。referrer_type は task/decision/feature も同型で受ける (書込元実装時に行が生まれる — 偽装挿入なし)。pytest 4 (実 SSE 経由の記録/dedupe/R-T08 越境 404) + vitest 2 + 実操作監査 .audit-k01-backlinks.mjs 3 連続 ALL PASS (実チャット送信→DB 突合→S-K01 実描画往復) | — | 2026-07-19 |
| ~~GAP-013~~ ✅解消 (2026-08-03) | S-O01 の「実行履歴」テーブル | **実装済**: cron_run_history テーブル (migration t-d-99za — RLS: platform 行は authenticated / project 行は member、INSERT policy 無しで API 改竄不可) + Inngest handler wrapper が全 cron 発火を running→success/error で記録 (履歴失敗でも cron を止めない best-effort、unit 3 件) + GET /cron-runs + S-O01 実行履歴テーブル (モック .history-card 準拠)。実操作監査 3 連続 ALL PASS。スケジュール別履歴は project cron 実行エンジン (T-F-20) 配線時に schedule_id/project_id 列でそのまま拡張可 | — | 2026-07-19 |
| ~~GAP-014~~ ✅解消 (2026-08-04) | S-O01 の「法令・運用バックエンド (必須)」セクション | **ジョブの実体から実装**: ① purge-deleted-accounts (毎日 0:00 JST) — T-A-05 が規定しながら不在だった退会 30 日後ハード削除 worker の実体 (所有 WS cascade → external_uploads → auth.users 物理削除、猶予中/現役は絶対に触らない、audit platform.account.purged) ② integrity-check (毎日 5:00 JST) — SQL のみ整合性チェック 5 種 (dangling 依存/AC/モック/工程-プロジェクト/工程担当越境)、検知時は WS owner の approval_inbox (integrity_alert、pending 重複なし) へ通知 (migration t-d-99ze) ③ GET /cron-platform-jobs (read-only 可視化 — 実 cron 式+次回+cron_run_history 実最終実行) ④ S-O01 に法令・運用節 (必須/無効化不可タグ、API 未到達時は節ごと非描画)。Inngest cron + 単独実行 (python -m src.services.platform_jobs) の 2 経路。pytest 7 + vitest 2 + 実操作監査 .audit-o01-legal.mjs 3 連続 ALL PASS | — | 2026-07-19 |
| ~~GAP-015~~ ✅解消 (2026-08-03) | S-M01 の解析結果構造化 (サマリー/話者分離/要件抽出/アクションアイテム) | **実装済**: transcription worker の後段に LLM 解析 (`services/meetings/analysis.py` — AnthropicClient / selected-stack v1、スキーマ正規化・コードフェンス許容・24k 文字切詰)。解析は additive (失敗しても transcription は成功のまま analysis_error 分類 → UI は誠実に「解析未実行」表示、偽の解析を出さない)。S-M01 にサマリー/話者/抽出要件/アクションアイテムのブロックを実装。unit 9 + vitest 2 + 実 UI 監査 3 連続 ALL PASS (storage スタブで署名 URL フロー E2E)。**実 LLM は ANTHROPIC_API_KEY 設定で稼働** | 残: 抽出要件→承認フロー連携 (「要件として承認」— GAP-031 側で管理) | 2026-07-19 |
| ~~GAP-017~~ ✅解消 (2026-08-03) | S-B03 の「プロジェクト跨ぎナレッジ参照」トグル | **実装済**: projects.settings.cross_project_knowledge (既定 true) を PATCH /projects/{id} + GET で read/write。false のとき /knowledge/search (project_id 付き) と chat RAG (SSE — thread の project_id を自動配線) が「自プロジェクト由来 + プロジェクト無所属 + 運営デフォルト」に限定され、他プロジェクトのナレッジを跨ぎ参照しない。S-B03 に実トグル (楽観更新+ロールバック、API 値未到達時は非描画)。pytest 2 (ON/OFF 往復・除外検証) + vitest 2 + 実操作監査 3 連続 ALL PASS (実 JWT 検索 API で除外/復帰を突合) | — | 2026-07-19 |
| ~~GAP-023~~ ✅解消 (2026-08-10) | S-G01 の 成果物ビューア 未対応要素 | **全 3 項目実装済** (migration t-d-99zj: workflow_outputs.meta + output_fix_proposals + RLS): ① **編集 = ドキュメント AI (スティーブ) への修正依頼** — GAP-024 (S-H01) と同じ確定アーキ判断 (Open Design パターン「自然言語指示 → AI が改訂」/ モックの ai-fix「スティーブの修正提案」と同一主体)。POST /outputs/{id}/revise (services/outputs/revise.py): 現行 HTML 署名 DL → LLM (AnthropicClient, ATELIER_DOC_MODEL 既定 claude-sonnet-4-6) が全文改訂 → 新パスへ署名 UL → 新バージョン行 (version+1, meta author=steve/revision_instruction/revised_from_version/model)。改訂版の json/md は null (旧版の別形式を新版として偽装しない)。LLM 未設定 503 / HTML 無 409 / 60k 字超 413、テストのみ ATELIER_ALLOW_FAKE_LLM=1 の決定的スタブ ② **JSON/MD タブ・バージョン選択・DL** — content-url に format=html/json/md (未生成 format は 409)。タブは実在形式のみ描画 (Rule 10)、JSON/MD は署名 URL → fetch の実テキスト描画。GET /outputs/{id}/versions (project+stage+phase チェーン) を実 select 化 (スティーブ（更新） 表示・選択で実遷移・最新版バッジは最新のみ)。DL は現在 format の blob 保存 (失敗は honest エラー) ③ **座標ピン注釈・AI 修正提案・スレッド返信** — GET /outputs/{id}/anchors (実 HTML から id 要素抽出 — 推測候補なし) → コンポーザの対象位置ピッカー → comments.target_element_id 永続 → チップ + 「本文へ →」= iframe #fragment 実ジャンプ。返信 = parent_comment_id 付き POST /comments + isReply 表示。AI 修正提案 = POST /comments/{id}/fix-proposal (人間の明示操作起点 — 自動生成しない) → pending (1 コメント 1 pending) → 承認 = 提案文を修正指示として revise 適用 (applied_output_id + 遷移) / 却下 = 文書不変。audit output.revise / fix_proposal.propose/approve/reject、R-T08 越境 404。**監査が実バグ 2 件を検出・是正**: 楽観追加中コメント (optimistic id) に操作ボタンが出て API 500 → pending ガード + 不正 UUID を 404 に (回帰 pytest)。pytest 5 クラス追加 (9 passed) + vitest 12 passed + a11y 87 passed + 実操作監査 .audit-g01-viewer.mjs 3 連続 ALL PASS (22 チェック — DB/storage/audit 突合) | — | 2026-07-19 |
| ~~GAP-022~~ ✅解消 (2026-08-10) | S-F02 の AI 提案フェーズ + F-IMP01 影響範囲解析 + 統計「F-IMP01 実行回数/日次整合性」 | **全項目実装済** (migration t-d-99zk: phase_proposals + impact_analyses + RLS): ① **AI 提案フェーズ** — 運用ルール「フェーズ追加は AI 提案のみ」の実体。提案主体 = COO AI ジャービス (モック avatar-jarvis)。「ジャービスに次フェーズを提案してもらう」(人間の明示操作 — 自動生成しない) → POST /workflow/phase-proposals (LLM が既存フェーズ+タスク状況から name/description/reason を生成、fake は決定的スタブ、未設定 503)。pending カード (.proposed 準拠: ✦/承認待ち pill/ジャービスが提案 · 日時/提案理由を見る トグル) → 承認 = 実 phases 行確定 (order 再計算 + approved_phase_id) / 却下 = 不変。1 プロジェクト 1 pending (409) ② **F-IMP01 影響範囲解析** — タスク+移動先を選択 → POST /workflow/impact-analysis が tasks.dependencies (uuid[]) を recursive CTE で推移的走査 (推測なし) → 影響ノード + 実装済み/その他 内訳。「承認して移動」= POST .../apply で実移動 + **完了済影響タスクのリファクタ自動起票 (F-CUC02: origin_type=refactor / triage / 暫定 1h 明示)**。別プロジェクト 422 / 二重適用 409 ③ **phase 別タスク集計** — GET /workflow/phase-task-stats (done/total/awaiting + 最新実行スコア平均 = 実 task_executions) をカード集計行に描画 (モック「8/24 タスク完了」実体化) ④ **統計** — 提案中 (実 pending 数) / F-IMP01 実行回数（本日）= impact_analyses 実カウント / モックの「日次整合性チェック」は日次バッチ不在のため**表示時実計算の「依存整合性チェック」** (dependencies 参照先の実在検証、dangling N 件表示) として誠実化。audit phase.proposal.propose/approve/reject + task.impact.analyze/apply、R-T08 404。pytest 3 クラス追加 (9 passed) + vitest 6 件追加 (10 passed) + 実操作監査 .audit-f02-phase-ops.mjs 3 連続 ALL PASS (22 チェック — 専用プロジェクトシード + DB/audit 突合) | — | 2026-07-19 |
| ~~GAP-021~~ ✅解消 (2026-08-15) | S-A03 の「プラン」タブ + アイコン変更 | **実装済** (migration t-d-99zp: workspace_billing + workspaces.icon): ① 課金 = Stripe テストモード実連携 (httpx REST 直叩き・SDK 追加なし) — GET /billing/plan (行なし=free 誠実返却 + stripe_configured) / POST /billing/checkout (mode=subscription, price_data インライン JPY 5000/月, **実 Stripe テスト API で session 実作成を統合テスト済**) / GET /billing/checkout/{session_id} (paid のみ pro 反映 — webhook 無し環境でもポーリングで完結・偽の成功なし) / POST /billing/webhook (Stripe-Signature HMAC v1・skew 300s・constant-time)。未設定は 503 + タブは「決済連携が未設定」表示のみ (死にボタンなし)。pro 化は workspaces.plan にも同期。audit billing.checkout.create / billing.plan.change / workspace.icon.update。R-T08 非メンバー 404 ② アイコン = workspaces.icon (絵文字/1〜3 文字・octet_length≤8 CHECK) を PATCH /workspaces で更新、S-A03 基本情報 + TopBar/ピッカーの頭文字ブロックが icon 優先表示。プランタブはモックのタブ順で復元。pytest 45 passed (実 Stripe 統合含む) + vitest 10 追加 (587 passed) + openapi 4 endpoint + Workspace.icon 宣言。**残: 本番 webhook 登録 (Dashboard → whsec を STRIPE_WEBHOOK_SECRET へ。ローカルは不要)** | — | 2026-07-19 |
| ~~GAP-020~~ ✅解消 (2026-08-15) | S-A01 の OAuth サインイン (GitHub / Google) | **実装済** (migration t-d-99zo: oauth_accounts / commit cf5e302): プロバイダ汎用 OAuth 層 (services/auth/oauth.py) — GET /auth/oauth/providers (env 設定済のみ返す) / {provider}/start (HS256 署名 state 10 分 + nonce → 302) / {provider}/callback (httpx code 交換 → google userinfo / github user+emails の primary&verified のみ → **連付け**: oauth_accounts 一致 → email 一致既存 user → 新規作成、既存 signin と同一 JWT 発行 → /auth/oauth-complete へフラグメント受け渡し)。email 未検証 400 (偽アカウント不作成)、state 改竄/期限切れ 400、未設定 503。audit auth.oauth_signin (login_kind=created/linked/existing)。S-A01 は providers 追従の条件描画 (0 件なら oauth-row ごと非表示 = 死にボタン禁止準拠)。redirect URI = {API}/auth/oauth/{provider}/callback (ローカル: http://127.0.0.1:8000/...)。pytest 18 (55 passed) + vitest 9 (26 passed) + openapi 3 endpoint 宣言。**残: ユーザーが Google/GitHub コンソールで client 発行 → .env 投入 (キー未設定でもボタン非表示で無害)** | — | 2026-07-19 |
| ~~GAP-019~~ ✅解消 (2026-08-10) | S-T01 運営ダッシュボードの未描画セクション | **全 6 セクション実装済** (migration t-d-99zm: admin_goals + beta_feedback + acquisition_records + admin_costs — RLS deny + is_admin ゲート + service session)。**表示値の出所を 3 種に限定する誠実設計**: 実カウント/実計測・運営の明示的記録・実設定状態 (推測の稼働率/創作売上を出さない): ① **ミッションヒーロー** — PUT /admin/goal で運営が目標を記録 (未設定は honest 表示 + 記録フォーム)。GET /admin/mission が実 WS 数・30 日実増分・残数・残月数・必要ペースを実計算 (進捗リングも実比率) ② **KPI bento 拡張** — GET /admin/platform-stats (タスク実行 30 日 + 実行スコア平均 = 実 task_executions / ベータ FB 件数 + 未対応 / 稼働 Bridge 数 = bridge_workers presence / 退会 30 日) ③ **トレンドグラフ** — GET /admin/trends の週次実累計 (created_at 由来・pytest で単調非減少検証) を実 SVG 描画。MRR は課金未導入の実額「¥0」明示 ④ **取得チャネル別** — acquisition_records の実集計バー + 30 日/累計 seg + 「獲得を記録」フォーム + 記録削除 (未記録は honest 空) ⑤ **プラットフォーム健全性** — GET /admin/health は実計測のみ (DB roundtrip 実測 ms / DB サイズ・接続数 / dispatcher キュー + Bridge presence / 外部 API 4 種のキー設定有無という事実) ⑥ **ベータ FB** — POST /beta-feedback (認証ユーザー収集) → admin 一覧 (カテゴリタグ・未対応/すべて seg) → 対応済み化 (open のみ、二重 404) ⑦ **運営コスト** — admin_costs へ記録 (月初正規化)・実合計・削除、「運営が記録した実費のみ」明示。全 GAP-019 エンドポイント非 admin 403。audit admin.goal.set / admin.acquisition.record(delete) / beta.feedback.create(resolve) / admin.cost.record(delete)。pytest 7 passed + vitest 8 件追加 (bundle-j 84 passed) + 実操作監査 .audit-t01-dashboard.mjs 3 連続 ALL PASS (25 チェック — DB/audit 突合) | — | 2026-07-19 |
| ~~GAP-018~~ ✅解消 (2026-08-10) | S-N01 商談ドラフトの未実装機能群 | **全 4 項目実装済** (migration t-d-99zl: workflow_stage_enum に contract/nda/invoice + sales_doc_sends + knowledge_references referrer_type=sales_doc): ① **doc_type 拡張** — 5 実タブ (提案書/見積書/業務委託契約/NDA/請求書、実件数バッジ) + 全 doc_type CRUD ② **PDF 出力** — GET /sales-docs/{id}/pdf (reportlab 内蔵 CID フォント HeiseiKakuGo-W5 で日本語実描画、本文空 409、実ブラウザ DL で %PDF- 突合)。**メール送信** — 送信ダイアログ → POST /send (既存 ResendSender。メール未設定環境は dry_run=true を応答/履歴/UI で明示 — 偽の送信成功を出さない)。**送信履歴** = sales_doc_sends 実描画 (宛先/日付/dry-run バッジ) ③ **生成トレース** — meta (generated_by=tony/model/inputs/knowledge_refs/steps) + knowledge_references 実記録。生成プロセスカードは**実行した工程のみ**表示 (実行していないナターシャ価格レビュー等は出さない)、参照ナレッジカードは実 RAG hit のみ、トレース無し手動ドキュメントは「（参考手順）」明示 ④ **AI 生成** — 「トニーにドラフト生成を依頼」(明示操作起点 — 自動生成しない) → 実ナレッジ RAG (search_knowledge、embeddings 未設定時は観点別 ilike 検索マージ — **監査開発中に長い合成クエリで 0 hit になる実バグを検出・是正**) → LLM が doc_type 別 Markdown 生成 (未確定情報は「(要確認)」指示、AI 補助ドラフト文言必須)。LLM 未設定 503 + 「AI を使わず保存」導線を併設。audit sales_doc.generate/send、R-T08 404。pytest 5 件追加 (16 passed) + vitest 10 件 (24 passed) + 実操作監査 .audit-n01-drafts.mjs 3 連続 ALL PASS (21 チェック — DB/PDF バイナリ/audit 突合) | — | 2026-07-19 |
| ~~GAP-024~~ ✅解消 (2026-08-10) | S-H01 モックビューアの「編集」ボタン + バージョンパネル「…」メニュー | **実装済 — 確定アーキ (decision_log / selected-libraries.md) の Open Design パターン準拠**: モック生成・反復は「自然言語指示 → AI が HTML を生成」であり、担当はデザイナー AI 社員ワンダ (S-H01 バージョン author「ワンダ（更新）」の実体)。人間の HTML 手編集ではない。①「編集」= ワンダへの修正依頼 — POST /mocks/{id}/revise (services/mocks/revise.py): 現行 HTML を署名 DL → LLM (AnthropicClient, ATELIER_DESIGN_MODEL 既定 claude-sonnet-4-6) が指示に従い全文改訂 → 新パスへ署名 UL → 新バージョン行 (parent 連鎖 + meta author=wanda/revision_instruction/revised_from_version/model)。ANTHROPIC_API_KEY 未設定は 503 llm_unconfigured (偽の改訂を出さない)、テストのみ ATELIER_ALLOW_FAKE_LLM=1 の決定的スタブ (指示バナー可視挿入で実体変化を E2E 検証可能に)、60k 字超 413。S-H01 ツールバーに「編集」→ 指示ダイアログ (ワンダに修正を依頼・実行中表示) → 成功で新バージョンへ自動遷移、パネルに「ワンダ（更新）」+ 修正指示表示 ② バージョン操作 (「…」メニュー相当) — POST /mocks/{id}/duplicate (同一 HTML 参照の新バージョン + 「vN の複製」表示、コピー偽装しない) / POST /mocks/{id}/discard (soft delete、2 段階確認、唯一の生存バージョンは 409 → honest エラー、表示中破棄は残存最新へ自動遷移)。audit mock.version_create/duplicate/discard、R-T08 越境 404。pytest 4 追加 (12 passed) + vitest 5 追加 (14 passed) + 実操作監査 .audit-h01-edit.mjs 3 連続 ALL PASS (14 チェック — storage 実体の改訂バナー/DB meta/parent 連鎖/2 段階/409 を突合) | — | 2026-08-01 |
| ~~GAP-025~~ ✅解消 (2026-08-04) | S-I02 タスク詳細の未描画要素群 | **全 4 項目実装済** (migration t-d-99zi: tasks.verifier_employee_id + task_execution_tests): ① 「あなたへの確認」仕様変更 3 択 — 実検知 = 紐づくモックと同一画面の新バージョンが後からアップロードされた状態のみ (GET /tasks/{id}/spec-changes、推測イベント無し)。POST .../resolve の 3 択 (2 段階確認): adopt=mock_id を最新へ差替 / split=実フォロータスク起票 (仕様変更フォロー・triage・見積未実施 1h 明示) / discard=blocked+dispatch 解除 (再分解待ち)。解決は metadata に記録し再表示なし、audit task.spec_change.* ② テスト結果タブ — task_execution_tests (Bridge complete の metadata.tests が記録) + GET /executions/{id}/tests (RLS member select のみ、未記録は誠実表示) ③ 関連資料タブ — GET /tasks/{id}/related (mock 実バージョン/spec/受入条件 実項目数/ブランチ+変更ファイル/参照ナレッジ = knowledge_references referrer_type='task'。**実リンクのみ**) ④ メタ行 — 検証担当 (verifier_employee_id、PATCH 設定・他 WS 422・未設定「未割当」)/見積・経過 (実 executions duration 合計 — TaskExecution.duration_seconds 追加)/変更ファイル数 (files_changed を complete が列永続、0 は「記録なし」)。pytest 6 + 回帰 62 passed + vitest 4 (11 passed) + 実操作監査 .audit-i02-detail.mjs 3 連続 ALL PASS (14 チェック — adopt/split/discard・tests rows・related 実リンク・メタを DB 突合) | — | 2026-08-01 |
| ~~GAP-026~~ ✅解消 (2026-08-04) | S-I03 実行モニターの未描画要素群 | **全 6 項目実装済** (migration t-d-99zh: dispatch_control + bridge_workers + tasks.dispatch_promoted_at): ① Bridge 接続バッジ — Bridge アプリ (apps/bridge) が毎 poll POST /bridge/ping (BridgeAuth) で presence を upsert、/bridge/status の workers (90 秒以内=接続中) から「接続中 · Bridge vX · host」/「Bridge 未接続」を誠実表示 ② 同時実行枠 = 実 parallel_limit/available_slots、すべて一時停止 = POST /dispatch/pause|resume (paused 中は実 /kanban/pick が no_available_task — 実行中は継続)、順番待ちから 1 件追加 = POST /dispatch/promote (最古 queued を dispatch_promoted_at 昇格 → pick 最優先。Bridge がローカル起動する構造のため API は spawn しない誠実文言) ③ キュー取消 = POST /tasks/{id}/dispatch-cancel (queued 限定 409、ready へ) ④ セッション停止 = POST /tasks/{id}/dispatch-stop (2 段階確認 — reclaimed+blocked+execution cancelled) ⑤ 表示方法 カード/一覧/ログ集約 — 集約 = GET /executions-events (実 task_executions 由来の開始/終了イベント、推測ログなし) ⑥ 経過 = 実 started_at の実測 tick、残りは「見積比 残り N 分 / 見積超過 +N 分」(estimated_hours 由来と明示)。audit dispatch.paused/resumed/promoted/cancelled/stopped、R-T08 越境 404。pytest 6 + dispatcher 回帰 12 + bridge 24 + vitest 5 + 実操作監査 .audit-i03-dispatch-ops.mjs 3 連続 ALL PASS (**監査が実プロジェクトタスクへの誤停止を検出し card 内 locator に是正 → 原状復帰済**) | — | 2026-08-01 |
| ~~GAP-027~~ ✅全解消 (②2026-08-04) | S-L01 クライアント招待の「再送」ボタン + 「使用回数」列 | ① **再送は実装済** (2026-08-03): POST /client-invitations/{id}/resend — token は hash 保存のため「再送 = 新 token へローテーション (旧リンク失効) + 新リンクをメール送付 + 新 raw token を発行バナーで 1 度だけ表示」。pending のみ対象 (使用済/失効/期限切れは 409)、2 段階確認 UI、audit `client_invitation.resend`。pytest + vitest + 実操作監査 3 連続 ALL PASS ② **使用回数も実装済** (2026-08-04): client_invitations.use_count (migration t-d-99zd — backfill は実 audit client.signin の集計から) をポータルサインイン成功ごとに増分 (失敗は増えない)。ClientInvitation 応答に use_count、S-L01 の「使用日」列をモック原文どおり「使用回数」(N 回) に復元。pytest 2 + vitest + 実操作監査 .audit-l01-usecount.mjs 3 連続 ALL PASS (S-L02 実サインイン ×2 → use_count=2 → UI「2 回」) | — | 2026-08-01 |
| ~~GAP-028~~ ✅解消 (2026-08-04) | S-L02 クライアントサインインの招待プレビュー + 同意永続 | **両方実装済**: ① POST /client/auth/preview — 招待トークンの署名前プレビュー (メタ限定: 招待元 workspace 名・オーナー表示名 (未設定は null — 推測で埋めない)・プロジェクト名・招待先メール・expires_at・remaining_days のみ。内部 ID/scopes は返さない。10/min/ip レート制限、トークンは body で受け URL に載せない、read-only — use_count/used_at/同意は不変)。S-L02 は URL トークン有効時にモック忠実の greeting-card (招待元行+project-card)・実「残り N 日」有効期限バー・招待先メール (disabled) を実描画、無効/期限切れは誠実なエラーバナーのみでプレビューカード非描画 ② 同意 2 種のサーバー永続 — /client/auth/signin が agree_legal+agree_confidential 必須 (欠落 422、JWT 発行なし)、client_invitations.legal_consented_at/confidential_consented_at (migration t-d-99zg) に初回同意時刻を記録し再サインインで上書きしない (法務証跡)、audit client.signin に consents。pytest 6 (preview メタ限定/read-only/401/410/422/同意不変) + vitest 5 + 実操作監査 .audit-l02-preview.mjs 3 連続 ALL PASS (実 UI 通し + DB 突合) | — | 2026-08-01 |
| ~~GAP-029~~ ✅解消 (2026-08-11・経営者承認済) | S-L03 クライアントポータルのコンテンツ未提供 | アクセス範囲カード等で honest 表示: client-portal API は GET /client/projects/{id} (id/name/description/scopes/表示名) のみで、モックの成果物一覧・フェーズ進捗バー・モックギャラリー・コメント一覧・リンク残日数を出す client スコープ read API が無い (staff API は R-T08 で別 JWT 系統のため使えない) | **実装済 (R-T08 経営者承認をチャットで取得後に着手)**: GET /client/projects/{id}/overview (実 phases + 実進捗% + 運営 + 招待リンク実有効期限) / outputs (stage 毎最新版 + 実在フォーマットのみ) / mocks (画面毎最新版) / comments (自分の招待のコメント + 運営返信のみ — 他クライアント・社内スレッド非開示) + POST comments (comment スコープ必須、author_invitation_id 記録、audit client.comment.create、他 project target は存在ごと秘匿 404、IP rate limit 30/min)。S-L03 UI に進捗バー/リンク残日数/成果物/モックギャラリー/コメント+投稿フォーム (view-only では非表示) を実データで描画。R-T08 越境試験: pytest 8 本 (全 endpoint 403 + target 404 + スコープ強制) + 実操作監査 .audit-l03-content.mjs 24 チェック 3 連続 ALL PASS | 2026-08-01 |
| ~~GAP-031~~ ✅解消 (2026-08-10) | **Gate #6 実照合で検出した記録漏れモック要素群** (CI 化 (GAP-102) の初回実走で検出 — 画面監査時に判断はされたが正確な記録が無かった、または見落とされた要素) | 未描画/未記録: ① S-E01 チャット — **「役立った」は実装済 (2026-08-03)**: 実は POST /chat/messages/{id}/feedback (T-A-19) が既存で UI 未配線だっただけ。アクション行 (役立った/コピー) を実装し audit_logs 突合で 3 連続 PASS (.audit-e01-feedback.mjs)。**「分岐」も実装済 (2026-08-04)**: POST /chat/messages/{id}/branch — 分岐点までの履歴を新スレッド「分岐: …」へコピー (parent_message_id 連鎖 + 先頭は分岐元への系譜ポインタ、元スレッド不変、audit chat_thread.branch、R-T08 越境 404)。**監査が実バグ 2 件を検出・修正**: 同一トランザクション内 user/assistant の created_at 同値 (now()=transaction timestamp) によるタプル境界取り漏れ + スレッド表示順の曖昧さ → SSE insert を clock_timestamp() 明示に是正。pytest 2 + vitest 3 + 実操作監査 .audit-e01-branch.mjs 3 連続 ALL PASS。**「承認して実行」も実装済 (2026-08-04)**: 書込ツールの人間承認ゲート — save_deliverable (APPROVAL_REQUIRED_TOOLS) は SSE agentic ループで即実行せず approval_inbox (type=tool_execution, target_type=chat_thread — migration t-d-99zf) に pending 起票し、LLM には「実行済みの体で回答しない」旨の tool_result を返す (無承認自動実行の廃止)。POST /chat/tool-approvals/{id}/execute (pending 検証 → execute_atelier_tool 実実行 → approved+resolution_note+tool メッセージ、二重実行 409) / POST .../reject (rejected + 差戻 system メッセージ、実行されない) / GET /chat/tool-approvals。S-E01 スレッド末尾に承認カード (title+tool_input 表示、承認して実行 / 差戻 / Inbox で確認 = /approvals 実リンク)、承認後は実行結果を実描画。audit chat_tool.approval_requested/executed/rejected。pytest 3 + vitest 3 + 実操作監査 .audit-e01-tool-approval.mjs 3 連続 ALL PASS (承認 → 実 knowledge 生成 DB 突合 / 差戻 → 生成されない)。**GAP-031 は ①〜⑦ 全て解消済み (最終 ⑤ 2026-08-10)** ② **S-F01 工程直接操作は実装済 (2026-08-04)**: 「この工程を完了として承認」(completed のみ — 自動開始しない)/「次工程（X）を開始」(進行中がある間 disabled、承認後 enabled)/「前工程の成果物を見る」(前工程選択+成果物タブ実切替)。vitest 3 + 実操作監査 .audit-f01-i03-ops.mjs 3 連続 ALL PASS ③ **S-I03 並び替えは実装済 (2026-08-04)**: 「要対応が上」(既定・区分表示)/「新しい順」(結合グリッド updated_at 降順)/「進捗順」(最新実行スコア降順)。vitest 2 + 同監査 3 連続 ALL PASS ④ **S-T02 再取込は実装済 (2026-08-04)**: POST /admin/skills/reimport — ATELIER_SKILLS_DIR (既定 ~/.claude/skills) を走査し SKILL.md frontmatter を解析して name キーで upsert (新規/更新/変更なしの summary、is_active 等の運用設定は上書きしない、dir 不在 409、audit skill.reimport)。2 段階確認 UI + 結果バナー。pytest 3 + vitest + 実操作監査 .audit-t02-t06-import.mjs 3 連続 ALL PASS ⑤ **S-T03 テンプレ編集は実装済 (2026-08-10)**: T-A-42 read-only 契約を仕様変更プロトコルで先行 expand (tickets.json T-A-42 note + files_changed_predicted 追記、validate 216/216) した上で、PATCH /admin/ai-employee-templates/{id} (部分更新 — display_name/department/role/system_prompt/specialty/default_skills(uuid[])/default_knowledge_cats、保存のたび version 自動 increment、audit template.update、空 body 422、admin gate + service session — RESTRICTIVE no_update のため) + GET .../deployment (ai_employees.template_id 参照の実カウント、archived 除外) を実装。UI はモック忠実の一覧+エディタ 2 ペイン — 基本情報/System Prompt (ADMIN ONLY)/専門領域/「スキル追加」(実 /admin/skills 由来候補)/「カテゴリ追加」/「保存して全 WS 反映」+ 実展開先「展開先：N WS（自動同期）」。変更なし保存は「変更がありません。」で honest ブロック。テンプレ「追加」・「複製」・バージョン履歴「復元」はモックにあるが本 scope 外 (死にボタンを出さない — 別途起票、tickets.json note に記録)。pytest 19 + vitest 88 (bundle-j) + 実操作監査 .audit-t03-template-edit.mjs 3 連続 ALL PASS (DB 突合: 部分更新/version 1→2→3/uuid[]/text[]/audit/展開先実カウント) ⑥ **S-T04 サポート連絡は実装済 (2026-08-04)**: POST /admin/support-contact (admin gate — 既存 ResendSender で送信、メール未設定環境は dry_run=true を応答で明示、audit support.contact 記録、退会予約中ユーザーも対象) + GET /admin/support-contacts (「最近のサポート対応」= 実 audit 逆引き)。行ボタン → 件名/本文ダイアログ → 結果バナー。pytest 3 + vitest 2 + 実操作監査 .audit-t04-support.mjs 3 連続 ALL PASS ⑦ **S-T06 SKILL.md 取込は実装済 (2026-08-04)**: .md 選択 → frontmatter (name/description) をクライアント側で解析 → 追加ダイアログへプレフィル → 人間確認後に既存 POST /admin/knowledge で platform 登録 (無確認書込なし)。vitest 3 + 同監査 3 連続 ALL PASS | 各 API/フローの起票実装 (フィードバック記録・tool 承認・工程直接操作・ソート状態・skills 再取込・テンプレ編集・サポート導線・SKILL.md パーサ取込) | 2026-08-03 |
| ~~GAP-030~~ ✅解消 (2026-08-03) | **Bridge 実行プロンプトにタスク内容/仕様が含まれない** | **是正済**: ① /kanban/pick 応答に task_title/task_description/assigned_employee を追加 (OpenAPI + 型再生成) ② dispatcher の既定プロンプトをタスク内容入り + 「参照不能な情報を探しに行かない」明示に刷新 (buildDefaultPrompt) ③ ラッパー無しのデフォルト経路で e2e 通し 19/19 PASS を実証 (旧: ID のみで子 Claude が長考→240s タイムアウト) ④ pty.ts スタブと「未実装を確認するテスト」を撤去 ⑤ bridge + 6 packages の `lint: echo placeholder` を実 ESLint (.eslintrc.workspace.json) に配線 | — | 2026-08-01 |
| ~~GAP-016~~ ✅解消 (2026-08-03) | **議事録パイプラインの Whisper worker が全リポジトリに不在** | **消費者を実装**: `apps/api/src/services/meetings/worker.py` (queue 消費 → storage 署名 DL → OpenAI Whisper API → 結果 JSON を `transcripts/results/{id}.json` へ UL → parse_result_path 差替+parsed_at 打刻 → audit `meeting.transcribe.complete`。失敗時は parse_error 記録+audit、force 再キューで再試行)。起動経路 2 つ: Inngest cron `transcribe-queue` (毎分) + 単独プロセス `python -m src.services.meetings.worker`。unit 11 本 (orchestration/HTTP 境界/queue query/loop) で検証。**実 Whisper 呼出は ATELIER_OPENAI_API_KEY + storage 設定が前提** (dev 未設定 — 本番投入時に env 設定のみで稼働) | — (GAP-015 構造化解析は別 gap のまま) | 2026-07-19 |
| ~~GAP-113~~ ✅解消 (2026-08-16) | **チャット LLM をオーナーの Claude サブスク (Agent SDK) で動かす経路が無い** — S-E01 チャットは ANTHROPIC_API_KEY (従量課金) のみで、オーナーが自分の Pro/Max サブスク枠で会話する手段が無かった | **実装済**: `ATELIER_LLM_PROVIDER=agent_sdk` の明示 opt-in で chat_sse の LLM 呼出を claude-agent-sdk (公式・サブスク認証は [公式ヘルプ 15036540](https://support.claude.com/en/articles/15036540) でパーソナルユース許可) 経路に切替する adapter (`services/chat_sse/agent_sdk.py`)。① include_partial_messages の text_delta を逐次 SSE 配信 (partial 不達時は完成 AssistantMessage で代替) ② 子プロセス env から ANTHROPIC_API_KEY/ANTHROPIC_AUTH_TOKEN/CLAUDE_CODE_API_KEY を必ず除去 (黙って従量課金へ流れる事故の閉塞 — ccstart.sh:113 と同方針) ③ CLI ツール全禁止 (allowed_tools=[]) + max_turns=1 ④ opt-in なのに SDK 不在なら黙って API/fake に落とさず SSE error で誠実停止 ⑤ claude-agent-sdk は optional dep (mcp SDK と同じ遅延 import 方針、CI 不要)。**規約上の線引き**: サブスク認証はパーソナルユース限定のため本モードは**セルフホスト個人インスタンス専用** — 顧客が使うホスト版では有効化禁止 (BYOK/API キーを使う。.env.example に明記)。**v1 制限 (誠実記録)**: Atelier ツール (save_deliverable 等 agentic ループ) は本モード未注入 — テキスト+RAG/ペルソナのみ。ツール注入は Agent SDK の in-process MCP (create_sdk_mcp_server) で追実装可能 (必要になったら別 gap 起票)。pytest 11 (opt-in 判定/SDK 検出/prompt 畳込/env 除去/partial・fallback stream/model env/stream_chat 配線 2 経路) 全 PASS | — | 2026-08-16 |
| ~~GAP-114~~ ✅解消 (2026-08-16) | **各ユーザーが自分の Claude 月額プランでアプリ内チャットを使う経路が無い** — GAP-113 (Agent SDK 直結) はセルフホスト個人専用で、ホスト版の各ユーザーには適用できない (サブスク認証のパーソナルユース制約)。Anthropic の third-party 認証制度は 2026-06-15 から一時停止中 | **実装済 (ローカル実行リレー)**: `ATELIER_LLM_PROVIDER=relay` で chat_sse の LLM 実行をサーバーで行わず、**各ユーザー PC の Bridge に中継** — Bridge がその PC の Claude ログイン (= 本人のプラン) で `claude -p --output-format stream-json --include-partial-messages` を実行し text delta を返送する (プロンプトも実行もログインも本人の PC 内で完結 = 既存 Bridge タスク実行と同じ規約上の立場)。① migration t-d-99zq: chat_relay_jobs (queued→running→done/error/expired) + chat_relay_chunks、RLS = requested_by 本人のみ SELECT / 書込は service session のみ ② dispatcher に BridgeAuth 3 endpoint (/chat-relay/pick = for-update-skip-locked claim / {id}/chunks / {id}/complete、audit chat_relay.enqueue/pick/complete/expire) ③ SSE 側 (services/chat_sse/relay.py): presence 90 秒鮮度チェック → 不在なら「Bridge がオフライン」と**誠実エラー (黙って API 課金へ切替えない)** → enqueue 即 commit → 0.25s ポーリング中継 → タイムアウト (既定 180s) で expired ④ Bridge (apps/bridge/src/chat-relay.ts): タスク claim と独立の 1s ループ、子プロセス env から ANTHROPIC_API_KEY/ANTHROPIC_AUTH_TOKEN/CLAUDE_CODE_API_KEY を必ず除去 (課金事故閉塞)、chunks は 300ms バッチ直列送信、partial 不達時は完成 assistant text で代替 ⑤ **BYOK は API のみ残し UI は出さない** (経営者指示「API 接続は一旦非表示・削除はしない」— UI はもともと未実装のため現状維持を明文化) ⑥ v1 制限 (誠実記録): リレーモードでは Atelier ツール (agentic 実行) 未注入 / Bridge オフライン時は送信不可 (エラー表示)。Anthropic の third-party 認証制度が再開したら「プランを接続」ボタン方式へ移行する (週次ウォッチ Routine 設定済)。pytest 9 (HTTP 一周/401/409/404/RLS 越境/SSE 中継 e2e/オフライン/エラー/expired) + wiring 2 + vitest 13 (parse/env 除去/args/worker 4 経路/headless 組込 3) 全 PASS | — | 2026-08-16 |
| ~~GAP-115~~ ✅解消 (2026-08-17) | **Stripe checkout でメールが自動入力されない** (登録済みなのに手入力させられる — Mac 実機テストで経営者が検出) | **実装済**: POST /billing/checkout が実行ユーザーの public.users.email を customer_email として Checkout Session に渡す (取得失敗時は未入力のまま Stripe 側で入力させる誠実 fallback)。pytest で Stripe へ送る form に customer_email が入ることを検証 (27 passed) | — | 2026-08-17 |
| ~~GAP-116~~ ✅解消 (2026-08-17) | **S-A03 の「タブ」が実際は縦積み全表示** (タブ見出しがあるのに全節が下スクロールで同時に見える — 経営者指示の仕様変更: タブごとに画面を切り替える) | **実装済**: 基本情報/メンバー/MCPトークン/AI学習/プラン を tablist + tabpanel (hidden 切替 — unmount しないためフォーム状態と Stripe 戻りポーリングを保持) に変更。招待管理/退会の実ページリンクは tablist の外 (ARIA: tablist の子は tab のみ — axe critical を実測して是正)。Stripe から戻った時 (成功/キャンセルとも) はプランタブを自動で開く (これまで戻っても基本情報タブで決済結果が見えなかった)。AI学習タブに保存ボタンを追加 (タブ分離で基本情報の保存が同時に見えなくなったため)。**追補 (2026-08-17 経営者指摘 2 件)**: ① hidden 属性が CSS grid に負けて実ブラウザではタブが切り替わらない実バグ → クラス側でも display 切替 + **実ブラウザ検証を必須化** ② 招待管理・退会が別ページ遷移でタブ文脈を失う → 両方タブパネル化 (招待管理 = S-L01 実体をプロジェクト選択付きで埋め込み / 退会 = WS 削除 danger zone を移設 + アカウント退会導線)。TopBar のプロジェクトピルの絵文字を lucide アイコンに置換 (絵文字禁止指示)。実ブラウザで 7 タブ全切替 + URL 不変を実測。vitest 更新+新規 4 (タブ切替/Stripe 戻り/tablist 構成) + a11y axe PASS | — | 2026-08-17 |
| ~~GAP-117~~ ✅解消 (2026-08-17) | **プロジェクトに入っても WS ナビが混在し文脈が分離されていない** (経営者指示の IA 変更: プロジェクト内はプロジェクト専用の環境にし、全体へ戻る導線を明示する) | **実装済**: ConditionalAppShell で文脈を判定 — プロジェクト系画面 (dashboard/工程/タスク/チャット/議事録/スケジュール/営業/モック/シークレット/設定) では**プロジェクト nav のみ** + 先頭に「← ワークスペース全体へ」導線、WS 系画面 (プロジェクト一覧/AI社員/ナレッジ/承認待ち/WS設定) では **WS nav のみ** (プロジェクトを記憶していても混ぜない)。TopBar にプロジェクト文脈ピル (📁 プロジェクト名) を表示。vitest 新規 5 (文脈分離 3 + TopBar ピル 2) 全 PASS | — | 2026-08-17 |
| ~~GAP-124~~ ✅解消 (2026-08-17) | **① agent_sdk モードでプラン枠 (5h/7日) が表示されない ② 「Bridge 未接続」等の技術用語が一般ユーザーに不親切 ③ 社員セクションが開閉できない** (経営者指摘 3 点 + 「オーナーのプランで実行は本当か」への実証要求) | **実装済**: ① agent_sdk 経路でも CLI の RateLimitEvent (5h/7日枠の実測使用率) を収集し chat_plan_status へ記録 — relay (GAP-119) と同じ `record_plan_status_for_user` に合流 (best-effort・チャット応答は壊さない・観測なしは書かない)。次のチャット実行から接続パネルに実測バーが出る ② 接続表示を平易化 — チップ「Bridge 未接続」→「プラン未接続 — ここから接続」/「自分のプランで実行中」、パネル「Bridge 接続中」→「お使いのパソコンと接続中」、詳細説明は「あなたの Claude 月額プランの範囲内で実行・追加の従量課金なし」等の一般語 (Bridge は「接続アプリ (Atelier Bridge)」として初出のみ) ③ スレッド一覧の AI 社員セクションをトグル開閉可能に (aria-expanded + 回転シェブロン・既定すべて開)。実ブラウザで 開閉往復・新文言・観測済みユーザーのプラン枠バー表示を実測。pytest 14 (RateLimitEvent 収集 1 追加) + web vitest 625 / tsc 0 / eslint 0 / build PASS | — | 2026-08-17 |
| ~~GAP-123~~ ✅解消 (2026-08-17) | **チャット未選択時の右ペインが空白 + スレッド一覧が AI 社員階層になっていない** (経営者指摘: 「右側の空白が気になる」「スレッド一覧 > AI 社員 > 各スレッドになっているのか」) | **実装済**: ① 未選択の右ペインを「AI 社員クイックスタート」に — 実 /ai-employees の社員カード一覧、クリックで即スレッド作成 → 開く (プロジェクト文脈があればそのまま、無ければ選択 UI + 未選択時はカード無効化で理由明示 = 死にボタン禁止準拠)。作成失敗は誠実エラー ② スレッド一覧を **AI 社員ごとの階層** に再構成 — 社員見出し (アバター + 名前 + 件数) 配下に当該社員のスレッド (更新順)、社員は直近更新順。旧・工程グルーピングの文脈は各カード内の工程チップで維持。カード内の社員名重複は排除 ③ 実ブラウザ検証: プロジェクト文脈でクイックスタート 10 名表示 → ワンダをクリック → 即スレッド開始 → 一覧がワンダ/ジャービスの社員見出しでグルーピングされることを実測。vitest 新規 4 (社員階層 1 + クイックスタート 3) — 625 passed / tsc 0 / eslint 0 / build PASS | — | 2026-08-17 |
| ~~GAP-122~~ ✅解消 (2026-08-17) | **Bridge 接続に手作業が必要 (共通トークン + 手動起動)** (経営者指示: 「Claude Code のようにボタンひとつで。Mac だけでなく全デスクトップ (スマホ除外)」) | **実装済 (ワンクリック接続)**: ① migration t-d-99zs: bridge_user_tokens (sha256 hash のみ保存・RLS 本人 SELECT) + bridge_workers.user_id ② **ユーザー別トークン API**: POST/GET /bridge-tokens + /revoke (raw は発行応答で 1 度だけ・失効は冪等・他人は 404、audit bridge_token.issue/revoke) ③ **二段 Bridge 認証**: X-Bridge-Token をインスタンス トークン (全権) / ユーザー トークンの二段照合 (hmac 定数時間比較 + hash 照合)。**ユーザー トークンは chat-relay + ping 限定** — kanban 系は 403 (過剰権限防止)、chat-relay pick は requested_by=本人の job のみ (他人のプロンプトが他人の PC に流れない)、presence は user_id 紐付け・接続状態パネルは自分の worker のみ表示 ④ **接続フロー UI**: 未接続パネル = ダウンロード (Releases・Mac/Win/Linux) → 「接続トークンを発行」→ 「アプリで接続」(atelier-bridge:// ディープリンク・トークン自動設定) + fallback の実トークン入りコマンドコピー ⑤ **Electron**: atelier-bridge:// スキーム登録 (builder protocols + setAsDefaultProtocolClient)、open-url (Mac) / second-instance argv (Win/Linux) 受信 → 設定保存 (~/.atelier-bridge.json mode 0600・api は http(s) のみ受理) → 再起動でループ開始。headless は env 優先 + 保存設定フォールバック ⑥ **通しが実バグ 2 件を検出・是正**: user トークンの Bridge が kanban 403 で fatal 終了 → チャット専用降格で常駐 / 降格後 presence が途絶える → 専用 ping ループで維持 ⑦ 3 OS パッケージング workflow (bridge-dist.yml — macos/windows/ubuntu ランナー、タグで Release 添付。**Mac DMG はコンテナでは作れないため CI ビルド**)。**実測**: 実 UI トークン発行 → 実 URL を deep-link モジュールで解釈・保存 → env なし起動 → user worker の presence → 実チャット一周 (job が user worker に pick — DB 突合)。**未実測 (誠実記録)**: OS レベルのスキーム起動はパッケージ済アプリでのみ検証可能 — 初回 DMG/EXE/AppImage を CI で作った後に実機確認。pytest 2 追加 (発行/失効/権限分離/本人 pick — 27 passed) + bridge vitest 10 追加 (59 passed) + web vitest 621 / ruff・pyright・eslint・tsc 0 | 初回配布物: Actions で bridge-dist を実行 → Releases に添付 | 2026-08-17 |
| ~~GAP-121~~ ✅解消 (2026-08-17 確定形に改訂) | **進捗ゲージが単色ブランド色** (経営者指示 v1「グラデーションっぽく鮮やかに」→ 確定形 v2「**1 本の中にグラデーションはかけない。単色で、工程が進むごとに色がグラデーション的に進む。動きも不要**」) | **実装済 (トークン化)**: design-tokens に `progressRamp` (teal #14B8A6 → lime #84CC16 → amber #F59E0B) + 補間関数 `progressColor(t)` を新設 — 塗りは常に単色で、進捗 t に応じてランプ上の色を返す (コンポーネントに色ハードコードなし)。適用 5 種: プロジェクトカード 9 段工程バー (セグメント i の色 = ramp(i/8)) / S-F01 全体進捗 / かんばん完了% / タスク詳細 AC 達成率・実行スコア (いずれも進捗率で色決定)。v1 で付けた pulse は削除、アップロード進行バーはゲージ対象外として原状復帰。「要対応」は semantic に secondary 単色維持。DESIGN-atelier.md を確定形で改稿。tokens 単体テストで端点/中点/クランプを厳密検証 (20 passed)、実ブラウザで単色 (backgroundImage=none)・動きなしを実測。vitest 621 全 PASS / tsc 0 / build PASS | — | 2026-08-17 |
| ~~GAP-120~~ ✅解消 (2026-08-17) | **ブランドロゴ未反映 + メインカラーがロゴと不一致** (経営者支給の公式ロゴ SVG 一式を反映し、Primary をロゴ色に全面変更する指示。「ハードコードなの？？気をつけてよ」→ 色のハードコード一掃も含む) | **実装済**: ① 公式ロゴ SVG 5 種を public/brand/ に配置 (logo-mark/vertical/horizontal + bg 変種)。サイドバー・サインイン・公開ヘッダの「A」ブロックを実ロゴに差替え、favicon (app/icon.png) をマークから生成 ② Primary を Vivid Sapphire #2563EB → **Atelier Olive #494535** (公式 SVG の実測支配色) に変更 — 正本 DESIGN-atelier.md を先行更新 (原則 5 も「工房の落ち着き」に改稿、WCAG 実測 9.57:1 AAA) → design-tokens (index.ts/tokens.css) → 全ソース ③ **ハードコード是正**: 散在していた色リテラル 74 箇所 (41 ファイル) を掃討 — `hover:bg-[#hex]`→`hover:bg-primary-hover` (新トークン primaryHover)、`shadow-[0_0_0_3px_#hex]`→`shadow-focus-ring` (tailwind boxShadow トークン)、inline style / SVG stroke のリテラルは `@atelier/design-tokens` の colors.primary 参照へ。残リテラル 0 を grep で確認 ④ 実ブラウザ検証: サインイン/プロジェクト一覧で実ロゴ描画・primary 実色 rgb(73,69,53)・favicon 200 を実測。vitest 621 + tokens 18 全 PASS / tsc 0 / eslint 0 / next build PASS | — | 2026-08-17 |
| ~~GAP-119~~ ✅解消 (2026-08-17) | **Claude プラン接続の状態が UI から確認できない** (経営者指示「月額プランとの接続を確認できる部分」「接続のフロー」「5h/1w のトークン何%か・接続中かどうか」) | **実装済 (実測値のみの誠実設計)**: ① 調査結論 — claude CLI は実行中にプラン枠の状態変化で `rate_limit_event` (status / rate_limit_type=five_hour・seven_day / **utilization 0.0-1.0** / resets_at — API 応答ヘッダー由来の実値) を stream-json に発行する (Agent SDK の RateLimitEvent と同一実体。バイナリ実地確認)。**オンデマンド取得は不可** — 観測はチャット実行時のみ。よって「直近実行時点のスナップショット」として observed_at 付きで表示し、未観測値は出さない ② migration t-d-99zr: chat_plan_status (user_id pk / status / 5h・7日 utilization+resets_at / observed_at、RLS = 本人 SELECT のみ・書込は service session) ③ Bridge: stream の rate_limit_event を解析し window 別最新値を complete に同送 (観測ゼロなら送らない) → dispatcher が requested_by の行へ upsert ④ GET /chat/connection-status (認証・RLS): mode (relay/agent_sdk/api/fake/unconfigured — chat_sse と同一判定) + Bridge presence 90 秒鮮度 (host/version/last_seen) + 本人の直近 relay 実行 + プラン枠観測値 ⑤ S-E01 に接続状態チップ (relay: 緑「自分のプランで実行」/ 赤「Bridge 未接続」、他モードは実行主体を明示 — api は「従量課金」明記) + 詳細パネル (モード説明 / Bridge worker / **5h・7日の使用率バー + リセット時刻 + 観測時点の明示** / 直近実行 / 30 秒自動更新 + 再確認) ⑥ **接続フロー**: Bridge 未接続時に手順 3 ステップ + headless 起動コマンドのコピー + 「認証情報は PC の外に出ない」明示。スレッド未選択画面にもチップ配置 (未接続だと送信自体ができないため)。**実ブラウザ通し実測**: 実 relay パイプ (web→API→キュー→実 Bridge→偽 claude が rate_limit_event 発行→complete) で「未計測の誠実案内 → チャット 1 回実行 → 5h 42%/7日 10% バー実描画 + 観測時点表示」を実測 (gap119-panel-before/after.png)。追補: サイドバーのスレッドプレビューに生 markdown 記号が見えていたのを stripMarkdown で平文化 (GAP-118 の取り零し)。openapi 1 endpoint + スキーマ 5 宣言 + 型再生成 (Gate #5/#7)。pytest 4 追加 (upsert/観測なし無書込/401/実測値+R-T08 他人不可視 — 13 passed) + bridge vitest 3 追加 (15 passed、touched 93%+) + web vitest 6 追加 (621 passed) | — | 2026-08-17 |
| ~~GAP-118~~ ✅解消 (2026-08-17) | **S-E01 チャットが markdown を生テキストのまま表示** (whitespace-pre-wrap 直描画のため `##` や `\| --- \|` が記号のまま見える — 経営者指示「md 形式だけではなくいろんなもので出せる状態に。ライブラリを徹底調査の上で」) | **実装済 (react-markdown + remark-gfm + rehype-highlight 採用)**: ライブラリ調査の結論 — react-markdown はデファクトで**既定で raw HTML を描画しない (XSS 安全)** + 要素ごとに自前コンポーネントを差し込める。不採用: streamdown (制御余地小/バンドル大) / @assistant-ui markdown アドオン (同社 runtime への全面移行が前提) / marked・markdown-it 直 (sanitize 自前保持のリスク)。`MessageContent.tsx` 新設 — 見出し (h1→h3 縮退で視覚階層維持)/リスト/GFM 表 (横スクロールコンテナ)/引用/リンク (新規タブ+noopener)/取消線/タスクリストを Atelier トークンで実描画。**コードブロックは言語ラベル + 「コピー」ボタン付きダークカード** (highlight.js 配色は globals.css の .chat-md スコープ、コピーは末尾改行を落とした生コード + 「コピー済み」フィードバック)、インラインコードはチップ。assistant 本文のみ適用 (user 発話は生テキストのまま)。**実ブラウザ通し実測**: web 本番ビルド + 実 API (relay) + 実 Bridge + 偽 claude (markdown リッチ応答) で ブラウザ送信 → SSE → 見出し/表/コードカード/引用/リンクの実要素描画 + 生 markdown 記号の残存なし + コードコピー実クリック → clipboard 実値一致 を確認 (gap118-rich.png / gap118-copied.png)。vitest 新規 21 (MessageContent 9 + ChatPanel 未カバー挙動 12) — touched カバレッジ MessageContent 92.9% / ChatPanel 96.1% (Gate #4 閾値 80% 維持) | — | 2026-08-17 |

## プロセス gap (計画・ゲートの構造欠陥)

| ID | 内容 | 現状 | 解消に必要なもの | 起票日 |
|---|---|---|---|---|
| ~~GAP-101~~ ✅解消 (2026-07-19) | QA 仕様書の欠落 13 画面 | **全 13 枚作成完了** (S-B03/B04/N01/PUB01〜04/T01〜06)。completion_gate.sh が STATUS: 完了 (画面台帳一致・planned=0・FAIL=0・全 547 TC) を返すことを確認 | — | 2026-07-18 |
| ~~GAP-102~~ ✅解消 (2026-08-03) | CI Gate #6 (mock-impl diff) がスタブ | **実照合を実装** (`scripts/ci/mock-impl-diff.py` — v3-gate.yml から呼出): [A] screens.json ∪ モック data-bf-screen-id ∪ QA 仕様書の 3-way 台帳突合 (35 画面一致) [B] 全モック `<button>` ラベル 153 個を「実装ソース or QA 仕様書 or gap-tracker」と照合し未説明 0 を enforce。初回実走で記録漏れ 28 要素を検出 → 仕様書アンカー転記 + GAP-031 起票で説明責任を完備 | — | 2026-07-18 |
| ~~GAP-103~~ ✅解消 (2026-08-03) | tickets.json の UI タスク AC がテンプレコピペ | **機械転写を実装** (`scripts/qa/transcribe-ui-acs.py`、冪等・再実行可能): UI/frontend 39 タスクの tier_2 を screens.json の fields/actions/states/transitions (1 要素 = 1 AC) + features.json の happy_path/api_endpoints (同一画面サブタスクの差別化) + タスクスコープ UBIQUITOUS + UNWANTED ガードで再生成。39/39 で tier_2 が全て相異 (旧: 34 タスクが一字一句同一)。validate.sh PASS (216/216) 維持 | — | 2026-07-18 |
| ~~GAP-105~~ ✅解消 (2026-08-13) | 運用 docs の環境変数名が実装と不一致 (`CC_BOOT_WAIT` は実在しない) | **是正済** (commit 2ded58e / 635527b): 実変数は `scripts/ccstart.sh:125` の `CC_BOOT_TRIES` (既定は CC-BOOT-DEFAULT で 90→**180** に引き上げ済)。存在しない `CC_BOOT_WAIT` の案内を全 4 箇所で置換 — `docs/agents/README.md:37` (REHEARSAL-1) + `tools/flow-kit/templates/README.md:41` / `tools/flow-kit/templates/rehearsal.md:24` / `docs/agents/rehearsal.md:24` (本 PR)。docs の例示値は新既定 180 を超える **300** に統一 (既定と同値では「伸ばす」例として機能しないため)。`git grep CC_BOOT_WAIT` の残存は本台帳の説明文のみ。配布テンプレート側も是正したため `install.sh` 経由の誤記伝播は根本から停止 | — | 2026-08-13 |
| ~~GAP-106~~ ✅解消 (2026-08-13) | 手動復旧コマンドが API キー環境変数を 1/3 しか外さない (従量課金に流れうる) | **是正済** (commit e943814): `docs/agents/README.md:62` / `tools/flow-kit/templates/README.md:66` の復旧手順を `scripts/ccstart.sh:113` と同じ 3 変数 (`ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` / `CLAUDE_CODE_API_KEY`) を `-u` で外す形に統一 (変数名・順序とも文字列一致を qa/pm が機械照合)。**セッション落ちからの手動復旧時だけ API 従量課金に切り替わる経路を閉塞**。commit 16597b8 の実装意図と docs が再整合。REHEARSAL-1 の qa 独立検証で検出された | — | 2026-08-13 |
| ~~CC-BOOT-DEFAULT~~ ✅解消 (2026-08-13) | `/rename` 空振り対策として起動待ち既定が短すぎる | **是正済** (commit 1a93a63・ユーザー指示): `CC_BOOT_TRIES` 既定を 90 → 180 に引き上げ、`scripts/ccstart.sh` と `tools/flow-kit/scripts/ccstart.sh` の 2 コピーをバイト一致で同期 (`install.sh:26` が kit → 導入先へ上書きコピーするため片側修正は不可)。129-132 行のプロセス検出待ち上限 30 は意図的に据え置き。**運用上の副作用**: `setup_target` は 211-213 / 243-245 行で 3 ペインを直列に呼ぶため、起動失敗で監視ループが尽きるケースの最大待ちが 1 ペイン 90s→180s、3 ペイン全滅時 ~4.5 分→~9 分に伸びる (正常時は入力欄検出で即 return するため影響なし) | — | 2026-08-13 |
| ~~GAP-104~~ ✅解消 (2026-08-01) | 通し (e2e-journey-walkthrough) が一度も実行されていない | **実施完了**: 19 行ジャーニー (登録→WS→プロジェクト→チャット→ナレッジ→見積→タスク→Bridge 実実行 (実 claude -p)→承認/差戻→メンバー招待/コメント→クライアント招待→ポータル→R-T08→通知/検索→最終成果+GAP-016 確認) を実 UI で **連続 3 回 19/19 全 PASS** (workbook: `.qa/e2e-journey/journey-20260801.xlsx`, 証跡: `evidence-20260801/`)。通しで製品バグ 2 件を検出・修正 (play→pick パイプ断絶 / approval_inbox プロデューサ不在, commit 57f14f5) + GAP-030 起票 — 「通しを後回しにするほどこの型の欠陥は長生きする」の実証。**v2 (2026-08-11)**: 8/10-11 実装の 7 機能群 (商談ドラフト トニー生成→PDF→送信 / ジャービス フェーズ提案→承認 / 影響解析→移動 / モック登録→ワンダ改訂 / 見積の顧客要望反映再生成 / ポータル実コンテンツ閲覧+客コメント→運営返信の往復 / 運営ダッシュボード+テンプレ配布) を含む 27 行に拡張し**連続 3 回 27/27 全 PASS** (workbook: journey-20260811.xlsx, evidence-20260811/)。通しが再び実欠陥を検出・是正: ① S-G01 が本文なし成果物 (営業ドラフト) を画面ごと拒否し客コメントへの返信画面が存在しない断絶 → noPreview モードで是正 (vitest 回帰) ② signup が Supabase Admin API 必須構成で新規登録不能になる環境断絶 (dev スタブに admin users を実装)。画面到達 21/35 (未踏 14 は human-grade-qa 全画面監査で担保) | — | 2026-07-19 |
| ~~GAP-125~~ ✅解消 (2026-08-17) | S-E01 スレッドの削除・タイトル編集・デフォルトタイトル (経営者指示) | **フロントのみで実装 (PATCH/DELETE /chat/threads/{id} は既存 API)**: ① 一覧は直近やりとりのプレビューではなく**タイトルを表示** (title 未設定は「無題スレッド」— プレビューのフォールバック廃止) ② スレッドカード hover/フォーカスで鉛筆/ごみ箱アクション — 鉛筆で**インライン編集** (input プリフィル + 保存/取消、Enter/Esc、PATCH {title} → 一覧と中央ペイン両方 invalidate) ③ ごみ箱は**はい/いいえの 2 段階確認** (「やりとりも削除され元に戻せません」明示) → DELETE → 選択中スレッドなら中央を未選択に戻す (onDeleted) ④ 作成時に**デフォルトタイトル自動付与** — `defaultThreadTitle()` =「新しい会話 8/17 19:30」形式 (Intl ja-JP)。サイドバーの新規フォームと空白ペインの社員カード両経路に配線。vitest 5 追加 (タイトル表示/プレビュー非表示/PATCH/はい・いいえ/デフォルトタイトル) + 全 628 PASS + 実ブラウザ (本番ビルド + 実 API + 新規ユーザー通し) 8/8 PASS + DB 突合 (title 実値 + deleted_at) — 証跡 .qa/gap-125/ | — | 2026-08-17 |
| ~~GAP-126~~ ✅解消 (2026-08-17) | ①編集/削除ボタン縮小 ②公式横ロゴ採用 ③Bridge ログイン時自動起動 (経営者指示) | ① S-E01 スレッドカードの鉛筆/ごみ箱を 24px→**20px 枠 / 10px アイコン**に縮小 ② ロゴの「マーク + アプリ側で別タイプした Atelier 文字」をやめ、**経営者支給の公式横ロックアップ** (logo-horizontal.svg) に統一。正本は 1920x1080 余白込みのため getBBox 実測で viewBox 化した派生 `logo-horizontal-lockup.svg` を生成 (正本は無改変で保持)。適用: サイドバー (折りたたみ時のみマーク単体)・サインイン S-A01・oauth-complete・公開ページヘッダ・クライアント S-L02/S-L03。運営 /admin はダーク地に公式ダークオリーブが沈むため対象外 (白抜き版ロゴ受領時に対応) ③ Bridge に**ログイン時自動起動**: mac/win = setLoginItemSettings、Linux = XDG autostart (~/.config/autostart、AppImage は APPIMAGE 実体を登録)。登録は「接続済み (トークン保有)」時のみ・dev 未パッケージでは登録しない・失敗しても接続は止めない。ワンクリック接続成立時にも同時登録。vitest 5 追加 (bridge auto-launch 100% cov、bridge 全体 81.6%≥80) + web 628 PASS + 実ブラウザ 5/5 PASS (ロックアップ実寸 105x32 / ボタン 20x20 実測) — 証跡 .qa/gap-126/ | — | 2026-08-17 |
| ~~GAP-127~~ ✅解消 (2026-08-17) | ①サイドバーロゴ縮小 ②Claude 未ログイン/未インストールの検知とログイン誘導 (経営者指示) | ① サイドバーの公式ロックアップを h-8→**h-6 (24px)** に縮小 (「デカすぎる」) ② Bridge chat-relay が claude CLI 失敗を**原因分類してタグ付け**: spawn 失敗 (ENOENT) → `[claude-not-found]` / result 本文・stderr 末尾 (新規捕捉) の認証パターン (Please run /login, Invalid API key, authentication_error 等) → `[claude-not-logged-in]`。分類不能時はタグ無しで exit code + 根拠断片 (推測で決めつけない) ③ 接続パネルが last_job.error のタグ (旧 Bridge はパターンフォールバック) から**具体的な復旧手順を案内**: 未ログイン→「ターミナルで claude を実行しログイン → もう一度送るだけで動く (再接続不要)」/ 未インストール→インストールページへの実リンク。生エラー表示からはタグを除去。bridge vitest 5 追加 (分類 4 + result detail) 68 PASS / web vitest 2 追加 630 PASS / 実ブラウザ 5/5 PASS (実 DB に error job を作成して実 API 経由で誘導表示を確認・ロゴ 79x24 実測) — 証跡 .qa/gap-127/ | — | 2026-08-17 |
| ~~GAP-128~~ ✅解消 (2026-08-17) | ①プラン枠が「未観測」になる実バグ ②チャット生成中の UI/UX ③F-CTX01 チップ密着 (経営者指摘) | ① **実測で原因特定**: 実 claude CLI の rate_limit_event は camelCase (`rateLimitType`/`resetsAt`) で、`utilization` はそもそも含まれない場合がある。(a) Bridge パーサが snake_case のみ読んで全 null 化 → camelCase 両対応 (b) UI が utilization 無しの実値 (枠種別+状態+リセット時刻) を捨てていた → 「5 時間枠: 利用可 · 8/17 15:20 にリセット」形式で表示 + 「% は Claude が報告した場合のみ」の誠実注記 ② 生成中インジケータ: SSE 実イベント連動の 3 段階 — 文脈収集中 (context chunk 前、「会話履歴とナレッジを検索中…」) → 思考中 (「◯◯が考えています…」+ ドット) → ストリーミング (本文 + 点滅カーソル)。演出でなく実段階のみ表示。外部チャット UI ライブラリはデザイントークン規約と衝突するため不採用 (トークンベース自前実装) ③ F-CTX01 チップをメッセージ列の px (32px) に整列。**検証はコンテナの実 Claude サブスク認証で agent_sdk 実チャット e2e**: 実応答「2」受信・インジケータ実表示・チップ余白 32px・パネルに実リセット時刻表示 = 4/4 PASS + DB 突合 (chat_plan_status.five_hour_resets_at 実値)。bridge vitest 69 / web vitest 634 PASS — 証跡 .qa/gap-128/ | — | 2026-08-17 |
| ~~GAP-129~~ ✅解消 (2026-08-17) | ①ロゴのマークと文字の間隔 ②チャットから PC 操作 (Claude Code 同等 + auto mode) (経営者指示) | ① 公式横ロゴは 1 枚画像で間隔が固定 (実測: 高さの約 11% しかない) → **公式素材を無改変のまま viewBox でマーク部/文字部に分割** (logo-lockup-mark/word.svg — ラスタ走査で分割位置 x=488..548 を実測、同一縦レンジでベースライン保持) し、間隔を CSS gap で制御する BrandLockup コンポーネントに統一 (サイドバー gap-2=8px、サインイン等 gap-3=12px)。全 6 箇所適用 ② **チャット PC 操作 (GAP-129 本体)**: ChatStreamRequest に tools_mode (off/auto、openapi + TS/Pydantic 再生成)。auto = agent_sdk 限定・本人 opt-in で Claude Code ツール (Read/Write/Edit/Bash/Glob/Grep) を permission_mode=bypassPermissions・cwd=作業フォルダ (~/AtelierChatWork、ATELIER_CHAT_WORKSPACE で変更可)・max_turns=25 で実行。ツール実行は SSE tool チャンクで実況 (コンポーザ上に「ツール実行中: Bash → Write」)。コンポーザに「PC 操作: なし/自動」トグル (agent_sdk 以外はトグル自体非表示 + サーバー側でも 403 相当の誠実エラー)。**実 e2e**: 実チャットで「test-gap129.txt を作って」→ ツール実況表示 → 実応答が絶対パス報告 → **disk 突合で実ファイル + 内容一致を確認**。承認を挟む対話モードは GAP-130 として次対応。pytest 3 追加 (437 PASS) / vitest 3 追加 (637 PASS) / ruff・Biome・tsc クリーン — 証跡 .qa/gap-129/ | — | 2026-08-17 |
| ~~GAP-130~~ ✅解消 (2026-08-18) | PC 操作の承認モード (Claude Code の permission prompt 同等 / 経営者指示「承認ボタンを挟むのはいい」) | tools_mode に **approve** を追加 (off/approve/auto の 3 段トグル、クリックで循環)。approve は allowed_tools に載せず can_use_tool コールバック (SDK 制御プロトコル) で実行ごとに SSE `pc_approval` カード (ツール名 + コマンド/パスの実値要約) を配信 → POST /chat/pc-approvals/{id} (openapi + 型再生成) の allow/deny で解決。プロセス内レジストリ (uuid4 + user_id 突合、他人の ID では解決不可)、無応答は 300 秒 (env 調整可) で**拒否側に倒す**。許可対象外ツール (WebSearch 等) は即拒否。**SDK バグ回避**: claude-agent-sdk 0.2.139 は can_use_tool があっても入力ストリーム終了で stdin を閉じ、CLI の許可要求が全て "Stream closed" で死ぬ (sdk_mcp_servers/hooks しか考慮しない) → プロンプト後に input_done Event で stdin を維持し ResultMessage 受信で解除する回避を実装 (生 CLI プロトコル解析で特定)。**実 e2e 9/9 PASS**: トグル循環 → 承認カード表示 (カード時点でファイル未作成を disk 突合) → 許可 → 実ファイル生成+絶対パス報告 / 拒否 → ファイル生成されず AI が「拒否されましたので実行しませんでした」と応答。pytest 446 PASS (9 追加) / vitest 641 PASS (7 追加) / ruff・Biome・tsc クリーン — 証跡 .qa/gap-130/ | — | 2026-08-18 |
| ~~GAP-131~~ ✅解消 (2026-08-18) | Vault (project_credentials) の 4 弱点 — ①列レベル revoke 不在 ②鍵ローテ手段なし ③reveal 再認証なし ④鍵運用 docs/ADR が main に無い (経営者合意「完璧に = セキュリティ堅牢 + 鍵をしっかり使える」) | ① **列レベル revoke** (gap-131 migration — BYOK t-d-95 と同型): encrypted_value を authenticated から SELECT 不可、非機密列のみ列 grant。reveal は「RLS で可視性チェック → service セッションで ciphertext 1 行取得」の 2 段構成に変更 ② **MultiFernet 鍵ローテーション**: ATELIER_VAULT_ENCRYPTION_KEYS (先頭=暗号化・全鍵=復号、旧単鍵 env 後方互換) + 再暗号化スクリプト apps/api/scripts/rotate_vault_key.py (dry-run / 復号不能行は skip 報告で黙って壊さない) ③ **reveal 再認証**: ログインパスワード再入力 (signin と同一検証経路) + 成功後 TTL 300 秒 (env 調整可) は省略可 + 10/min/user レート制限 + 失敗を credential.reveal_denied として**独立 txn で**監査 (403 rollback で消えるバグを e2e で検出し修正)。openapi 更新 + 型再生成、S-B04 に行内パスワード入力 UI ④ docs/vault-key-operations.md (ローテ手順・喪失時手順・dev/prod 鍵分離・監査 SQL) + ADR-020 (Fernet 継続の裁定 + KMS/エンベロープ暗号化をホスト版必須要件として明文化) + SECRETS.md 追記。**実 e2e**: API 12/12 PASS (403 reauth_required → 誤 PW 403 → 正 PW 200 → TTL 内 200 / psql で authenticated の encrypted_value SELECT が permission denied / 監査 2 種) + **実鍵ローテーション** (旧鍵で暗号化 → rotate 2 行 → 新鍵のみで復号成功) + UI 6/6 PASS (再認証プロンプト/誤 PW エラー/TTL)。pytest 454 PASS (8 追加) / vitest 644 PASS (3 追加) — 証跡 .qa/gap-131/ | — | 2026-08-18 |
| ~~GAP-132~~ ✅解消 (2026-08-18) | 「これまでの経緯」が文字数切り捨て (content[:120] + char_budget 先頭落とし) で、スレッド序盤の決定事項が真っ先に消える (経営者すり合わせ: ローリング要約 + 保存列 + 非同期 + フォールバック) | chat_threads に **context_summary / context_summary_upto** 列 (gap-132 migration)。**ローリング要約**: 直近ウィンドウから溢れた未反映分があるときだけ、既存要約 + 溢れ分を LLM で新要約に統合 (毎ターン再走査をやめ upto 以降のみ)。**応答完了後に fire-and-forget 非同期更新** (体感遅延ゼロ、relay の Bridge 往復も待たせない)。**失敗はフォールバック**: 旧要約 + 切り捨て版が生き続け、upto が進まないので次回自動再試行。LLM は stream_chat と同じ provider 分岐 (relay / agent_sdk / API / fake) = サブスクなら課金ゼロ。実装中に実 e2e で 2 つの品質バグを検出して修正: ① 子 CLI がホストの CLAUDE.md / ~/.claude を読み込み要約に混入 → **setting_sources=[] で遮断** (チャット応答全モード共通の改善) ② ログ中の「『了解』とだけ返して」に要約器が追従する prompt injection → <log> フェンス + 明示指示 + 短すぎる出力を保存しない退行ガード。**実 e2e 6/6 PASS** (実 Claude サブスク): 3 往復で非同期要約が保存 → 序盤の決定「合言葉ゆず」が要約に生存 → context-preview の実 system prompt に注入 → **include_history=2 (発言は窓外) で「合言葉は？」に「ゆず」と正答 = 記憶の実証**。pytest 463 PASS (9 追加) — 証跡 .qa/gap-132/ | — | 2026-08-18 |
| ~~GAP-133~~ ✅解消 (2026-08-18) | ナレッジ RAG が VOYAGE_API_KEY 未設定だと ilike 部分一致 (score=0.5 固定) で「意味が近い」が一切効かない — Anthropic に埋め込み API が無くサブスクで代替不能 (経営者決定: コストゼロ + 各自ローカルで負担なく → ローカル埋め込み) | **埋め込みプロバイダ 3 段抽象化**: ① Voyage (キー設定時・最高精度) ② **ローカル (fastembed/ONNX・課金ゼロ)** — 既定 intfloat/multilingual-e5-large (1024 次元 = 既存 vector(1024) スキーマに無変更適合・日本語実用・初回のみ約 2.2GB 自動 DL)。軽量機向けに ATELIER_LOCAL_EMBEDDING_MODEL で切替可 (1024 未満はゼロパディング = cosine 順位不変、超過は誠実拒否) ③ どちらも無ければ ilike + **search_mode="text_fallback" を UI に明示** (「意味検索は無効です」)。**モデル空間の分離**: knowledge_nodes.embedding_model 列 (gap-133 migration + 既存行 voyage backfill)、検索は同一タグの行のみ (異空間 cosine を黙って混ぜない)。切替移行は scripts/reembed_knowledge.py (dry-run 付き)。**潜在バグ 2 件を e2e で検出し修正**: ① `<=>` 演算子が search_path 外で Voyage 経路の意味検索もこの DB 構成では常に 500 だった → operator(extensions.<=>) 明示構文 ② HF ダウンロードが企業プロキシ/xet で失敗 → ATELIER_CA_BUNDLE 設定口 + HF_HUB_DISABLE_XET。依存は uv extras (localrag / subscription) として宣言し dev-update.sh が `uv sync --all-extras` で構造的に導入。**実 e2e**: API 6/6 PASS — 「価格提示の書類の作り方」(共通語ゼロ) で「お見積書テンプレート」がトップヒット (旧 ilike では 0 件を同時実証)・embedding_model タグ保存・reembed 復元。UI 3/3 PASS — S-K01 に「(意味検索: ローカル)」バッジ + スコア 0.83/0.79 の実ランキング。pytest 470 PASS (7 追加) / vitest 644 PASS — 証跡 .qa/gap-133/ | — | 2026-08-18 |
| ~~GAP-131/133 追補~~ ✅ (2026-08-18) | 経営者確認への対応: ①「鍵ローテを勝手にやるな (Supabase キー等が変わるのでは)」②「初回モデル DL で UX 低下しないか・自動化されているか」 | ① **誤解の解消をコードと文書で固定**: ローテーションは保管庫の暗号鍵の交換のみで**保管された値 (Supabase キー等) は 1 文字も変わらない** (e2e で前後同一値を突合済)。**自動実行経路はゼロ** — さらに rotate_vault_key.py に確認入力ゲート (rotate と打つ / 非対話は --yes 必須) を追加し誤実行を構造的に防止。docs/vault-key-operations.md 冒頭に明記 ② **起動時ウォームアップ + 自動バックフィル**: API 起動時にモデル DL/ロードをバックグラウンド実行 (main.py lifespan)。**DL 中もユーザー操作を一切ブロックしない** (is_ready ガードで ilike に誠実 degrade) — 完了後、その間に登録されたナレッジも**人手ゼロで自動埋め込み** (実測: 再起動 4 秒後に欠損 3 行を自動復元)。reembed スクリプトは service 層 backfill_missing_embeddings に一本化。pytest 471 PASS (1 追加) | — | 2026-08-18 |
| ~~GAP-134~~ ✅解消 (2026-08-18) | **アーキテクチャすり合わせ (経営者): 標準構成 =「全ユーザーが自分の PC + 自分の Claude サブスクで実行 (PC 操作込み)」**。従来 PC 操作は agent_sdk (サーバー内実行) 限定で、Bridge (relay) 経路はテキストのみだった | **Bridge 経路に PC 操作をフル実装**: ① chat_relay_jobs.tools_mode + chat_relay_chunks.kind (delta/tool) + **chat_relay_approvals** テーブル (gap-134 migration — SSE と Bridge はプロセスが別なので承認往復は DB 経由。RLS: 本人のみ SELECT・書込は service 経路のみ) ② SSE アダプタ (relay.py) が agent_sdk と**同一形のイベント** (delta / tool 実況 / pc_approval / resolved) を多重化 — フロントは経路を意識しない。決定 API (/chat/pc-approvals/{id}) はメモリレジストリ→DB の 2 系統を同じ入口で解決 ③ **Bridge (Electron/TS)**: claude CLI を tools_mode 別引数で起動 (auto=bypassPermissions / approve=--permission-prompt-tool stdio + stream-json 入力で **stdin を result まで維持** — GAP-130 で解明した実プロトコル)。control_request can_use_tool → サーバー承認キュー → ユーザー決定をポーリング → control_response 返送。許可対象外ツールは即拒否・無応答は拒否に倒す。作業フォルダは本人 PC の ~/AtelierChatWork ④ トグル表示を relay にも拡張、Bridge 用 API 3 本 (approvals POST/GET + chunks kinds) を openapi + 型再生成。**実 e2e 6/6 PASS (API=relay + 実 Bridge 常駐 + 実 Claude)**: relay でトグル表示 → 承認カード (実コマンド表示) → 許可 → **ユーザー PC 側 (/root/UserPCWork) に実ファイル生成** → 拒否 → 生成されず。ヘッダーは「自分のプランで実行中」。pytest 474 PASS (3 追加) / bridge vitest 33 PASS (13 追加) / web 変更は gating のみ | — | 2026-08-18 |
| ~~GAP-135~~ ✅解消 (2026-08-18) | **「どのデバイスでも完璧に」(経営者指示)**: ① Windows は WSL2 前提と案内していた ② 素の `spawn('claude')` は Windows ネイティブ (npm 版は claude.cmd シムで起動不可) と macOS の Dock 起動 (login shell の PATH 非継承で claude 不発見) で壊れる実バグ ③ Bridge の初回オンボーディングが placeholder UI のみ ④ 更新の通知手段なし | **全 OS 対応 + オンボ + 更新チェックを実装**: ① `src/command.ts` — spawn 前に claude 実体を解決 (PATH + 既知ディレクトリ ~/.local/bin・/opt/homebrew/bin・/usr/local/bin・~/.claude/local・$NVM_BIN・%APPDATA%\npm)。Windows は claude.exe 最優先、npm の .cmd シムは **shell を経由せず** cli.js を ELECTRON_RUN_AS_NODE=1 で直接実行 (shell 経由は system prompt 再解釈のインジェクション経路のため不採用)。未解決は従来 ENOENT → GAP-127 分類 ② `src/doctor.ts` — CLI 検出/version/`claude auth status` (JSON)/接続設定の 3 チェック診断 ③ renderer 全面刷新 — 初回オンボーディング画面 (3 ステップを診断して足りない手順だけ OS 別コマンド表示、全 ok で「常駐中」バナー) + preload/IPC (contextIsolation+sandbox 維持、外部リンクは https のみ) ④ `src/updates.ts` + API `GET /public/bridge-latest` (env 駆動・DB 不使用) — 新版バナー + 自 OS インストーラのワンクリック DL (サイレント自動更新は署名インフラ前提のため通知+DL まで — docs に明記) ⑤ docs/bridge-setup.md (全 OS・初回 1 回だけ・再インストール不要表) + agents README の WSL 注記を「開発フロー限定」に訂正。**検証**: bridge vitest 108 PASS (+22: win32 シム解決/巡回/doctor/updates/prependArgs 実 spawn) + 実環境証跡 .qa/gap-135/ (実 CLI 解決 2 系統 + 実 spawn --version + runDoctor 実走 loggedIn=true + live API フィード + オンボ UI 実描画 3 状態スクショ) + pytest (bridge-latest 2 追加) | — | 2026-08-18 |
| ~~GAP-136~~ ✅解消 (2026-08-18) | PC 操作の実行中 UX: ① 長時間のツール実行中「ツール実行中: X」1 行のみで経過も進行も見えず**止まって見える** (経営者スクショ指摘) ② 完了すると実況が消え痕跡ゼロ ③ 実行中にモードトグル (なし/承認/自動) を切り替えても走行中 CLI には反映されない (起動時引数で固定) のに UI が変更を受け付けて誤解を生む | ① 実況をタイムライン化: 実行中ツール + パルス + **経過 N 秒** (実イベント由来の開始時刻から算出・進捗% の推測はしない) + 完了済みツールは ✓ 行で残す + 「応答はツール完了後に届きます」の明示 ② 完了後は「PC 操作完了: N ツール実行 (M 秒)」を次送信まで表示 ③ 実行中はトグル disabled + title「実行中は変更できません (次の送信から変更できます)」— 途中切替の意味論は Claude Code と同じ「次の実行から適用」に統一。web vitest bundle-h 133 PASS (3 追加/1 改) + tsc | — | 2026-08-18 |
| ~~GAP-137~~ ✅解消 (2026-08-18) | **チャットの PC 操作で作った成果物 (LP モック等) がユーザー PC のフォルダに置かれるだけで、ツール内 (モック) に反映されない** (経営者スクショ指摘「モックとしてちゃんと反映されていないね」)。加えてローカル開発は Supabase Storage 未設定でモック閲覧自体が 503 だった | **成果物の自動反映パイプラインを実装**: ① Bridge がツールジョブの前後で作業フォルダをスナップショットし、新規/更新 HTML (10 件・512KB 上限) を complete 前に POST /chat-relay/{job}/artifacts (BridgeAuth、user トークンは本人ジョブのみ) ② サーバーは thread→project のモックとして取り込み — 同名画面は **新バージョン連鎖** (parent_mock_id、S-H01 の履歴にそのまま乗る)。HTML は新テーブル mock_contents (RLS default deny) に保存し html_storage_path=mockdb://{id} — **Storage 未設定環境でも閲覧可能** (gap-137 migration) ③ 閲覧は content-url が mockdb を検知して自己署名 (HMAC + 期限) URL を返し GET /mocks/{id}/content が配信 (iframe は Authorization を送れないため) ④ kind='artifact' chunk → SSE type=artifact → チャットに「モックとして保存しました → モックで開く」カード (S-H01 リンク、応答完了後も次送信まで残る) ⑤ agent_sdk (サーバー内実行) 経路も対称実装 (pc_artifacts.py)。**実 e2e (実 claude + 実 Bridge + 実 API + 実 DB)**: 「price-page.html を作って」→ ユーザー PC 相当に実ファイル生成 → artifact イベント → mocks v1 (mockdb) → content-url → 実 HTML 配信 (title 一致)。2 回目の修正依頼 → 実 Edit → **同一画面 v2 連鎖** (parent 付き) を versions API で確認。pytest 30 PASS (+16) / bridge vitest 112 (+4) / web vitest +1 / ruff / pyright 0 | — | 2026-08-18 |
| ~~GAP-138~~ ✅解消 (2026-08-18) | ① モックタブに**作成フローが無い** (経営者指摘 — 一覧と単票ビューアのみ) ② 複数画面を俯瞰する **figma 的キャンバスが無い** ③ mock 改訂 (Open Design) の LLM が **ANTHROPIC_API_KEY 直依存** — 「全ユーザーが自分のサブスクで」の確定アーキに不整合 ④ (e2e で発見した実バグ) off モードの relay ジョブが Bridge 起動 cwd で claude を実行し、リポジトリ内だと .claude (hooks/CLAUDE.md) を拾って不安定に失敗 | ① **新規モック**: S-H01 に作成フォーム (画面名 + 指示) → POST /mocks/generate — ワンダが HTML 全文を生成し mockdb 保存・同名画面は版連鎖。「あなたの Claude プラン (Bridge) で生成されます」を明示 ② **キャンバスビュー**: 画面ごとの最新版を実 HTML (署名付き content-url) の縮小 iframe で並べ、ズームスライダ + スクロールで俯瞰。各カードから「開く」「編集 (= ワンダへの修正依頼)」に直行 ③ **llm_chain.py に単発 LLM を一元化**: relay (本人サブスク) → agent_sdk → API → fake の費用順。Bridge オフラインは黙って API 課金に落とさず 503 で誠実表示。mock generate/revise/チャット要約が同一チェーンに。revise は mockdb モックなら mockdb に新版 (Storage 未設定でも編集可能に)。chat_relay_jobs.thread_id を nullable 化 (gap-138 migration — チャット外のシステムジョブ) ④ 全モードで作業フォルダを cwd に固定 (チャット応答の環境汚染を排除)。**実 e2e**: 生成 → 実 claude (meta model="relay" = 本人サブスク課金) が 25KB のダッシュボード実 HTML → 配信確認 → 修正依頼「紺ヘッダー + タイトル変更」が実 HTML に反映され v2 連鎖 (スクショ .qa/gap-138)。pytest 78 PASS (+8) / bridge vitest 112 / web vitest 138 (+4) / ruff / pyright 0 | — | 2026-08-18 |
| ~~GAP-139~~ ✅解消 (2026-08-18) | **「HTML だから全部モック行き」は誤り (経営者指摘)** — 見積書・提案書・テスト仕様書等も HTML で作られるが、GAP-137 はチャット成果物を無条件に mocks へ入れていた | **種類判定 + 振り分けを実装**: ① classify_artifact — 文書自身の <title> → ファイル名 (quote/proposal 等の英語名も) → **直近のユーザー指示のみ** (fold_prompt から抽出 — 履歴の「見積」で誤爆しない) の優先順の決定的規則。LLM は使わない (取り込みは Bridge complete 直前の同期処理で、relay 再入はデッドロックするため) ② mock → mocks / estimate・proposal・invoice・contract・nda・verification (テスト仕様書)・requirements・hearing (議事録) → **workflow_outputs (S-G01 成果物、stage 別バージョン連鎖・mockdb 保存)** ③ outputs 側にも mockdb 配信 (content-url 分岐 + GET /outputs/{id}/content 自己署名) — Storage 未設定でも成果物閲覧可 ④ チャットカードは種類ラベル (見積書/提案書/…/モック) + 適切なリンク先 (成果物で開く/モックで開く)。旧形式 (type 無し) は mock として後方互換 ⑤ relay + agent_sdk 両経路。**実 e2e**: 実 claude に「お見積書 HTML」→ type=output/stage=estimate で workflow_outputs へ (mocks に無いことも確認) → outputs content-url で実 HTML 配信。pytest 57 PASS (+5) / web vitest 23 (+2) / ruff / pyright 0 | — | 2026-08-18 |
| ~~GAP-140~~ ✅解消 (2026-08-18) | **タスク分解と画面モックが独立で、分解時に「どの画面のタスクか」を宣言できない** (経営者指示: 分解時にまっさらのデザイン (タイトルだけ) をモックに置いておき、そこに作り込む形にしたい)。チャット成果物もタスクに自動では繋がらなかった | **タスク⇄画面モックの双方向ループを実装**: ① TaskCreate/TaskUpdate に screen_name — 指定すると同 project+画面名のモックチェーン最新に紐づけ、無ければ**プレースホルダー v1 (タイトルだけの枠 + 「ここに作り込んでください」) を mockdb で自動作成**して tasks.mock_id に設定。同じ画面の 2 個目のタスクは同じチェーンを共有 (重複作成しない) ② S-I01 作成フォームに「対象画面 (任意)」入力、S-I02 ヘッダに「画面: ログイン画面 →」チップ (モック直行)、TaskResponse に mock_id/mock_screen_name ③ ループの閉じ方 (既存機能との合成): チャットの PC 操作成果物は同じ画面名で自動的に v2, v3… と連鎖 (GAP-137) → 紐づいたタスクに**仕様変更カード** (GAP-025: adopt=最新採用 / split=フォロータスク自動起票 / discard=再分解待ち) が出る — 「モック側の変更がタスクを更新する」逆方向も成立。統合テストで分解→プレースホルダー→作り込み v2→仕様変更カードの全ループを実証。pytest 36 PASS (+3) / web vitest bundle-i 70 (+2) / ruff / pyright 0 / tsc | — | 2026-08-18 |
| ~~GAP-141~~ ✅解消 (2026-08-18) | **ローカル作業フォルダと Supabase 正本の二重化** (経営者指摘): ローカルに古いファイルが残ると AI がそれを土台に編集し、版連鎖が乱れる。ローカルで立ち上げてテストもしたい | **「ローカル = 正本のチェックアウト」方式**: ① GET /chat-relay/{job}/workspace (BridgeAuth・本人ジョブのみ) がプロジェクト最新版 (モック各画面 + mockdb 成果物各 stage、元ファイル名保持・パス安全化・上限 20 件) を返す ② Bridge はツールジョブの CLI 起動前に作業フォルダへ上書き展開 → **展開後に**スナップショット (未編集 seed は再取り込みされない) ③ seed 失敗はジョブを止めない。**実 e2e**: 空フォルダ → seed 3 ファイル実体化 → 実 claude が price-page.html を Read/Edit → **料金ページ v3 として正しく連鎖** (チェーン [1,2,3]・追記+元内容とも新版に反映)。ローカルテストは auto モードで「サーバー立てて確認して」等を依頼すれば本人 PC 上でそのまま実行できる (正本と同期済みの作業場で)。pytest +1 / bridge vitest 114 (+2) / ruff / pyright 0 | — | 2026-08-18 |
| ~~GAP-142~~ ✅解消 (2026-08-18) | **モック画面が Open Design の UI/UX を再現できていない** (経営者指摘 — 実物 https://github.com/nexu-io/open-design を確認: 中核は Studio = 会話 + ライブプレビュー統合 + sandboxed iframe)。従来はトグル式の「編集」ダイアログのみで、会話も要素選択も無かった | **モックスタジオを実装**: ① MockViewer を 3 ペイン化 — 左「ワンダとデザイン」会話パネル (バージョン履歴を「指示 → ワンダが vN を作成しました」の会話として表示 + 常設の指示入力。「あなたの Claude プランで実行」明示)、中央ライブプレビュー (デバイス切替維持)、右 履歴/コメント ② **要素クリック選択**: mockdb 配信 ?sel=1 で選択スクリプト注入 — hover 紫点線ハイライト、クリックで postMessage (selector/label/HTML 断片) → 親でチップ表示 → 送信時に指示へ自動添付 (composeInstruction 純関数) ③ 旧「編集」ボタン/ダイアログ廃止。**実ブラウザ e2e**: 実 claude 生成モックに hover ハイライト + クリック → 実 postMessage (CSS セレクタ/ラベル/HTML 実値) を確認 (.qa/gap-142)。web vitest 161 PASS (+3/2 改 + a11y listitem 修正) / pytest +3 / ruff / pyright 0。※ Open Design 本体はローカル Electron アプリ・Atelier は SaaS 内蔵という違いは残る (DESIGN.md 型のデザインシステム蓄積は今後の拡張候補) | — | 2026-08-18 |
| ~~GAP-143~~ ✅解消 (2026-08-18) | ① **ワンダのモック生成/改訂に「格納済みスキル」「デザインの好み」が一切注入されていなかった** (経営者指摘:「DESIGN.md は design-md というスキルとかナレッジとして好み・傾向を把握する感じにしているはず」「誰にどのスキルをどの時に読ませるかを自動的にさせる必要もある」) — スキル注入はチャットのみで、GAP-142 の残課題だった DESIGN.md 相当も未実装 ② (e2e で発見した実バグ 3 件) 学習ジョブの actor_id="system" が relay の requested_by (uuid) cast で silent fail しジョブ自体が走らない / off モード relay で claude が自発的ツール呼出し (--max-turns 1 と衝突) して間欠 exit=1 / Bridge 子プロセスに CLAUDE_* 環境が漏れる | **デザインノート + スキル自動注入 + 好み自動蓄積**: ① projects.settings.design_note (jsonb・migration 不要・2000 字) + GET/PUT /projects/{id}/design-note (RLS + audit) + S-H01 エディタ。**build_design_context がノート (「必ず従うこと」ブロック) + design 部門 AI 社員 (= ワンダ) のペルソナ + 装着スキル SKILL.md をチャットと同一機構 (_load_persona_and_skills) で全生成・改訂の system prompt に前置** — 「誰に (design 部門社員)・いつ (全生成/改訂)・どのスキルを」が自動化 ② **好みの自動蓄積**: 生成/改訂指示から恒久的決定 (色/フォント/トーン等) だけを LLM がノートに統合 (fire-and-forget・失敗/変化なしは保存しない・一時的指示は追記しない)。実行は指示した本人の Bridge = 本人サブスク ③ バグ修正: 学習 actor を本人に (requested_by 整合) / off モード CLI 引数に --tools '' / sanitizedChildEnv が CLAUDECODE + CLAUDE_* prefix を drop。**実 e2e (実 Bridge + 実 claude)**: ノート (紺 #1e3a5f) → 実生成が紺を採用・改訂も維持 → 「見出しは Noto Serif JP」改訂 (v4, 16s, relay) → **学習ジョブが完走しノートに「- 見出しフォントは Noto Serif JP を使用」が自動追記** (既存 3 行は保持・relay-jobs 証跡付き)。pytest 61 PASS / bridge vitest 115 / web uc13 22 / ruff / pyright 0 — 証跡 .qa/gap-143/ | — | 2026-08-18 |
| ~~GAP-144~~ ✅解消 (2026-08-18) | 経営者確認「スキルは管理側で設定・ユーザーは作れない・詳細は見えない、だよね？」→ 管理専用 CRUD (is_admin 403 + service 書込 + audit) と API 応答の本文非公開 (カタログは Lite のみ) は元からできていたが、**本番 JWT (Supabase Auth 発行 = PostgREST でも有効) で DB を直叩きすると skills.content_md (運営ノウハウ) と allowed_* を全文読めた** — RLS skills_select_all は行レベル全開放で列を隠せない | **列レベル revoke (GAP-131 vault と同手法 / gap-144 migration)**: authenticated/anon から表全体 grant を剥奪し、authenticated にはカタログ用軽量列 (id/name/version/description/is_active/created_at/updated_at) のみ列単位 grant。API 内部の本文読取は service 経路へ一本化 — 注入は services/skills.fetch_skills_md (新設・チャット/モック生成共用)、admin 閲覧 (list/get_skill_admin) も service 化 (route の is_admin gate 維持)。**実 DB 証跡**: authenticated で content_md select → permission denied / 軽量列 select → 成功 (.qa/gap-144)。回帰テスト 1 本 (拒否 + カタログ可 + admin 詳細維持 + 注入継続) 追加、pytest 37+43 PASS / ruff / pyright 0 | — | 2026-08-18 |
| ~~GAP-145~~ ✅解消 (2026-08-18) | **「なんで PPTX や MP4、画像等できないのか？？」(経営者)** — 実行エンジン (本人 PC の Bridge + claude CLI) は HTML と同一なのに、成果物取り込み (GAP-137/139) が HTML 限定で、HTML 以外は本人 PC に置き去りだった | **全形式対応を実装**: ① 対応形式 = 画像 (png/jpg/gif/webp/svg)・文書 (pdf/pptx/xlsx/docx/csv 等)・動画 (mp4/webm/mov)、1 件 8MB・対応外拡張子は 4xx で誠実拒否 ② Bridge のスナップショット検出を全形式に拡張しバイナリは base64 送信 (agent_sdk 経路も対称)。実体は artifact_files (bytea / RLS default deny / gap-145 migration)、workflow_outputs に filedb://{id} 行 — **バージョンは project+stage+ファイル名で連鎖** (logo.png v1,v2…) ③ stage 判定はファイル名 (日英キーワード) → 直近指示 → 既定 (画像/動画=design、他=delivery) の決定的規則 (見積書.xlsx → estimate) ④ 配信 GET /outputs/{id}/content が実 MIME + inline / ?dl=1 attachment (自己署名 URL — Storage 未設定でも動く) ⑤ チャットに種類ラベル (画像/スライド/表計算/文書/PDF/動画) つき保存カード。S-G01 は既存 iframe のまま画像/PDF/動画をネイティブ表示。動画の**生成**は LLM 守備範囲外のため PC 操作の ffmpeg 等で (取り込み・配信は本ゲートで対応済 — README に明記)。**実 e2e**: 実 claude が実 PNG (74 bytes) を生成 → type=file/design で取り込み → image/png + PNG マジックバイトで配信 (E2E_OK: True / .qa/gap-145)。pytest 28+45 / bridge 116 / web 24 PASS、ruff / pyright / tsc 0 | — | 2026-08-18 |
| ~~GAP-146~~ ✅解消 (2026-08-18) | **モックスタジオの体験批判 (経営者スクショ)**: ①「ツールの中に組み込まれてますよ感がすごい — 枠の入れ子 + サイドバーで狭い。独自ツール的にしたい (戻れる状態は必須)」②「デスクトップなのにスマホくらいの広さでしか見れない — 実寸で表示できていない」③「チャットもしょぼい。タブのチャット (S-E01) の方がよっぽど優れているのになんで下がるの」 | **スタジオを独立ツール化**: ① ?mock= を `fixed inset-0 z-[500]` の全画面 overlay に — サイドバー・カード枠・max-width の外に出て全面使用。「← モック一覧」を topbar 左端に常設 (脱出路)。新規タブ/HTML/チャット依頼も topbar に集約。実バグも発見: TopBar (z-[100]) が旧 z-50 スタジオの上に被っていた → 修正 ② **実寸プレビュー**: iframe をデバイス実寸 CSS px (1440/1024/768/320) で描画し、キャンバス実測 (ResizeObserver) へ `transform: scale()` 自動フィット (Figma/Open Design 方式)。フィット/100% 切替 + 実測 % 表示、高さもキャンバスいっぱい ③ **会話を S-E01 同等に**: 指示 (右) → ワンダ応答 (左) + タイムスタンプの吹き出し、送信直後の楽観表示 + 「ワンダが作業中… (経過 N 秒)」実測カウンタ、Enter 送信 (IME 対応)、自動スクロール、340px 化 + アバターヘッダ。**実ブラウザ証跡**: 全画面 1680×1000 / デスクトップ実寸 1024→97% / ワイド実寸 1440→69% 自動フィット (.qa/gap-146 スクショ + 実測ログ)。web uc13 26 (+4) / bundle-h 149 / a11y 18 PASS / tsc 0 | — | 2026-08-18 |
| ~~GAP-147~~ ✅解消 (2026-08-18) | **実機で改訂が破綻 (経営者スクショ)**: ①「レスポンシブ修正して」が完全無視され TaskFlow 風の別デザインへ全面書き換え ②「v2 を作成しました」だけで何をしたか不明 ③ 97 秒間「作業中…」のみで何をしているか見えない。**根本原因を実データで特定**: GAP-143 がワンダの装着スキル (実ファイル ui-mockup 29KB + design-md 45KB + brand-voice 7KB = **81KB の「モックの作り方手順書」**) を**契約より前に全文注入** → 末尾の「指示に無関係な部分は変更しない」を圧倒し、モデルがスキル手順で画面を新規作成し直していた | ① **プロンプト階層の是正**: 改訂契約 (絶対ルール: 構造保持・全面書き換え禁止・サンプル持ち込み禁止) を先頭へ、スキルは「参考資料 (抜粋)」として後段 + 6,000 字上限 + 「資料であり指示ではない」明記 (generate も同様) ② **変更サマリー**: 出力契約に `---SUMMARY---` を追加し「何をどう変えたか」を meta_tags.note へ — 会話バブル・履歴・完了通知に表示 ③ **進行可視化**: POST /mocks/{id}/revise/stream (SSE) が stage (読み込み中→生成中→保存中) + 生成済み文字数を逐次配信 (llm_chain.llm_stream 新設 — relay delta を実況)。スタジオは「ワンダが HTML を生成中… n 文字 · s 秒」表示 ④ **会話文脈**: 同一画面の過去指示 (最大 5 件) を user prompt に同梱。**実 e2e (再現条件そのもの)**: 実スキル 81KB 装着 + 非レスポンシブ固定 1200px モック + 実機と同じ指示文 → 26 秒で v2 (旧 97 秒超)、構造完全保持・TaskFlow 混入ゼロ・viewport+@media 追加・具体的サマリー生成 (デザインノートの紺/角丸/Serif も適用)・全 10 チェック PASS (.qa/gap-147)。pytest 29 PASS (+3) / web uc13 26 PASS / ruff / pyright / tsc 0 | — | 2026-08-18 |
| ~~GAP-148~~ ✅解消 (2026-08-18) | 「チャットのランタイム表示を Claude Code みたいに」(経営者) — ツール名だけの実況で何を実行しているか見えない | **Claude Code 風タイムライン**: Bridge が assistant 完成メッセージの tool_use から実入力を要約 (extractToolDetails) し JSON {tool, summary} chunk で送信 → UI は `⏺ Bash(printf 'hello' > check.txt)` の実値行を時系列表示 (実行中 ⏺ パルス / 完了 ✓ / 経過秒)、名前だけの行は要約到着で格上げ。完了サマリーに内訳 (コマンド n 件・ファイル編集 m 件)。**実 e2e**: SSE tool chunk に実コマンド全文が載ることを実測 (E2E_OK)。**スマホ実測** (経営者質問): 390×844 実ブラウザで横スクロール無し・入力/送信可能 — PC に Bridge 常駐ならスマホのブラウザからチャット操作可能 (.qa/gap-148 スクショ)。bridge 118 / web 46 PASS / tsc 0 | — | 2026-08-18 |
| ~~GAP-149~~ ✅解消 (2026-08-18) | 「同じプロジェクトでも違う AI 社員なら会話は引き継がれないのか？」(経営者) — スレッドは project × 社員ごとで、社員をまたぐと文脈ゼロだった | **他スレッド要約の横断注入**: GAP-132 のローリング要約を利用し、同一プロジェクトの他の社員との会話要約 (最新 4 件 × 500 字、実在のみ) を全チャットの system prompt に自動注入 (_peer_thread_summaries) — どの社員も「他の社員と何を決めたか」を知った状態で応答。統合テストで別社員スレッドの決定事項が実 system prompt に載ることを実証 (chat_sse 12 PASS) | — | 2026-08-18 |
| ~~GAP-150~~ ✅解消 (2026-08-18) | **経営者との設計すり合わせ**:「どの社員も自由に使えると逆にやりづらい。基本は COO と会話し、COO が必要な社員へ繋いで引き継ぎ、終わったら COO に戻る。フローで進むのが基本。スキップもある。チャットタブも自由でなくフロー起点に」→ 合意:**フローが背骨、COO がハブ、自由は例外。ソフトゲート基本 + 契約/納品のみハードロック** | ① **project_flow_stages** (gap-150 migration): 状態 pending/done/skipped の 3 値のみ・現在 = 最小 seq の pending を導出 (遷移状態レス = 差し戻しも壊れない)。テンプレ: 受託 10 工程 (提案/見積/アーキ = skippable、契約/納品 = hard_gate)・社内 7 工程。担当 = 部門 → workspace 代表社員 (COO = ジャービス) ② API: flow GET (自動初期化)/complete (hard_gate は confirm 必須 403)/skip (理由必須)/reopen (後続の完了実績保持)。全操作 audit ③ **全社員の system prompt にフロー注入**: 「✓ 1. … / ● 2. 提案 ← 現在のステージ (担当: X)」+ 担当外依頼は担当への切替を案内 — COO 運用の中核 ④ **FlowRail** (チャット左ペイン最上部常設): クリックで担当スレッドへ (無ければ作成)、順序外はソフトゲート警告、現在に完了/スキップ (理由記録)、hard_gate は赤承認パネル、完了直後「次は「◯◯」— △△に繋ぎます」引き継ぎバナー。**実ブラウザ証跡** (.qa/gap-150 スクショ 2 枚 + DB 実測)。実バグも検出・修正: sendJson の envelope 二重剥がしでフロー消失 (実ブラウザで発見)。pytest 15 / web 173 (flow-rail 6 追加) PASS / ruff / pyright / tsc 0 | — | 2026-08-18 |
| ~~GAP-151~~ ✅解消 (2026-08-18) | 経営者すり合わせ「チャットというタブではなく工程ごとに会話が備わっている形に。スレッドは各工程の中。社員は固定的にこっちで決めておく」 | **工程 = 会話の入れ物**: project_flow_stages.thread_id (gap-151 migration) で各工程が専用スレッドを 1 本保持。チャット機能は完全同一。担当社員は工程 × 部門の代表で**サーバー固定** (ユーザーは選ばない・部門空は 409)。POST /flow/{stage}/thread が取得/作成 — 既存プロジェクトは担当社員の未割当スレッドを採用して会話を分断しない。UI: ナビ「チャット」→「**進行**」、開くと**現在工程の会話が自動で開く**、左ペインは進行フロー主役 + 既存スレッド一覧は「その他の会話」アコーディオンに収容。実ブラウザ証跡 (自動オープン + 担当固定 + アコーディオン)。pytest 4 PASS / web flow-rail 7 + bundle-h 156 PASS / ruff / pyright / tsc 0 | — | 2026-08-18 |
| ~~GAP-155~~ ✅解消 (2026-08-18) | 経営者すり合わせ「デザインは複数人同時だと怖くないか」「ブランチはしない。誰がどう変えたかわかって戻せたらいいレベル。モック以外もね」— 実 DB 確認で **mocks / workflow_outputs に (project, 画面/stage, version) の一意制約が無く、同時改訂で「v4 が 2 つ」できる lost update の実バグ**を検出 | **① 一意制約** (gap-155_version_guard.sql — 既存重複は version+1 退避でデータ不滅) **② 人間の改訂の同時衝突は 409 で誠実に** (黙って積み直さない): mocks create_version/duplicate/revise + outputs revise/fix-proposal 承認/restore を savepoint+IntegrityError→typed conflict→409、Web はサーバ文言をそのまま表示 **③ Bridge 取り込みは version 採り直し 3 回リトライで吸収** (新規ファイル由来で止める相手がいない) **④ 差分**: GET /mocks|outputs/{id}/diff/{other} — サーバ側 difflib が実 HTML 2 版から unified diff (+n/−n 行)。スタジオは各旧版に「差分」、S-G01 は「前版との差分」→ 共有の色分けモーダル。別チェーン/バイナリは 409 (テキスト化偽装しない) **⑤ 復元**: POST /outputs/{id}/restore — 旧版本文を新版として積む (履歴不滅・meta に author=restore/restored_from_version・最新版は 409)。モックは既存「複製」が同機能 **⑥ 付随是正**: mockdb 成果物の anchors/revise/fix-proposal が storage 直行 503 で不能だった → mockdb 対応ローダー統一 + mockdb 保存。**実 e2e**: 並列 curl 2 本の実レースで 201/409 を実証・料金ページ v3→v4 の実差分モーダル・御見積書 v1 復元→201 v3・最新版復元 409。pytest は**本物の 2 セッション同時実行**で conflict と ingest リトライを検証 (API 47 PASS / web 44 PASS) — 証跡 .qa/gap-155/ | — | 2026-08-18 |
| ~~GAP-152~~ ✅解消 (2026-08-19) | 経営者すり合わせ「段階的にフェーズとして進める。成果物は確定的になればそれ以上の追加はつけられない。追加は次フェーズでやる。プロジェクト内でも切り替えられたら。追加の見積もり・タスク・依存も分けて考えられる」 | **① delivery_phases 新設** (納品単位のフェーズ1..N — 既存 phases=工程とは別概念。active はプロジェクトに 1 つの partial unique。既存全プロジェクトへフェーズ1 backfill) **② 確定 = 凍結 + 次フェーズ自動開始**: POST /delivery-phases/{id}/freeze (confirm 必須の明示承認)。project_flow_stages の一意キーを (project, phase, stage) 化しフローはフェーズごとに新しい 1 周 (確定と同時に即初期化・工程スレッドは「見積（フェーズ2）」で周回区別) **③ スナップショット**: 新規行 (モック/成果物/タスク/Bridge 取り込み/改訂新版/復元) は常に active フェーズへスタンプ = 確定フェーズには構造的に足せない。凍結行への破壊操作 (メタ更新/削除/破棄) は 409。改訂・復元は許可し新版が次フェーズの追加作業に積まれる (追加見積の分離が自動) **④ 切替**: GET /flow?phase= で当時の周回閲覧、/mocks・/outputs・/tasks?delivery_phase_id= でフェーズ絞込 **⑤ UI**: S-E01 進行にフェーズバー + 確定 CTA (凍結件数の実数を出す赤承認) + 凍結スナップショットの読み取り専用ビュー、S-H01 モック一覧にフェーズ絞込 **⑥ チャット注入**: 全社員の system prompt に「過去フェーズは凍結・追加要望は現在フェーズとして扱う」。**実 e2e**: 実データ (モック10・成果物4) の e2e プロジェクトで UI から確定→フェーズ2 開始・凍結モック破棄 409・複製新版がフェーズ2 へ・二重確定 409 を実測。pytest +2 (95 PASS) / web +2 (flow-rail 9 PASS) — 証跡 .qa/gap-152/ | — | 2026-08-19 |
| ~~GAP-154~~ ✅解消 (2026-08-19) | 経営者すり合わせ「見積もりとか出力のテンプレは自作できる形にして基本的にそれを使う。workspace 単位のみ」 | **output_templates 新設** (workspace × 種類 = workflow stage 体系 14 種で upsert・RLS member)。**「基本的にそれを使う」を構造で保証**: ①全チャットの system prompt に現在工程 (active フェーズ current stage) のテンプレを自動注入 (「必ずこの構成・項目・書式に従う。独自構成へ勝手に変えない」の契約文つき) ②スティーブ改訂・AI 修正提案の承認適用にも該当種類のテンプレを注入 (revise_output 全経路)。API GET/PUT/DELETE /workspaces/{id}/output-templates[/{stage}] (未知種類 404 / RLS 404 / 削除で誠実にテンプレ無し生成へ復帰)。UI はナビ「WS設定」の実体ページを新設 (従来デッドリンク) — 種類ピッカー (設定済みバッジ) + Markdown エディタ。**実 e2e**: UI から議事録テンプレ保存 → 実チャットの system prompt への注入を context-preview で実測。pytest +3 (改訂 prompt capture 含む) / web +3 — 証跡 .qa/gap-154/ | — | 2026-08-19 |
| ~~GAP-153~~ ✅解消 (2026-08-19) | 経営者すり合わせ「ユーザー提案フローはしない。運営として裏で AI を走らせて自動で分ける。その中でセキュリティも担保する」 | **運営側バッチ** (運営の ANTHROPIC_API_KEY 費用 — テナントのサブスク不使用・未設定は 503) が全テナントの良質ナレッジ (usage≥3 or confidence≥0.7) を走査し、LLM が有用性判定 + 固有情報除去の一般化本文へ書き直し。**セキュリティ二重担保 (LLM を信用しない)**: 決定的リークスキャン — 元テナントの workspace 名/全プロジェクト名/氏名/メールを DB 照合 + email/電話/URL 正規表現。1 件でも残存すれば rejected_security に機械排除し、**承認の瞬間にも再スキャン**。**承認ゲート**: knowledge_curations (RLS default deny — テナント不可視) に貯め、運営 admin が S-T06 の承認キューで承認して初めて platform ナレッジ (is_anonymized・approved_by 記録) として全アカウント共有 — 既存 RAG が横断参照し embedding は GAP-133 自動バックフィル。1 ソース=1 提案で再走重複なし。**実 e2e**: リークスキャンが実データの社名 (e2e137-ws)/プロジェクト名/メール/URL を実検出して除外、UI 承認→platform 行公開、authenticated ロールから curations 0 件 (default deny) を実測。pytest +3 / web +2 (計 44+2 PASS) — 証跡 .qa/gap-153/ | — | 2026-08-19 |
| ~~GAP-156~~ ✅解消 (2026-08-19) | 経営者すり合わせ「既存プロジェクトのアップロードで途中からでも。既存資料をツールの形式に当てはめられる状態に」 | **POST /projects/{id}/import** (一括 30 ファイル・LLM 不使用の決定的仕分け): Bridge のチャット成果物取り込み機構 (GAP-137/139/145) を Web 一括アップロードに開放 — HTML はモック/見積・提案・テスト仕様等の成果物へ自動仕分け、MD/テキストは閲覧可能 HTML に包んで成果物へ (内容不変)、画像/PPTX/PDF/Excel/動画は filedb ファイル成果物へ。すべて現在フェーズにスタンプ (GAP-152) し、1 ファイルの失敗は per-file の honest エラーで全体を落とさない。**フロー現在地**: 取り込めた種類から「もう終わっている工程」を提案 (現在フェーズの pending × フロー順) — **自動反映せず**ユーザーのチェック + 確定で既存 flow complete を実行。UI はプロジェクトナビに「取り込み」を新設。**実 e2e**: 実ブラウザで 4 ファイル取り込み→仕分け→3 工程確定→進行タブに ✓ 反映、成果物のフェーズ分離を実測。pytest +1 (11 PASS) / web +2 — 証跡 .qa/gap-156/ | — | 2026-08-19 |
| ~~GAP-157~~ ✅解消 (2026-08-19) | 経営者フィードバック「フェーズはヘッダーとかで画面切り替えるくらいでもいい。各タブの細々なところでやるというよりも全体で切り替える感じ。しかも切り替えてもちゃんと他の前フェーズとかも把握してちゃんと依存とかもタスク分解も徹底できる状態で」— GAP-152 はフェーズ選択と確定を進行タブ内 (FlowRail) に置いていた | **ヘッダー 1 か所に集約 + 全タブ追従 + 前フェーズ文脈の AI 注入**: ① `components/layout/PhaseSwitcher.tsx` — プロジェクト文脈で常設のフェーズピル (表示中フェーズ・件数つき一覧・「確定して次フェーズへ」の赤枠明示確認)。GAP-152 の hard gate をここへ移設し FlowRail 内の UI は撤去 ② `lib/currentPhase.ts` — localStorage `atelier_phase_sel_{projectId}` + `atelier-phase-changed` イベントで選択を共有し、進行/モック/タスクの 3 タブが同時に追従。確定済みを選ぶと 3 タブとも読み取り専用スナップショット ③ `services/flow/phases.py: phase_history_block` — チャット system prompt に確定済みフェーズの完了工程・成果物 (版/種類)・画面モックと「凍結済み。追加変更は現在フェーズの作業。新しいタスク・依存は上記確定内容を前提に分解すること」を毎回注入 → フェーズを切り替えても AI が前フェーズを把握したまま依存・タスク分解できる。**検証**: 実ブラウザ e2e (.qa/gap-157/ — ピル/一覧/確定確認/3 タブ同時スナップショット/復帰の実測スクショ 7 枚) + context-preview に実データが注入される curl 実測 + API pytest (test_flow) + web vitest (phase-switcher 2 / flow-rail 8 書き換え) | — | 2026-08-19 |
| ~~GAP-158~~ ✅解消 (2026-08-19) | 経営者フィードバック「テンプレの認識が違いすぎる。基本これらはスキルでやるので決まった内容になりやすい。だからフォーマットとかではなく**出力のデザインを作る場所**。テンプレートに関してはそのタブ作っていい。そこのデザイン (md や json はスキルで整えるし、最後人間=クライアントに見せる html や pdf のデザインのテンプレって意味) なので **open design の機能で作る状態で十分**。それをしっかり見れる感じで」+「そもそも元々の WS の中身がガラッと変わっているけどなんで？？」 | **GAP-154 (構成テンプレ = content_md を人が手書き) を廃止し「見た目の型」へ作り直し**: ① DB `output_templates` を drop → `output_design_templates` (HTML 実体・版連鎖・mockdb 保存・RLS は select/insert のみ = 履歴不滅) ② 専用タブ「テンプレート」`/templates` — Open Design 型 3 ペインスタジオ (左: 14 種類 + 設定済みドット / 中央: **A4 実寸 794×1123 を fit 縮小した大プレビュー** / 右: ワンダへの指示 + 経過秒 + 版履歴クリックで過去版閲覧) ③ ワンダの契約 = 完全 HTML 全文のみ・内容はダミーでよくレイアウト/配色/タイポ/表様式/余白が本体・A4 印刷 PDF 前提・外部リソース非依存・改訂は指示箇所以外を変えない ④ **注入**: 成果物の生成 (チャット) と改訂 (スティーブ) の system prompt に該当種類の HTML を自動注入し「このテンプレのレイアウト・CSS・表の様式・トーンを踏襲し、内容だけを依頼内容とスキルの構成に従って差し替える」を強制 = 内容はスキル/見た目はテンプレの分離を prompt で担保 ⑤ 生成は**本人の PC の Bridge = 本人サブスク** (relay→agent_sdk→API の確定チェーン)、経路無しは 503 で正直に断り偽テンプレを積まない ⑥ **WS設定の復旧**: GAP-154 が `app/workspace-settings/page.tsx` を新設したことで Next.js 実ルートが `/workspace-settings → /auth/s_a03` の rewrite を覆い隠し、元の設定画面 (名前/アイコン/メンバー/招待/MCPトークン/AI学習/プラン/退会) がテンプレ画面に置き換わっていた実バグを、当該ディレクトリ削除で完全復帰。**検証**: 実ブラウザ e2e (.qa/gap-158/ — スタジオ/過去版/種類切替/UI からの生成成功/WS設定復帰の実測スクショ 6 枚) + curl 実測 (作成→改訂 v2→版履歴→署名 URL 配信→context-preview 注入文面→RLS 越境 404→経路無し 503) + API pytest (TestGap158DesignTemplates 4) + web vitest (design-template-studio 4)。※ この環境は Bridge/API キー無しのため生成は fake LLM スタブで**配線と契約**を検証 (README に明記) | — | 2026-08-19 |
| ~~GAP-159~~ ✅解消 (2026-08-19) | 経営者指示「出力デザインは 見積/提案/請求/契約/NDA/要件定義/アーキ設計/デザイン仕様/テスト仕様/納品・完了報告 で十分」「変更加えれる前提で初めのデフォルトはこちらの管理側で設定しているものでいい」「管理側でデフォルトを決めるがそこでも変更・更新・追加などできる状態か」 | **種類を 10 種へ整理 + 運営既定の継承モデル**: ① STAGE_LABELS を指定の 10 種に絞り、外れた工程 (議事録/機能分解/タスク一覧/実装ドキュメント) はデザイン注入対象からも除外 ② `output_design_templates.is_platform_default` (workspace_id NULL) を追加し、**運営既定を全テナントが継承** — RLS は「既定は全テナント閲覧可・書込不可 / 自社版は自社のみ」、運営側の書込は admin gate + service セッション ③ 各社が変更すると自社版が実効になり (既定 HTML を**土台に**改訂するので作り直しにならない)、**「運営の既定デザインに戻す」は削除ではなく新版として積む**ため履歴不滅を維持 ④ AI 注入も継承する (自社版が無ければ既定が system prompt に入り「（運営既定）」表記) ⑤ 運営管理画面 `/admin/design-templates` は**ユーザー側と同一コンポーネントを mode=platform で使用** — 作り・操作・見た目が完全に同じで追加・更新・改訂できる。**併せて実バグ 2 件を修正**: routes/outputs の content 配信 service セッションが単一 lru_cache で event loop に固定 (asyncpg は loop を跨げない) → loop 毎分離、GAP-153 キュレーションテストの運営横断グローバル件数依存 → 自テナント由来にスコープ。**検証**: 実ブラウザ e2e (.qa/gap-159/ — 運営スタジオ/既定改訂 v2/テナント継承/上書き/既定に戻す の実測 5 枚) + curl 実測 (運営作成 → 継承 → 上書き → reset → 一般ユーザー 403) + API pytest (TestGap159PlatformDefaults 3) + web vitest (studio 7) | — | 2026-08-19 |
| ~~GAP-160~~ ✅解消 (2026-08-19) | 経営者指示「ユーザー側も管理側もテンプレ部分も戻れる状態で左のサイドバーなくしてしっかり全画面で見れる状態に」「進行も戻れる状態でサイドバーはなくしたい。左パネルは右パネルみたいな作りにして。チャット部分との境がないしわからない」「ヘッダーとサイドバーのアトリエロゴのヘッド部分のラインがずれているので統一。ロゴ小さくしていいので高さをヘッダーに合わせる」 | **全画面モード + 戻る導線 + パネルの作り + ロゴのライン統一**: ① `AppShell.hideSidebar` と `TopBar` の戻るリンクを新設し、テンプレート (/templates) と進行 (/chat) はサイドバー無しの全画面に。代わりにヘッダー左へ「← プロジェクト一覧へ戻る」「← プロジェクトへ戻る」を常設 ② 運営側 /admin/design-templates も運営メニューを畳んだ専用ヘッダー (「← 運営メニューへ戻る」+ 運営タグ) の全画面 ③ チャット左パネルを右のコンテキストペインと同じ作り (aside・白地・右境界線・独立スクロール) にして中央チャットとの境目を明確化 ④ サイドバーのブランド枠を h-14 にしてヘッダーと境界線を 1 本に揃え、ロゴは h-5/h-6 に縮小。**検証**: 実ブラウザ実測 (.qa/gap-160/ — HEADER_BOTTOM=56 と SIDEBAR_BRAND_BLOCK bottom=56 の一致、テンプレ/進行/運営でサイドバー 0・戻る 1、戻るリンクが実際に /projects へ遷移、左パネルの border-right/白地を computed style で確認、スクショ 4 枚) + web vitest bundle-h 176 PASS (回帰) | — | 2026-08-19 |
| ~~GAP-161~~ ✅解消 (2026-08-19) | 経営者指摘「デザインモックも、このテンプレもだけど画像や PDF やファイルやエクセルをアップロードしてそれを参考にすることがチャットでできていないけどどうして？？」 | **実バグだった**: チャット添付は保存・表示のみで **LLM に一切渡っていなかった** (chat_messages.attachments が prompt に入らない)。加えて Excel/Word/PowerPoint は許可形式ですらなく、スタジオには資料を渡す口が無かった。**実装**: ① `services/attachments` で PDF/Excel/Word/PowerPoint/CSV/テキストを **LLM 不使用の決定的抽出** — 追加費用ゼロ・Bridge/サブスク/API の全経路で同一に効く ② チャットは現在 + 直近 6 メッセージ分の添付 (最大 5 件) を「# 添付資料」として system prompt に注入 (「さっき送った資料を見て」が成立) ③ 取得失敗・未対応・破損は「取り込めませんでした」と明示、**画像は内容を推測しない**と明記 ④ 許可形式に xlsx/xls/docx/pptx を追加 (API・Web 両方) ⑤ `POST /reference-uploads` を新設し、モックスタジオ (ワンダ) とデザインテンプレスタジオに「参考資料を追加」を設置 → 生成/改訂の system prompt に「# 参考資料」として入る ⑥ 画像は Bridge 経路で実物を渡す (GAP-141 の作業場 seed を base64 対応に拡張し、本人 PC の Claude Code が直接開ける)。**検証**: 実 API で実 xlsx をアップロード → 実 storage → 抽出 → context-preview の system_prompt に明細行が入ることを実測 (.qa/gap-161/curl-evidence.txt。storage だけ Supabase 署名 API 互換のローカルスタブで実 HTTP 往復 — 抽出・注入は本番同一コード)、実ブラウザで両スタジオのピッカーと実 PUT 成功を確認、API pytest 8 追加、web vitest 3 追加、bridge vitest 118 PASS | — | 2026-08-19 |
| ~~GAP-162~~ ✅解消 (2026-08-19) | 経営者質問「PDF や資料、エクセル、html など様々で出せる感じだよね？？」「これをこのままリンクとして資料を渡せる状態にもなっている？？」— 実態はどちらも未実装 (署名 URL は認証必須の社内閲覧用、書き出し無し) | **クライアントにそのまま渡せる状態にした**: ① `POST /outputs/{id}/share-links` で期限つき共有リンクを発行。**トークンは DB にハッシュのみ保存**し一覧では URL を返さない (発行直後だけ表示) ② `GET /share/{token}` は**認証不要**でデザイン適用済みの成果物を表示し、上部に「PDF で保存 / 印刷」(印刷時は消える)。失効・期限切れは **410** で正直に断り、閲覧回数も記録 ③ 書き出し `GET /outputs/{id}/export?format=html|xlsx` — HTML はそのまま、Excel は HTML 内の表を決定的変換 (見積・請求など表が主役の成果物)。**表が無ければ 409 で断り空ファイルを出さない** ④ PDF はサーバー側レンダリングを使わずブラウザ印刷 — デザインテンプレの CSS を最も忠実に出せるため (偽 PDF を作らない判断を README に明記) ⑤ 成果物ビューアに「クライアントに渡す」パネル (発行・URL コピー・有効リンク一覧・無効化)。**検証**: 実データの見積書で実測 (.qa/gap-162/ — 発行 → Cookie 無しブラウザで 200 表示 → 一覧は URL 非返却・閲覧数加算 → xlsx を openpyxl で開いて明細行を確認 → 失効後 410)、クライアント視点のスクショ 3 枚、API pytest 2 / web vitest 3 | — | 2026-08-19 |
| ~~GAP-163~~ ✅解消 (2026-08-19) | 経営者質問「エクセルとかだとここの表示はどうなるの？？」「エクセルとかスプレッドシートをここで修正とかできるの？PDF もだけど」— 実態は Excel はダウンロードのみで画面では中身が見えなかった | **表示と編集を実装 (できない線は正直に)**: ① Excel(.xlsx)/CSV を**シート切替つきの表として表示**し、**セルを直接編集 → 新しい版として保存** (`GET/POST /outputs/{id}/sheet`)。保存は元と同じ xlsx なのでそのままクライアントに渡せる ② **数式・書式・グラフは保持しない (値のみ)** ことを画面と API の note に明記し黙って落とさない ③ 編集は新バージョンとして積み元の版は不変 (履歴不滅) ④ **PDF は表示のみ** — inline 配信でブラウザ内蔵ビューアに出し、編集要求には 409 で「直接の編集はできません。修正は元の成果物を AI に直してもらってから出し直してください」と理由と代替を返す (編集できるふりをしない) ⑤ artifact_files は authenticated から revoke 済のため、行の認可は RLS・実体は service 経路に分離 (mock_contents と同方式)。**検証**: 実 xlsx と実 PDF で実測 (.qa/gap-163/ — 表取得 → 編集保存 → 新版に反映・元版不変 → 保存版を openpyxl で開いて確認 → PDF は 409 + inline 200)、実ブラウザでセル編集 300000→999000 が新版に反映されるまでを確認、API pytest 2 / web vitest 3 | — | 2026-08-19 |
| ~~GAP-164~~ ✅解消 (2026-08-19) | 経営者の疑問「チャット部分に学習に使われませんと書いているけど、いいものは自動でナレッジ的にする状況にしていなかったっけ？？ここら辺がわかっていない」 | **3 つの別の話が混ざっており、実装も欠けていた**: A=外部のモデル学習 (渡さない・鉄則どおり)、B=自社ナレッジへの自動蓄積 (**未実装だった** = 記憶とのズレの正体)、C=全アカウント共有 (GAP-153 で実装済だが入口 B が無く空回り)。**実装**: ① `services/knowledge/auto_capture.py` — 応答完了後の非同期処理で直近会話から**一般化できるノウハウだけ**を抽出し workspace ナレッジ (scope=common / source_type=ai_extracted) に保存。抽出は**本人のサブスク経路**で走る ② プロンプトで社名・人名・金額・URL・日付など**案件固有の事実の持ち出しを禁止**、該当が無ければ何も作らない、壊れた応答や短すぎる本文は採用しない、同じ題は二度作らない、失敗しても会話は止めない ③ チャットの表示を実態に合わせ**「外部の学習には使いません · 社内ナレッジには残ります」**に変更 (ホバーで確認・削除できる旨を補足) ④ これで C は B で貯まったものを運営が匿名化+承認して広げる一本の流れになった。**検証**: 実チャット 1 往復で抽出が走ること、fake 経路では 0 件のまま (でっち上げない)、抽出契約をスタブして実 DB へ 1 件作成→2 回目は重複なし、実 API のナレッジ一覧と実ブラウザのナレッジ画面に出ることを確認 (.qa/gap-164/)、API pytest 3 | — | 2026-08-19 |
| ~~GAP-165~~ ✅解消 (2026-08-19) | 経営者質問「どうフェーズを確定として判断して、そこからの変更や修正は許容できる状態か。これはできない・フェーズ2として作りましょう、の境目はどうしているのか」 | **境目は既に構造で引かれていた (確定済みの版は不変 / 修正すると必ず現在フェーズの新版になる) が、判断材料と説明が無かった**。① `GET /projects/{id}/delivery-phases/{phase}/freeze-check` を新設し、**未完了工程・完了していないタスク・未解決コメント・成果物/モック件数を実数で提示** (判定は人が行い、AI は確定しない) ② 確定時に「このフェーズで確定した範囲」メモ (任意) を書け、実データの自動サマリー (完了工程/成果物/画面) と併せて phase.note に保存 ③ UI 文言を「確定後も修正はできます。確定済みの版は記録として残り、修正すると新しい版が次フェーズの成果物になります」に統一 (確定ダイアログ・進行タブ・モック一覧) ④ AI にも確定メモと境目ルールを注入 (「確定済みの版は書き換えられない。修正依頼は断らずに新版を作り、現在フェーズの作業として記録される旨を伝える」)。**検証**: API pytest 3 (freeze-check の実数 / **確定後の修正が 201 で通り新版は次フェーズ・元版は不変** という境目の構造的証明 / AI 注入) + web vitest 1 追加 (残作業の一覧・確定後も直せる表示・確定範囲メモの送出) | — | 2026-08-19 |
| ~~GAP-166~~ ✅解消 (2026-08-19) | 経営者指摘「でもこれ Claude Code ではできるくない？？ サブスクのプランでやっているしできるはずだけどどうしてできないの？？ できる状態にして欲しい」(Excel/PDF の編集) | **そのとおりだったので経路を通した**: サーバー側は値だけの表しか扱えない (GAP-163) が、本人の PC で走る Claude Code はファイルそのものを開いて直せる。① `POST /outputs/{id}/ai-file-edit` — 認可は RLS で確認しジョブを積む (ジョブ行は service 経路。差し替え口 `job_session_factory` を公開) ② **作業場 seed を拡張**してプロジェクトのファイル成果物 (xlsx/pdf 等) の最新版を base64 で配る ③ system prompt で「同じファイル名で上書き保存・形式は変えない・指示に無い箇所は触らない」を拘束し、Bridge が変更を検出して**新バージョン**として取り込む ④ 費用は**本人の Claude サブスク**、**Bridge オフラインは enqueue せず 503 で正直に断る** ⑤ UI は成果物ビューアに「AI にファイルごと直してもらう（あなたの PC の Claude Code で実行）」を Excel 表編集と併設し、PDF など表にできない形式でも表示。**検証**: API pytest 3 (オフラインで 503 かつジョブ 0 件 / PDF でもジョブが積まれ prompt・system prompt・tools_mode・requested_by が正しい / 作業場 seed に実体 base64 が入る) + web vitest 2。※ Bridge 実接続での往復 (直って戻るところ) はこの環境に Bridge が無いため未実測 (README に明記) | — | 2026-08-19 |
| ~~GAP-167~~ ✅解消 (2026-08-19) | 経営者指摘「この形式とか、しかも全て溜めるのはノイズになるし、ちゃんとどれを入れるかなどもできる状態になっているのか？？」 | **ご指摘どおり GAP-164 は拾ったものを直接ナレッジに入れていた** (溜めてから消す形)。**入れる前に選ぶ**形へ変更: ① `knowledge_candidates` を新設し、自動抽出は**候補**にだけ入る (人が採用して初めて `knowledge_nodes` になる) ② ナレッジ画面に候補セクション — **採用 / その場で編集して採用 / 却下** ③ **却下した題は二度と提案しない** (workspace × title 一意 + 重複判定にナレッジと候補の両方を含める) ④ **候補ゼロなら UI に何も出さない** (それ自体をノイズにしない) ⑤ 機械側の絞りも維持 — 一般化できるものだけ・案件固有の事実は書かせない・1 回最大 2 件・短すぎる本文や壊れた応答は捨てる・該当なしは空。**検証**: API pytest 5 (候補一覧 → 編集して採用 → ナレッジ化 / 二重採用 409 / 却下はナレッジにならず同じ題は自動抽出からも再提案されない / 自動抽出は候補にだけ入りナレッジ本体は 0 件) + web vitest 5。**併せて回帰修正**: ナレッジ画面に候補セクションを足したことで既存 S-K01 テストが呼び出し順とラベル曖昧一致で落ちたため、パス特定に直し候補パネルも想定外応答を描画しないよう防御 | — | 2026-08-19 |
| ~~GAP-168~~ ✅解消 (2026-08-19) | 経営者指摘「以前実装で、もし接続できていない場合、接続させるフローが出てくる状態に更新しているはずだけど、なんで出てない？ バグじゃないか？？」 | **ご指摘どおり、私の実装漏れ (実バグ) でした**。接続フロー (`ConnectFlow`) は**チャット画面 S-E01 の中にしか存在せず**、その後に足した Bridge 必須の画面 (モック生成・モック改訂・出力デザインテンプレ・Excel/PDF の AI ファイル修正) では「AI 実行経路が使えません」の**文字が出るだけで繋ぐ手段が無かった**。① 接続手順を `components/bridge/BridgeConnectFlow.tsx` へ切り出し (中身は GAP-122 のまま) — チャット画面もこれを使う形に ② `BridgeOfflineNotice` を新設 (「何が止まったか」+「あなたの PC の Claude で動く」+ その場に折りたたみ式の接続フロー) と `isBridgeOffline(error)` ③ **5 画面すべてに露出**: テンプレスタジオ (作成/改訂で文言が変わる) / 成果物のファイル AI 修正 / モックスタジオの改訂 / モック一覧の生成 / キャンバス内の改訂 ④ **グローバル toast の 503 文言も是正** — 「サーバーでエラーが発生しました。」は嘘 (サーバーは正常で、未接続なのは利用者の PC) → 「お使いのパソコン (Bridge) が未接続です。画面の案内から接続してください。」 ⑤ 二重表示にならないよう、未接続時はエラー行を出さず接続フローに一本化。**検証は fake LLM を切った実 API (= 本当に実行経路ゼロ) + 実ブラウザ**で 4 画面すべてに接続フローが出ること、実際に「接続トークンを発行」→ `atelier-bridge://connect?api=…&token=…` の「アプリで接続」まで出ることを実測 (.qa/gap-168 スクショ 5 枚)。web vitest 8 追加 + 既存 3 ファイルを「文字だけ」から「文字 + 接続フロー」へ更新。全 web 716 PASS / tsc 0 / biome 0 | — | 2026-08-19 |
| ~~GAP-169~~ ✅解消 (2026-08-19) | GAP-168 で全画面に接続フローを出した直後、**その接続フローで発行したトークンで実際に Bridge を繋いでみた**ところ繋がらなかった。この環境に headless Bridge を本当に起動して往復させて初めて出た不具合で、いずれも画面上は成功に見えるもの | **実バグ 3 件を修正**: ① `verify_bridge_token` が最初に `ATELIER_BRIDGE_TOKEN` (インスタンス共通トークン) の設定有無を見て**未設定なら 500** を返していた — これは kanban / タスク実行系のための任意設定で本人の PC を繋ぐのに必須ではなく、本番で入れ忘れると**画面からトークンを発行しても全員 `bridge auth failed: 500`**。本人トークンの検証を独立させ、未知トークン・ヘッダー無しは 401 に (誤設定と誤認証を混同しない) ② `GET /chat-relay/{job}/workspace` が `f["html"]` 決め打ちで、base64 のファイル項目 (GAP-161 添付 / GAP-166 ファイル成果物) が 1 つでも混ざると `KeyError` で **seed 全体が 500** → Bridge は「seed 失敗でも実行は止めない」設計のため**作業場が空のまま Claude Code が起動**しており、GAP-166 は実際には成立していなかった。html/content_b64 の両対応 + 使わない側は null ではなく省略 (`response_model_exclude_none`) ③ Bridge の seed 展開が `f.html !== undefined` で分岐しており、サーバーの `html: null` が真になって `writeFileSync(target, null)` が投げ、**catch で黙って握り潰されて**いた → 型判定 (`typeof === 'string'`) + api-client で null→undefined 正規化。**実往復を実測**: 画面と同じユーザートークンで `headless.js --loop` を実 API に繋ぎ、`claude` の位置に「作業場の xlsx を実際に開いて 1 行足す」スタブを置いて、ジョブ → 作業場 seed → 実ファイル編集 → 成果物検出 → **新版 v2 として取り込み**まで 4 PASS / 0 FAIL (.qa/gap-169/roundtrip.txt)。GAP-166 の「Bridge 実接続の往復は未実測」の但し書きはこれで解消 (実測したら壊れていた)。回帰テスト: API 2 (インスタンストークン無しで本人トークンが通る / HTML と Excel が混ざった seed が 500 にならない) + Bridge vitest 1 (html:null の項目がバイナリとして展開される)。bridge 119 PASS / tsc 0 / eslint 0 | — | 2026-08-19 |
| ~~GAP-170~~ ✅解消 (2026-08-19) | GAP-161/163 で `pyproject.toml` に足した Excel/PDF 系の依存 (pypdf / openpyxl / python-docx / python-pptx) が `uv.lock` に反映されておらず、`uv lock --check` が fail する状態だった (作業中に lock が毎回書き換わるのを「ノイズ」と誤認して revert し続けていた私のミス) | 影響は**クリーン環境の CI / デプロイ**: `uv sync --locked` を使う経路で、添付ファイルのテキスト抽出 (GAP-161) と Excel/PDF の表示・編集 (GAP-163) に必要な依存が入らない、または lock 検証で止まる。再生成した lock を commit し `uv lock --check` が通る状態に戻した (Resolved 111 packages / exit 0)。**修正前の状態も実測**: 旧 lock に戻すと "The lockfile at `uv.lock` needs to be updated, but `--locked` was provided" で fail することを確認 | — | 2026-08-19 |
| ~~GAP-171~~ ✅解消 (2026-08-19) | 確定アーキテクチャは「全ユーザーが自分の PC の Bridge = 自分の Claude サブスクで実行」なのに、**画面から使う AI 機能の半分が運営の `ANTHROPIC_API_KEY` を直接叩いていた** (= 運営の従量課金で動いていた)。GAP-168/169 の作業中に発見 | **4 機能を費用順チェーンへ統一**: 成果物の改訂 (スティーブ) / コメントからの修正提案・適用 (スティーブ) / 営業ドラフト (トニー) / 次フェーズ提案 (ジャービス) を relay → agent_sdk → API キー → fake に乗せ替え。① `llm_chain.py` に `llm_complete_or_injected()` を追加し、テスト注入の client は差し替え口として残したまま本番経路だけを移行 ② `meta.model` は**「どの実行経路 = 誰の費用で作られたか」**を記録する意味に変更 (`relay`/`agent_sdk`/`api`/`fake` — GAP-138 のモックと同表記) ③ 未接続 (503) は 3 画面 (成果物ビューア・営業ドラフト・工程) でも**その場に接続フロー**を出し、文言も「◯◯ AI が未設定」→「お使いのパソコン (Bridge) が未接続」に是正 (未設定なのは運営ではなく利用者の PC) ④ ナレッジ自動キュレーションは**意図どおり運営負担のまま**据え置き。**証拠**: `ANTHROPIC_API_KEY` を設定せず `ATELIER_LLM_PROVIDER=relay` で API を起動し、headless Bridge を実接続して、改訂 (5 PASS: 新版作成・経路 relay・作成者 steve・本人 PC が返した内容が実体に入る) と 営業ドラフト (3 PASS) と フェーズ提案 (DB 実測 model=relay) を実測 (.qa/gap-171)。回帰テスト 2 件 (relay + Bridge オフライン → 503 かつ偽の版を積まない / 経路ゼロ → 503 で運営費用で生成しない)。web 716 PASS / tsc 0 | — | 2026-08-19 |
| ~~GAP-172~~ ✅解消 (2026-08-19) | `scripts/dev-bootstrap.sh` が migration を 18 本も黙って skip したまま「✓ DB ブートストラップ完了」と表示していた。新規セットアップすると **delivery_phases / mock_contents / output_design_templates / knowledge_candidates / project_flow_stages / artifact_files が丸ごと無い DB** (48 テーブル) ができ、アプリが動かない。経営者の Mac で新規構築した場合も同じ | **原因 3 つを全部修正**: ① `ls | sort` は `gap-*.sql` を土台の `t-d-*.sql` より先に並べるため、1 周だけの適用では依存順が解けず必ず失敗 → それを「Supabase 依存だから skip」と飲み込んでいた。**収束するまで何周でも回し**、最後まで通らないものだけをエラー本文つきで本物の失敗として表示する ② ロール GRANT が migration の後だったため、`authenticated` に切り替えて実データを読む **RLS 越境試験 (t-d-31/32 — R-T08 致命級) が `permission denied` で毎回 skip され、1 度も実行されない DB** になっていた → GRANT を関数化し各周の頭でも適用 ③ `grant all on all tables` が migration の張った**列レベル REVOKE を全部取り消し**、機密列 (encrypted_value / content_md / artifact data) がアプリロールから丸見えの DB になっていた → revoke を含む 15 本を GRANT の後に再適用。**併せて**: `service_role` を `bypassrls` つきで作成 (R-T07 の前提。素の create role だったため越境防止テストが落ちる DB だった) / `t-d-31`・`t-d-32` の fixture insert に `on conflict do nothing` (後から入った owner membership 自動作成トリガと重複キーで衝突していた)。**実測**: 修正前 63 applied / 20 skipped・48 テーブル → 修正後 **83 applied / 0 skipped (2 周で収束)・56 テーブル**、機密列 revoke 有効・非機密列は読める・service_role BYPASSRLS=true を確認し、まっさらな DB に対し **RLS テスト 9 件が全 PASS** (.qa/gap-172) | — | 2026-08-19 |
| ~~GAP-173~~ ✅解消 (2026-08-19) | API の全件テストを**運営シード適用済みの DB** (= 本番や開発機に近い状態) で走らせると 8 件が `duplicate key value violates unique constraint "ai_employees_workspace_id_name_key"` で**フィクスチャの段階で**落ちる。CI はシードを入れない使い捨て Postgres を使うので通ってしまい、**実 DB に近いほど落ちる**という逆転が起きていた | **原因**: `workspaces` には運営シードの AI 社員テンプレを実体化するトリガ (`workspaces_bootstrap_ai_employees`) が付いており、ワークスペース作成時点で jarvis/tony/natasha/steve/… が既に居る。テストが同じ名前で insert すると一意制約に当たる。**修正**: `tests/routes/_fixtures.py` に `ensure_ai_employee()` を追加し「その名前の社員が居ることを保証し**実際の id を返す**」形に統一 (`on conflict (workspace_id, name) do update … returning id`)。固定名 (steve/tony/jarvis/wanda) を直接 insert していた **9 か所すべて**を置換 (test_outputs 5 / test_knowledge / test_ai_employees / test_chat / test_flow / test_mock_generate / test_skills)。ランダム名の箇所は元から衝突しないのでそのまま。**実測**: 修正前は同じシード入り DB で 10 failed / 1070 passed → 修正後 **1082 passed / 1 skipped / 0 failed** (シード入りの実 DB に対して API 全件がグリーンになったのはこれが初めて。8 件は本 GAP のフィクスチャ修正、2 件は GAP-172 の DB 構築修正で解消。.qa/gap-173/full-run.txt に全件ログ) | — | 2026-08-19 |
| ~~GAP-174~~ ✅解消 (2026-08-19) | Excel / PDF の成果物を開くと、表もファイル名も正しく出ているのに右下に赤い **「エラーが発生しました（HTTP 409）」** が出ていた (GAP-168 の実ブラウザ証跡 `.qa/gap-168/168-output-file-edit.png` に写り込んでいたもの) | **原因**: 成果物ビューアは `GET /outputs/{id}/anchors` (コメントを本文位置に紐づける id 一覧) を必ず取りに行くが、Excel/PDF (filedb) はテキストではないので API は 409「バイナリ形式のファイルはテキスト差分を表示できません」を返す — **これは正常応答**で画面も位置指定 UI を出さないだけで正しく描けている。ところが `QueryCache.onError` が横断でグローバル toast を出す作りだったため、**画面が正しく処理している想定内の状態まで赤 toast** になっていた (「エラーが出ているのに画面は正常」= 最も混乱する状態)。**修正**: `reportQueryError(error, source)` が query/mutation の `meta` を読むようにし、`meta: { expectedErrors: true }` を宣言したものだけグローバル toast の対象外に (条件は「画面側で必ずその状態を表示していること」— 握りつぶしではない)。宣言したのは画面が確実に描き分けている 4 か所のみ (anchors / content-url / content-url?format / sheet)。それ以外は従来どおり toast し本当の障害は隠さない。**証拠**: 実ブラウザで Excel 成果物を開き `ERROR_TOASTS: 0` / 中身は表示されていることを実測 (.qa/gap-174)。web vitest 2 追加 (718 PASS)。**併せて報告 (未修正)**: Excel 成果物の上部に「HTML プレビュー」の空の白枠が出る (バイナリを HTML iframe に流しているため) — 別途対応が必要 | — | 2026-08-19 |
| ~~GAP-175~~ ✅解消 (2026-08-19) | 経営者の確認「API キーで LLM は動かさない状態にしているよね？？ 全てサブスクプランだよね？？」→ **答えは「いいえ」だった**。確定アーキテクチャ (全ユーザーが自分の PC・自分の Claude サブスクで実行) と**実装の既定値が正反対**で、`ATELIER_LLM_PROVIDER` 未設定だと relay を飛ばして `ANTHROPIC_API_KEY` (運営の従量課金) に落ちていた。つまり **Bridge が繋がっていない利用者の分だけ LLM 費用が黙って運営持ち**になる構造 (人数が増えるほど運営課金が増える)。`.env.example` も「chat/RAG に必須・本番にも必ず投入」と書いており API キーが既定の設計だった | ① `relay_mode_enabled()` を反転し **未設定 = relay (本人の PC の Bridge)** を既定に。`agent_sdk`/`api` は明示指定時のみ (テスト専用 `ATELIER_ALLOW_FAKE_LLM=1` の環境には Bridge が無いので relay を試さない) ② `api_billing_allowed()` を新設し、**`ATELIER_ALLOW_API_BILLING=1` が無い限り `ANTHROPIC_API_KEY` 経路を使わない**。チャット SSE (`use_api`) も同ゲート ③ 経路ゼロのエラーを `unconfigured` → **`bridge_offline`** に変更 (実態は利用者の PC 未接続なので、画面が GAP-168 の接続フローを出す) ④ `.env.example` / `SECRETS.md` を実態に合わせて全面改訂 ⑤ ナレッジ自動キュレーションは運営バッチなので従来どおり運営負担 (対象外)。**併せて実バグ 1 件**: `relay._session_factory()` が `lru_cache(maxsize=1)` で event loop を跨いで engine を使い回し、loop 再作成時に "attached to a different loop" となって**「Bridge 未接続」が「実行が失敗」に誤分類** (503 が 502 に) されていた → loop ごとに engine を分けた。**GAP-171 の但し書きを正直に訂正**: 昨日の実測は `ATELIER_LLM_PROVIDER=relay` をこちらで明示設定して行ったもので、「既定でサブスクになっている」ことは証明していなかった。回帰テスト 4 件 (既定 = relay / 明示分岐 / 課金ゲート / **キーがあっても Bridge 未接続なら課金せず断る**) (.qa/gap-175) | — | 2026-08-19 |
| ~~GAP-176~~ ✅解消 (2026-08-19) | 経営者指摘「iframe なんで？？」— Excel 成果物の画面に**空の白い枠**が出ていた (.qa/gap-174 のスクショに写り込んでいたもの)。成果物ビューアが `activeFormat === "html"` のとき**中身が何であっても iframe に URL を流し込む**作りで、Excel も PPTX も画像も全部「HTML プレビュー」の空 iframe になっていた | **contract-first で修正**: ① `openapi.yaml` の `ContentUrl` に `kind`/`file_name`/`mime` を追加 (`kind`: html/pdf/image/sheet/binary) し `sync-types.sh` で TS・Pydantic を再生成 ② `GET /outputs/{id}/content-url` が filedb 成果物の実体 MIME から `kind` を返す ③ 画面は `kind` で描き分け — **sheet (Excel/CSV)** は iframe を出さず「下の表で確認・編集できます」と案内 (実体の表は SheetEditor が描く) / **image** は `<img>` / **pdf** は iframe のまま (ブラウザ内蔵ビューアで読める・題に「（PDF）」) / **binary (PPTX/動画等)** は「この画面で表示できない形式です」+ 原本・DL 導線 / **html** は従来どおり ④ 形式タブと副題のラベルも種別に合わせ **Excel を「HTML」と呼ばない**ようにした。**実測** (実 API + 実ブラウザ): `CONTENT_URL_KIND: sheet` / `IFRAME_COUNT: 0` (修正前 1) / 案内表示あり / 表のセル 16 は従来どおり編集可 / エラー toast 0 (.qa/gap-176)。web vitest 5 追加 (コンテナ経由 = 実 API 応答の形で検証) | — | 2026-08-19 |
| ~~GAP-178~~ ✅解消 (2026-08-19) | 経営者指摘 ①「削除したわけではないよね？？ 一旦 API は使わない流れにしただけ。もしかするとこの場合も出てくる」②「env 消さないと使われる設計もおかしいですよね？？」— **両方そのとおり**。①API 経路は消していない (`AsyncAnthropic` / `AnthropicClient` / BYOK / 運営バッチ すべて健在、env 2 つで即復帰) が、②GAP-175 時点では**環境変数の「不在」に正しい挙動が依存**しており、昔の値が残っているだけで黙って支払者が変わる・打ち間違いが黙って経路を変える・今どの経路か env を読まないと分からない、を全部踏んでいた (実際「Mac の .env から消してください」という運用回避をお願いしてしまった = 設計の敗北) | ① **決定箇所を 1 つに集約** — `llm_route.resolve_llm_route()` で env を読むのはここだけにし、`relay_mode_enabled` / `api_billing_allowed` / `subscription_mode_enabled` を全部委譲 ② **怪しい設定は安全側 (本人サブスク) へ倒して必ず警告** — 未知の値は「綴りを確認」/ `=api` でも課金許可が無ければ運営課金せず本人サブスクへ / キーがあるのに使わないことを明言 / 肩代わり有効時は「運営に LLM 費用が発生します」と必ず警告 ③ **消し忘れが事故にならない** (昔の値が残っても支払者は変わらない) ④ **見える化** — 起動ログ 1 行 + **運営ダッシュボードの「プラットフォーム健全性」に「AI 実行経路 / 費用の出どころ」を常設**。⑤ API を使いたくなったら `ATELIER_LLM_PROVIDER=api` + `ATELIER_ALLOW_API_BILLING=1` で即復帰、「本人サブスク主体 + Bridge 未接続時だけ運営が肩代わり」も選べる。**検証は `ANTHROPIC_API_KEY` を設定した状態**で行い、それでも「本人サブスク・運営に費用は発生しません」と出ることを実測 (.qa/gap-178: 5 パターン + 実起動ログ + 実 /admin/health + 実ブラウザ)。回帰テスト 8 件 | — | 2026-08-19 |
