# Gap Tracker — design-audit で確定した未実装/未整備の正本

> CLAUDE.md ルール12 準拠: 「あとで」「placeholder」を口にした時点で登録する台帳。
> 各項目は解消 PR で本ファイルからチェックを付けて消し込む。黙って消さない。

## 機能 gap (バックエンド未実装のため UI から要素を撤去したもの)

| ID | 内容 | 現状 | 解消に必要なもの | 起票日 |
|---|---|---|---|---|
| ~~GAP-001~~ ✅解消 (2026-08-04) | S-E01 チャット添付 | **実装済**: ① POST /chat/attachments/upload-url — 署名付きアップロード URL (Supabase Storage 契約、bucket `chat-attachments/{thread_id}/{uuid}/{sanitized}`、MIME 画像/PDF/テキスト/MD/CSV/ZIP 限定 415・10MB 超 413・viewer 403・不可視スレッド 404・storage 未設定 503) ② SSE stream body の attachments (≤10 件) → user message の chat_messages.attachments jsonb (既存列) に永続。**他スレッド配下の storage_path は 422** (可視性バイパス拒否) ③ GET /chat/messages/{id}/attachments/{index}/url — RLS 可視メッセージの署名付き DL URL (範囲外/越境 404) ④ S-E01 コンポーザに「添付」復元 — 複数選択/送信前チップ (個別削除)/client 側事前検証/アップロード失敗時は送信中止/メッセージに添付チップ → 署名付き URL を新タブで開く。pytest 5 (503/415/413/403/404/422 越境/jsonb 永続/署名 DL) + vitest 3 + 実操作監査 .audit-e01-attach.mjs 3 連続 ALL PASS (実 PDF PUT → 実バイト保存 → DB 突合 → 署名 URL 実 GET バイト一致) | — | 2026-07-18 |
| ~~GAP-002~~ ✅解消 (2026-08-04) | S-E01 /コマンド | **実装済** (S-E01 のモック要素はこれで全て実装済): コマンド体系 3 種 — ① `/要約` = 会話の要点整理 (決定事項/未解決の論点/次のアクション) を依頼する実文面に置換して SSE 送信 (LLM 実行) ② `/決定 <内容>` = POST /chat/threads/{id}/commands (command=decision) — 実 decisions 行 (decided_by=対話相手 AI 社員、with_user=true) ③ `/タスク化 <タイトル>` = 同 API (command=task) — 実 tasks 行 (category=チャット起票、triage、見積未実施の暫定 1h を description に明示)。サーバーはコマンド原文 (user) + 実行結果 (system) をスレッド永続 + audit chat_command.executed。viewer 403 / 不可視 404 / 空白 args・未対応 command 422。UI はモックの「/コマンド」(zap) パレット (usage+説明) → 挿入 → 送信時解釈、未対応 /xxx は何も実行せず inline error。pytest 3 + vitest 3 + 実操作監査 .audit-e01-commands.mjs 3 連続 ALL PASS (実 decisions/tasks 行 + user/system 永続 + audit ×2 を DB 突合) | — | 2026-07-18 |
| ~~GAP-003~~ ✅解消 (2026-08-03) | 確定事項の「ピン留め」操作 (モック現物は S-E01 主力決定カードの .decision-pin-btn) | **実装済**: decisions.pinned 列 (migration t-d-99z、冪等) + PATCH /decisions/{id} {pinned} + 一覧 order by pinned desc + S-E01 主力決定カードのトグル UI (aria-pressed)。pytest 回帰 + 実操作監査 .audit-e01-pin.mjs 3 連続 ALL PASS (DB 突合往復) | — | 2026-07-18 |
| ~~GAP-004~~ ✅解消 (2026-08-03) | phases への担当 AI 社員割当 | **実装済**: phases.assigned_employee_ids uuid[] (migration t-d-99zb) + PATCH /workflow/phases/{id} (丸ごと置換、他 WS 社員は 422 で拒否) + S-F02 割当 UI (チップ/外す/追加 select、楽観更新) + S-F01 ヘッダーアバターは実割当優先 (未割当は従来集計フォールバック)。**実操作監査が実バグを検出**: 社員候補が無フィルタで他 WS の同名社員を混ぜていた → project→workspace フィルタ修正。pytest 2 + vitest 1 + 監査 3 連続 ALL PASS | — | 2026-07-18 |
| ~~GAP-005~~ ✅解消 (2026-08-03) | S-B02 の「未解決コメント」KPI | **実装済**: GET /comments/unresolved-count?project_id= (workflow_output/mock/task/acceptance_criteria の 4 種 target を project へ逆引きし open のみ集計、RLS 可視分のみ)。S-B02 KPI をモック本来の「未解決コメント」実カウントに差替え (確定/未確認は sub 表示へ)。pytest (解決/削除で減算・越境 0) + 実操作監査 .audit-b02-unresolved.mjs 3 連続 ALL PASS | — | 2026-07-18 |
| ~~GAP-006~~ ✅解消 (2026-08-03) | S-I01 の「依存グラフ」ビュー | **実装済**: tasks.dependencies は API 契約是正 (57f14f5) で公開済だったため描画側を新設 — 表示トグル「依存グラフ」(モック .seg 準拠) + 層別 DAG (層 = 前提からの最長距離、SVG ベジェ辺 + 矢印、ノードは S-I02 への実リンク、循環依存は層 0 フォールバック + バッジ明示)。vitest 4 件 + 実操作監査 .audit-i01-deps.mjs 3 連続 ALL PASS (シード 3 タスク・辺 3 本・詳細遷移) | — | 2026-07-18 |
| ~~GAP-007~~ ✅解消 (2026-08-01) | TopBar の通知ベル (通知センター) | **実装済**: T-UC-36 監査で TopBar ベル (未処理承認待ち件数バッジ + aria-label) → 通知センター /t-uc-36 実リンクを実装。通知の実体は approval_inbox (e2e 通しでプロデューサ欠落も是正済 — complete_task が挿入・approve/reject が resolve)。journey J-17 で「pending=1 がベルバッジ+通知センターに実表示」を 4 回の全 PASS 実走で確認 | — | 2026-07-18 |
| ~~GAP-008~~ ✅解消 (2026-08-03) | S-C02 の「活動履歴」タブ + 「最近の活動」カード | **実装済**: GET /ai-employees/{id}/activities — tasks(担当)/decisions(decided_by)/task_executions(担当タスク)/chat_threads(対話相手) の 4 源を union して新しい順 (RLS 可視分のみ、越境は社員自体 404)。S-C02 に活動履歴タブ (種別バッジ+件数、最近の活動カード)。pytest 4 件 (fixture は WS 自動シードと両立) + vitest + 実操作監査 .audit-c02-activities.mjs 3 連続 ALL PASS | — | 2026-07-19 |
| ~~GAP-009~~ ✅解消 (2026-08-04) | S-C02 の「画像アップロード」(アイコン画像) | **実装済** (S-C02 のモック要素はこれで全て実装済): ① POST /ai-employees/{id}/icon-upload-url — 署名付きアップロード URL (meetings と同じ Supabase Storage 契約、bucket `avatars/ai-employees/{id}/{uuid}/{sanitized}`、MIME png/jpeg/webp 限定 415・512KB 超 413・storage 未設定 503) ② PATCH icon=storage_path で確定 (icon 列は lucide 名 / storage path 両対応) ③ GET /ai-employees/{id}/icon-url — 署名付き閲覧 URL (path でないときは 409) ④ S-C02 に「画像アップロード」復元 — client 側事前検証 → 実 PUT → PATCH → ヘッダ/プレビューに `<img>` 実描画、「頭文字に戻す」で解除、503 は誠実表示。**実 Supabase Storage は env (ATELIER_SUPABASE_ADMIN_API_URL / SERVICE_ROLE_KEY) 設定のみで稼働** (dev 監査は storage スタブで実バイト往復)。pytest 8 + vitest 3 + 実操作監査 .audit-c02-icon.mjs 3 連続 ALL PASS (実 PNG PUT → DB 突合 → naturalWidth>0 実描画) | — | 2026-07-19 |
| ~~GAP-010~~ ✅解消 (2026-08-04) | S-K01 の「グラフ」ビュー | **実装済**: GET /knowledge/graph — ノード (RLS 可視・参照回数順最大 120、超過は truncated 明示) + 実エッジ (parent_id 階層 / タグ共起。**推測エッジなし**)。UI は SVG 円形レイアウト (決定的配置・外部グラフライブラリなし)、実線=階層/破線=タグ共起、scope 別配色+凡例、ノードクリック → 実 GET /knowledge/{id} → ノートビュー遷移。R-T08 越境 0 件は pytest で検証。pytest 2 + vitest 2 + 実操作監査 .audit-k01-graph.mjs 3 連続 ALL PASS (実 JWT API 突合 + SVG 実描画 + クリック遷移) | — | 2026-07-19 |
| ~~GAP-011~~ ✅解消 (2026-08-04) | S-K01 の「Obsidian で開く / Vault に書出」 | **実装済** (S-K01 のモック要素はこれで全て実装済): ① GET /knowledge/vault-export — RLS 可視分のみを Obsidian Vault 形式 zip (`<scope>/<category>/<title>.md` + YAML frontmatter、衝突は連番回避、X-Vault-Nodes) ② 「Obsidian Vault に書出」= 認証付き fetch → zip DL (書出中表示+失敗 inline error) ③ 「Obsidian で開く」= 選択ノートの obsidian://new?name+content 実 URI (未選択時 非描画)。pytest 2 (zip 構成/frontmatter + R-T08 越境 0 件) + vitest 2 + 実操作監査 .audit-k01-obsidian.mjs 3 連続 ALL PASS (実 DL → unzip → DB 突合、zip 件数 = DB 可視ノード数) | — | 2026-07-19 |
| ~~GAP-012~~ ✅解消 (2026-08-04) | S-K01 の「バックリンク」セクション | **実装済**: knowledge_references テーブル (migration t-d-99zc — RLS は knowledge_nodes の可視性をサブクエリ継承、insert/update も可視ナレッジ限定) + チャット SSE の RAG 消費時に (knowledge, chat_thread) を upsert 永続化 (再参照は reference_count++ に畳む) + GET /knowledge/{id}/references (referrer_title を chat_threads/tasks/decisions から実解決、RLS 不可視・削除済の参照元は除外。無題スレッドは「無題スレッド」fallback) + S-K01 バックリンク節 (件数見出し・種別ラベル・文脈・n 回、参照 0 は「まだ参照されていません」の誠実表示)。referrer_type は task/decision/feature も同型で受ける (書込元実装時に行が生まれる — 偽装挿入なし)。pytest 4 (実 SSE 経由の記録/dedupe/R-T08 越境 404) + vitest 2 + 実操作監査 .audit-k01-backlinks.mjs 3 連続 ALL PASS (実チャット送信→DB 突合→S-K01 実描画往復) | — | 2026-07-19 |
| ~~GAP-013~~ ✅解消 (2026-08-03) | S-O01 の「実行履歴」テーブル | **実装済**: cron_run_history テーブル (migration t-d-99za — RLS: platform 行は authenticated / project 行は member、INSERT policy 無しで API 改竄不可) + Inngest handler wrapper が全 cron 発火を running→success/error で記録 (履歴失敗でも cron を止めない best-effort、unit 3 件) + GET /cron-runs + S-O01 実行履歴テーブル (モック .history-card 準拠)。実操作監査 3 連続 ALL PASS。スケジュール別履歴は project cron 実行エンジン (T-F-20) 配線時に schedule_id/project_id 列でそのまま拡張可 | — | 2026-07-19 |
| ~~GAP-014~~ ✅解消 (2026-08-04) | S-O01 の「法令・運用バックエンド (必須)」セクション | **ジョブの実体から実装**: ① purge-deleted-accounts (毎日 0:00 JST) — T-A-05 が規定しながら不在だった退会 30 日後ハード削除 worker の実体 (所有 WS cascade → external_uploads → auth.users 物理削除、猶予中/現役は絶対に触らない、audit platform.account.purged) ② integrity-check (毎日 5:00 JST) — SQL のみ整合性チェック 5 種 (dangling 依存/AC/モック/工程-プロジェクト/工程担当越境)、検知時は WS owner の approval_inbox (integrity_alert、pending 重複なし) へ通知 (migration t-d-99ze) ③ GET /cron-platform-jobs (read-only 可視化 — 実 cron 式+次回+cron_run_history 実最終実行) ④ S-O01 に法令・運用節 (必須/無効化不可タグ、API 未到達時は節ごと非描画)。Inngest cron + 単独実行 (python -m src.services.platform_jobs) の 2 経路。pytest 7 + vitest 2 + 実操作監査 .audit-o01-legal.mjs 3 連続 ALL PASS | — | 2026-07-19 |
| ~~GAP-015~~ ✅解消 (2026-08-03) | S-M01 の解析結果構造化 (サマリー/話者分離/要件抽出/アクションアイテム) | **実装済**: transcription worker の後段に LLM 解析 (`services/meetings/analysis.py` — AnthropicClient / selected-stack v1、スキーマ正規化・コードフェンス許容・24k 文字切詰)。解析は additive (失敗しても transcription は成功のまま analysis_error 分類 → UI は誠実に「解析未実行」表示、偽の解析を出さない)。S-M01 にサマリー/話者/抽出要件/アクションアイテムのブロックを実装。unit 9 + vitest 2 + 実 UI 監査 3 連続 ALL PASS (storage スタブで署名 URL フロー E2E)。**実 LLM は ANTHROPIC_API_KEY 設定で稼働** | 残: 抽出要件→承認フロー連携 (「要件として承認」— GAP-031 側で管理) | 2026-07-19 |
| ~~GAP-017~~ ✅解消 (2026-08-03) | S-B03 の「プロジェクト跨ぎナレッジ参照」トグル | **実装済**: projects.settings.cross_project_knowledge (既定 true) を PATCH /projects/{id} + GET で read/write。false のとき /knowledge/search (project_id 付き) と chat RAG (SSE — thread の project_id を自動配線) が「自プロジェクト由来 + プロジェクト無所属 + 運営デフォルト」に限定され、他プロジェクトのナレッジを跨ぎ参照しない。S-B03 に実トグル (楽観更新+ロールバック、API 値未到達時は非描画)。pytest 2 (ON/OFF 往復・除外検証) + vitest 2 + 実操作監査 3 連続 ALL PASS (実 JWT 検索 API で除外/復帰を突合) | — | 2026-07-19 |
| GAP-023 | S-G01 の 成果物ビューア 未対応要素 | 未描画/静的: ① 編集(成果物編集 API 不在で死にボタン撤去) ② JSON/MD タブ・バージョン選択・DL は content-url が HTML のみのため静的クローム ③ 座標ピン(target_element_id)によるインライン注釈・AI 修正提案(承認/却下)・スレッド返信投稿はコメント高度機能 API 未対応。コメント status(open/resolved)/親子(parent)は v2 で配線済 | 成果物編集 API・JSON/MD 版の content-url・DL エンドポイント・target_element_id ピン UI・AI 修正提案フロー・返信投稿 UI | 2026-07-19 |
| GAP-022 | S-F02 の AI 提案フェーズ + F-IMP01 影響範囲解析 + 統計「F-IMP01 実行回数/日次整合性」 | 未描画 (フェーズ提案 API・影響範囲解析 API・per-phase タスク集計 API が不在。モックの承認/却下/影響グラフ/提案理由・タスク数「8/24」は実装せず、確定フェーズの状態遷移のみ実装) | フェーズ提案生成 API (承認→confirm/却下)・タスク移動の影響範囲解析 (depends_on 走査)・完了タスク影響時の自動リファクタ起票・phase 別タスク集計 API | 2026-07-19 |
| GAP-021 | S-A03 の「プラン」タブ + アイコン変更 | 未描画 (課金/プラン API・WS アイコン更新 API が不在。死にタブ/死にボタンで置かない) | 課金プラン API (Stripe 連携) + workspaces への icon 格納/更新 API | 2026-07-19 |
| GAP-020 | S-A01 の OAuth サインイン (GitHub / Google) | ボタン未描画 (OAuth プロバイダ連携 API が不在。死にボタンで置かない) | OAuth 認可フロー (プロバイダ登録・コールバック・アカウント連付け) の API + ボタン配線 | 2026-07-19 |
| GAP-019 | S-T01 運営ダッシュボードの未描画セクション | ミッションヒーロー (100社獲得)・トレンドグラフ・取得チャネル別・プラットフォーム健全性・ベータFB・運営コストは未描画 (実データ供給 API が無く、モックのダミー業務数値を虚偽表示しないため) | 事業 KPI (獲得目標/チャネル) の記録 API・ヘルスチェック集約・FB 収集・コスト集計の各 API + 描画 | 2026-07-19 |
| GAP-018 | S-N01 商談ドラフトの未実装機能群 | ① doc_type が提案/見積のみ (モックの業務委託契約/NDA/請求書タブは撤去) ② PDF 出力・メール送信・送信履歴は API 不在のため撤去 ③ 参照ナレッジ/生成トレース表示は API 不在のため汎用手順表示に置換 ④ ドラフト生成は入力の構造化保存 (AI 文面生成はチャット/スキル側の責務のまま) | ① sales-docs doc_type enum 拡張 (contract/nda/invoice) ② PDF レンダリング + メール送信 API + 送信履歴テーブル ③ 生成トレース (参照ナレッジ ID 群) の記録と read API ④ AI 生成エンドポイント (ナレッジ RAG + テンプレ) | 2026-07-19 |
| GAP-024 | S-H01 モックビューアの「編集」ボタン + バージョンパネル「…」メニュー | 未描画 (モック HTML を編集する API が不在 — PATCH /mocks/{id} は storage_path/meta_tags の差替のみでエディタ体験を構成できない。バージョン一覧の「…」メニューも対応操作 API 不在)。バージョン履歴/コメント/修正依頼/一覧ピッカーは v2 で実 API 配線済 | モック HTML 編集 (エディタ or 再アップロード) フロー + バージョン操作 (複製/破棄) API + ボタン配線 | 2026-08-01 |
| ~~GAP-025~~ ✅解消 (2026-08-04) | S-I02 タスク詳細の未描画要素群 | **全 4 項目実装済** (migration t-d-99zi: tasks.verifier_employee_id + task_execution_tests): ① 「あなたへの確認」仕様変更 3 択 — 実検知 = 紐づくモックと同一画面の新バージョンが後からアップロードされた状態のみ (GET /tasks/{id}/spec-changes、推測イベント無し)。POST .../resolve の 3 択 (2 段階確認): adopt=mock_id を最新へ差替 / split=実フォロータスク起票 (仕様変更フォロー・triage・見積未実施 1h 明示) / discard=blocked+dispatch 解除 (再分解待ち)。解決は metadata に記録し再表示なし、audit task.spec_change.* ② テスト結果タブ — task_execution_tests (Bridge complete の metadata.tests が記録) + GET /executions/{id}/tests (RLS member select のみ、未記録は誠実表示) ③ 関連資料タブ — GET /tasks/{id}/related (mock 実バージョン/spec/受入条件 実項目数/ブランチ+変更ファイル/参照ナレッジ = knowledge_references referrer_type='task'。**実リンクのみ**) ④ メタ行 — 検証担当 (verifier_employee_id、PATCH 設定・他 WS 422・未設定「未割当」)/見積・経過 (実 executions duration 合計 — TaskExecution.duration_seconds 追加)/変更ファイル数 (files_changed を complete が列永続、0 は「記録なし」)。pytest 6 + 回帰 62 passed + vitest 4 (11 passed) + 実操作監査 .audit-i02-detail.mjs 3 連続 ALL PASS (14 チェック — adopt/split/discard・tests rows・related 実リンク・メタを DB 突合) | — | 2026-08-01 |
| ~~GAP-026~~ ✅解消 (2026-08-04) | S-I03 実行モニターの未描画要素群 | **全 6 項目実装済** (migration t-d-99zh: dispatch_control + bridge_workers + tasks.dispatch_promoted_at): ① Bridge 接続バッジ — Bridge アプリ (apps/bridge) が毎 poll POST /bridge/ping (BridgeAuth) で presence を upsert、/bridge/status の workers (90 秒以内=接続中) から「接続中 · Bridge vX · host」/「Bridge 未接続」を誠実表示 ② 同時実行枠 = 実 parallel_limit/available_slots、すべて一時停止 = POST /dispatch/pause|resume (paused 中は実 /kanban/pick が no_available_task — 実行中は継続)、順番待ちから 1 件追加 = POST /dispatch/promote (最古 queued を dispatch_promoted_at 昇格 → pick 最優先。Bridge がローカル起動する構造のため API は spawn しない誠実文言) ③ キュー取消 = POST /tasks/{id}/dispatch-cancel (queued 限定 409、ready へ) ④ セッション停止 = POST /tasks/{id}/dispatch-stop (2 段階確認 — reclaimed+blocked+execution cancelled) ⑤ 表示方法 カード/一覧/ログ集約 — 集約 = GET /executions-events (実 task_executions 由来の開始/終了イベント、推測ログなし) ⑥ 経過 = 実 started_at の実測 tick、残りは「見積比 残り N 分 / 見積超過 +N 分」(estimated_hours 由来と明示)。audit dispatch.paused/resumed/promoted/cancelled/stopped、R-T08 越境 404。pytest 6 + dispatcher 回帰 12 + bridge 24 + vitest 5 + 実操作監査 .audit-i03-dispatch-ops.mjs 3 連続 ALL PASS (**監査が実プロジェクトタスクへの誤停止を検出し card 内 locator に是正 → 原状復帰済**) | — | 2026-08-01 |
| ~~GAP-027~~ ✅全解消 (②2026-08-04) | S-L01 クライアント招待の「再送」ボタン + 「使用回数」列 | ① **再送は実装済** (2026-08-03): POST /client-invitations/{id}/resend — token は hash 保存のため「再送 = 新 token へローテーション (旧リンク失効) + 新リンクをメール送付 + 新 raw token を発行バナーで 1 度だけ表示」。pending のみ対象 (使用済/失効/期限切れは 409)、2 段階確認 UI、audit `client_invitation.resend`。pytest + vitest + 実操作監査 3 連続 ALL PASS ② **使用回数も実装済** (2026-08-04): client_invitations.use_count (migration t-d-99zd — backfill は実 audit client.signin の集計から) をポータルサインイン成功ごとに増分 (失敗は増えない)。ClientInvitation 応答に use_count、S-L01 の「使用日」列をモック原文どおり「使用回数」(N 回) に復元。pytest 2 + vitest + 実操作監査 .audit-l01-usecount.mjs 3 連続 ALL PASS (S-L02 実サインイン ×2 → use_count=2 → UI「2 回」) | — | 2026-08-01 |
| ~~GAP-028~~ ✅解消 (2026-08-04) | S-L02 クライアントサインインの招待プレビュー + 同意永続 | **両方実装済**: ① POST /client/auth/preview — 招待トークンの署名前プレビュー (メタ限定: 招待元 workspace 名・オーナー表示名 (未設定は null — 推測で埋めない)・プロジェクト名・招待先メール・expires_at・remaining_days のみ。内部 ID/scopes は返さない。10/min/ip レート制限、トークンは body で受け URL に載せない、read-only — use_count/used_at/同意は不変)。S-L02 は URL トークン有効時にモック忠実の greeting-card (招待元行+project-card)・実「残り N 日」有効期限バー・招待先メール (disabled) を実描画、無効/期限切れは誠実なエラーバナーのみでプレビューカード非描画 ② 同意 2 種のサーバー永続 — /client/auth/signin が agree_legal+agree_confidential 必須 (欠落 422、JWT 発行なし)、client_invitations.legal_consented_at/confidential_consented_at (migration t-d-99zg) に初回同意時刻を記録し再サインインで上書きしない (法務証跡)、audit client.signin に consents。pytest 6 (preview メタ限定/read-only/401/410/422/同意不変) + vitest 5 + 実操作監査 .audit-l02-preview.mjs 3 連続 ALL PASS (実 UI 通し + DB 突合) | — | 2026-08-01 |
| GAP-029 | S-L03 クライアントポータルのコンテンツ未提供 | アクセス範囲カード等で honest 表示: client-portal API は GET /client/projects/{id} (id/name/description/scopes/表示名) のみで、モックの成果物一覧・フェーズ進捗バー・モックギャラリー・コメント一覧・リンク残日数を出す client スコープ read API が無い (staff API は R-T08 で別 JWT 系統のため使えない) | client JWT で読める project 限定の outputs/mocks/comments/phases read API (+コメント投稿) と招待 expires_at の返却 | 2026-08-01 |
| GAP-031 | **Gate #6 実照合で検出した記録漏れモック要素群** (CI 化 (GAP-102) の初回実走で検出 — 画面監査時に判断はされたが正確な記録が無かった、または見落とされた要素) | 未描画/未記録: ① S-E01 チャット — **「役立った」は実装済 (2026-08-03)**: 実は POST /chat/messages/{id}/feedback (T-A-19) が既存で UI 未配線だっただけ。アクション行 (役立った/コピー) を実装し audit_logs 突合で 3 連続 PASS (.audit-e01-feedback.mjs)。**「分岐」も実装済 (2026-08-04)**: POST /chat/messages/{id}/branch — 分岐点までの履歴を新スレッド「分岐: …」へコピー (parent_message_id 連鎖 + 先頭は分岐元への系譜ポインタ、元スレッド不変、audit chat_thread.branch、R-T08 越境 404)。**監査が実バグ 2 件を検出・修正**: 同一トランザクション内 user/assistant の created_at 同値 (now()=transaction timestamp) によるタプル境界取り漏れ + スレッド表示順の曖昧さ → SSE insert を clock_timestamp() 明示に是正。pytest 2 + vitest 3 + 実操作監査 .audit-e01-branch.mjs 3 連続 ALL PASS。**「承認して実行」も実装済 (2026-08-04)**: 書込ツールの人間承認ゲート — save_deliverable (APPROVAL_REQUIRED_TOOLS) は SSE agentic ループで即実行せず approval_inbox (type=tool_execution, target_type=chat_thread — migration t-d-99zf) に pending 起票し、LLM には「実行済みの体で回答しない」旨の tool_result を返す (無承認自動実行の廃止)。POST /chat/tool-approvals/{id}/execute (pending 検証 → execute_atelier_tool 実実行 → approved+resolution_note+tool メッセージ、二重実行 409) / POST .../reject (rejected + 差戻 system メッセージ、実行されない) / GET /chat/tool-approvals。S-E01 スレッド末尾に承認カード (title+tool_input 表示、承認して実行 / 差戻 / Inbox で確認 = /approvals 実リンク)、承認後は実行結果を実描画。audit chat_tool.approval_requested/executed/rejected。pytest 3 + vitest 3 + 実操作監査 .audit-e01-tool-approval.mjs 3 連続 ALL PASS (承認 → 実 knowledge 生成 DB 突合 / 差戻 → 生成されない)。**GAP-031 の未解消は ⑤ (S-T03 — T-A-42 契約変更が前提) のみ** ② **S-F01 工程直接操作は実装済 (2026-08-04)**: 「この工程を完了として承認」(completed のみ — 自動開始しない)/「次工程（X）を開始」(進行中がある間 disabled、承認後 enabled)/「前工程の成果物を見る」(前工程選択+成果物タブ実切替)。vitest 3 + 実操作監査 .audit-f01-i03-ops.mjs 3 連続 ALL PASS ③ **S-I03 並び替えは実装済 (2026-08-04)**: 「要対応が上」(既定・区分表示)/「新しい順」(結合グリッド updated_at 降順)/「進捗順」(最新実行スコア降順)。vitest 2 + 同監査 3 連続 ALL PASS ④ **S-T02 再取込は実装済 (2026-08-04)**: POST /admin/skills/reimport — ATELIER_SKILLS_DIR (既定 ~/.claude/skills) を走査し SKILL.md frontmatter を解析して name キーで upsert (新規/更新/変更なしの summary、is_active 等の運用設定は上書きしない、dir 不在 409、audit skill.reimport)。2 段階確認 UI + 結果バナー。pytest 3 + vitest + 実操作監査 .audit-t02-t06-import.mjs 3 連続 ALL PASS ⑤ S-T03 の「カテゴリ追加」「スキル追加」「保存して全 WS 反映」(T-A-42 read-only 契約によりテンプレ編集 API 自体が不在) ⑥ **S-T04 サポート連絡は実装済 (2026-08-04)**: POST /admin/support-contact (admin gate — 既存 ResendSender で送信、メール未設定環境は dry_run=true を応答で明示、audit support.contact 記録、退会予約中ユーザーも対象) + GET /admin/support-contacts (「最近のサポート対応」= 実 audit 逆引き)。行ボタン → 件名/本文ダイアログ → 結果バナー。pytest 3 + vitest 2 + 実操作監査 .audit-t04-support.mjs 3 連続 ALL PASS ⑦ **S-T06 SKILL.md 取込は実装済 (2026-08-04)**: .md 選択 → frontmatter (name/description) をクライアント側で解析 → 追加ダイアログへプレフィル → 人間確認後に既存 POST /admin/knowledge で platform 登録 (無確認書込なし)。vitest 3 + 同監査 3 連続 ALL PASS | 各 API/フローの起票実装 (フィードバック記録・tool 承認・工程直接操作・ソート状態・skills 再取込・テンプレ編集・サポート導線・SKILL.md パーサ取込) | 2026-08-03 |
| ~~GAP-030~~ ✅解消 (2026-08-03) | **Bridge 実行プロンプトにタスク内容/仕様が含まれない** | **是正済**: ① /kanban/pick 応答に task_title/task_description/assigned_employee を追加 (OpenAPI + 型再生成) ② dispatcher の既定プロンプトをタスク内容入り + 「参照不能な情報を探しに行かない」明示に刷新 (buildDefaultPrompt) ③ ラッパー無しのデフォルト経路で e2e 通し 19/19 PASS を実証 (旧: ID のみで子 Claude が長考→240s タイムアウト) ④ pty.ts スタブと「未実装を確認するテスト」を撤去 ⑤ bridge + 6 packages の `lint: echo placeholder` を実 ESLint (.eslintrc.workspace.json) に配線 | — | 2026-08-01 |
| ~~GAP-016~~ ✅解消 (2026-08-03) | **議事録パイプラインの Whisper worker が全リポジトリに不在** | **消費者を実装**: `apps/api/src/services/meetings/worker.py` (queue 消費 → storage 署名 DL → OpenAI Whisper API → 結果 JSON を `transcripts/results/{id}.json` へ UL → parse_result_path 差替+parsed_at 打刻 → audit `meeting.transcribe.complete`。失敗時は parse_error 記録+audit、force 再キューで再試行)。起動経路 2 つ: Inngest cron `transcribe-queue` (毎分) + 単独プロセス `python -m src.services.meetings.worker`。unit 11 本 (orchestration/HTTP 境界/queue query/loop) で検証。**実 Whisper 呼出は ATELIER_OPENAI_API_KEY + storage 設定が前提** (dev 未設定 — 本番投入時に env 設定のみで稼働) | — (GAP-015 構造化解析は別 gap のまま) | 2026-07-19 |

## プロセス gap (計画・ゲートの構造欠陥)

| ID | 内容 | 現状 | 解消に必要なもの | 起票日 |
|---|---|---|---|---|
| ~~GAP-101~~ ✅解消 (2026-07-19) | QA 仕様書の欠落 13 画面 | **全 13 枚作成完了** (S-B03/B04/N01/PUB01〜04/T01〜06)。completion_gate.sh が STATUS: 完了 (画面台帳一致・planned=0・FAIL=0・全 547 TC) を返すことを確認 | — | 2026-07-18 |
| ~~GAP-102~~ ✅解消 (2026-08-03) | CI Gate #6 (mock-impl diff) がスタブ | **実照合を実装** (`scripts/ci/mock-impl-diff.py` — v3-gate.yml から呼出): [A] screens.json ∪ モック data-bf-screen-id ∪ QA 仕様書の 3-way 台帳突合 (35 画面一致) [B] 全モック `<button>` ラベル 153 個を「実装ソース or QA 仕様書 or gap-tracker」と照合し未説明 0 を enforce。初回実走で記録漏れ 28 要素を検出 → 仕様書アンカー転記 + GAP-031 起票で説明責任を完備 | — | 2026-07-18 |
| ~~GAP-103~~ ✅解消 (2026-08-03) | tickets.json の UI タスク AC がテンプレコピペ | **機械転写を実装** (`scripts/qa/transcribe-ui-acs.py`、冪等・再実行可能): UI/frontend 39 タスクの tier_2 を screens.json の fields/actions/states/transitions (1 要素 = 1 AC) + features.json の happy_path/api_endpoints (同一画面サブタスクの差別化) + タスクスコープ UBIQUITOUS + UNWANTED ガードで再生成。39/39 で tier_2 が全て相異 (旧: 34 タスクが一字一句同一)。validate.sh PASS (216/216) 維持 | — | 2026-07-18 |
| ~~GAP-104~~ ✅解消 (2026-08-01) | 通し (e2e-journey-walkthrough) が一度も実行されていない | **実施完了**: 19 行ジャーニー (登録→WS→プロジェクト→チャット→ナレッジ→見積→タスク→Bridge 実実行 (実 claude -p)→承認/差戻→メンバー招待/コメント→クライアント招待→ポータル→R-T08→通知/検索→最終成果+GAP-016 確認) を実 UI で **連続 3 回 19/19 全 PASS** (workbook: `.qa/e2e-journey/journey-20260801.xlsx`, 証跡: `evidence-20260801/`)。通しで製品バグ 2 件を検出・修正 (play→pick パイプ断絶 / approval_inbox プロデューサ不在, commit 57f14f5) + GAP-030 起票 — 「通しを後回しにするほどこの型の欠陥は長生きする」の実証 | — | 2026-07-19 |
