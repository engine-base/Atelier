# T-A-48 — 受入基準

F-CTX01 完全実装：本物ベクトルRAG + 直近+要約メモリ + プロジェクト状態 + ペルソナ + スキル注入 + stub排除

## Tier 1 構造
- UBIQUITOUS: F-CTX01 文脈は ペルソナ + 装着スキル(content_md) + 直近メッセージ + ローリング要約 + プロジェクト状態 + ナレッジRAG(本物ベクトル) を含む
- UBIQUITOUS: ナレッジ参照は ilike キーワードではなく knowledge service の Voyage/pgvector cosine を呼ぶ

## Tier 2 機能
- [EVENT-DRIVEN] When a chat message is sent, the system shall build context via cosine knowledge search (not ilike) and inject attached_skills content_md
- [EVENT-DRIVEN] When the thread exceeds the recent window, the system shall fold older messages into a rolling summary (context_summary)
- [UNWANTED] If the user is not a member of the thread's workspace, the system shall return 403 (RLS 越境=0) （致命）
- [UNWANTED] If the real LLM is unavailable, the system shall surface an error and shall NOT silently return a fake/stub stream （致命）

## Tier 3 回帰
- 既存のチャット送信/SSE/分岐が従来どおり動作する
- RAG OFF 指定時は従来どおり履歴のみで応答する

## 関連

- 画面: S-E01 / エンティティ: E-011, E-018, E-007 / 機能: F-008, F-024
- CI 13 gate すべて PASS を完了条件とする。
