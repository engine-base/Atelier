{
  "name": "@atelier/bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Atelier Bridge — Vibeyard fork (Electron + node-pty + xterm.js) による Claude Code マルチセッション dispatcher",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo 'placeholder — Vibeyard fork 取込後に electron-forge start に置き換え' && exit 0",
    "lint": "echo 'placeholder' && exit 0",
    "type-check": "tsc --noEmit",
    "test": "echo 'placeholder — T-F-24 で vitest に置き換え' && exit 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@atelier/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
