# 死にクリック総当たりスイープ — 2026-07-18 (S-B01 / S-B02 / S-F01 / S-I01 / S-E01)

> 目的: 監査完了済み 5 画面の**全インタラクティブ要素**
> (`button` / `a[href]` / `[role=tab]` / `input[type=checkbox]` の :visible 全件) を
> Playwright で実クリックし、観測可能な効果 (URL 遷移 / DOM 変化 / :8000 への API 発火 /
> aria 状態変化 / 新規タブ) が 1 つも無い「死に要素」を検出する。
> スクリプト: `apps/web/.audit-sweep.mjs` (アプリ生存ガード + 全ルート warm-up 付き)。
> 生ログ: scratchpad/sweep.json (139 要素)。

## 結果サマリー

| 画面 | 掃引要素数 | 実効果あり | 死に要素 (検出→是正) |
|---|---|---|---|
| S-B01 プロジェクト一覧 | 24 | 24 | 0 (※ワークスペースピッカーは全画面共通 → 下記) |
| S-B02 プロジェクトダッシュボード | 28 | 28 | 0 |
| S-F01 工程ワークフロー | 38 | 38 | 0 |
| S-I01 タスクボード | 22 | 22 | 0 |
| S-E01 チャット | 27 | 27 | 0 |

効果の内訳: 遷移 77 / DOM 変化 24 / aria 状態変化 11 / disabled(仕様) 4 / その他(説明済) 23。

## 検出した死に要素と是正 (Rule 10)

| 要素 | 出現画面 | 検出時の状態 | 是正 |
|---|---|---|---|
| TopBar ワークスペースピッカー pill | 全画面 | `onClick` の無い button (死にボタン) | **実装**: 実 `GET /workspaces` 一覧の listbox ドロップダウン化。現在 WS に aria-selected、選択は T-UC-38 と同一キー `atelier_current_workspace` に永続。ハンドラ未配線時は button を出さず非インタラクティブ表示に（構造的再発防止） |
| TopBar 通知ベル | 全画面 | `onClick` の無い button。通知 API がバックエンドに存在しない | **撤去 + GAP-007 起票** (docs/gap-tracker.md) |
| TopBar アバター「Q」 | 全画面 | ハードコードの偽イニシャル | **実装**: `GET /me` の実プロフィール (display_name) からイニシャル表示、`サインイン中: <名前>` ラベル付与 |

## 「効果なし」判定だが死にではないと確認した要素 (全件説明責任)

| 要素 | 画面 | 説明 |
|---|---|---|
| 現在ページの nav 自己リンク (プロジェクト / ダッシュボード / 工程 / タスク) | 各画面 | 同一 URL への Link。無遷移は仕様 |
| 選択中タブ / トグルの再クリック (確定事項タブ・かんばん・機能別) | S-F01 / S-I01 | aria-selected / aria-pressed が既に true。状態不変は仕様 |
| シークレット / 設定 nav リンク | S-B01 | 個別再検証で `/projects/vault` / `/projects/settings` へ遷移 + 見出し描画を確認 (dev 初回コンパイル遅延がスイープの観測窓 1.2s を超えただけ) |
| S-F01 進行中チャット リンク | S-F01 | 個別再検証で `/chat?project=…` へ遷移確認 (同上のコンパイル遅延) |
| メインコンテンツへスキップ | 全画面 | sr-only スキップリンク。キーボードフォーカス時のみ可視 (a11y 仕様)。マウスクリック不可は正常 |
| モーダル背後の要素 (S-B01 タブ・カード類 6 件) | S-B01 | 直前の「新規プロジェクト」クリックで開いたモーダルに覆われたため。要素自体は S-B01 監査ラウンドで実操作 PASS 済 |
| Open Next.js Dev Tools | dev のみ | Next.js dev オーバーレイ。アプリのコードではない |

## disabled(仕様) の 4 件

| 要素 | 画面 | 条件 |
|---|---|---|
| ページング 戻る / 次へ | S-B01 | カーソルが無い (1 ページのみ) 間は disabled |
| スレッドを作成 | S-E01 | タイトル未入力の間は disabled |
| 送信 | S-E01 | 本文未入力の間は disabled |

## スイープ起因の追加是正

- S-B01 新規プロジェクトモーダル / S-I01 タスク追加モーダル: **Escape で閉じられなかった**
  (スイープ中のモーダル残留で発覚) → Escape ハンドラ追加。S-I01 は背面クリック close も追加。

## 再実行手順

```bash
# stack (postgres / uvicorn :8000 / next dev :3100) 起動 + scratchpad/token.txt 更新後:
cd apps/web && node .audit-sweep.mjs
# → total=N dead=0 (自己リンク・dev ツール等の説明済み no-op を除く) を確認
```
