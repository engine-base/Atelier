# SO01 自動スケジュール(cron) 画面別テスト仕様（S-O01・human-grade-qa test-plan）

> 種別: list / 結果列は空=planned（実機実行は環境要因で BLOCKED）。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SO01-001 | 自動スケジュール(cron) | 画面表示 | 画面が正常表示（全 UI 要素・モック準拠） | - | 1. 自動スケジュール(cron) を開く | モックの全要素（見出し/ボタン/一覧/フォーム）が表示される | PASS |  |
| SO01-002 | 自動スケジュール(cron) | 視覚忠実度 | 配色/角丸/タイポが design token 準拠 | - | 1. 画面を目視 | --color-* / --rounded-* / --text-* トークンで描画（ハードコード色なし） | | 段1: token値=DESIGN-atelier.md 一致(済) |
| SO01-003 | 自動スケジュール(cron) | データ取得 | loading skeleton 表示（スケジュール一覧） | 取得未完 | 1. 自動スケジュール(cron) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | |  |
| SO01-004 | 自動スケジュール(cron) | 空データ | 0 件時の空状態（スケジュール一覧） | GET /cron-schedules?project_id が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | | セクションH |
| SO01-005 | 自動スケジュール(cron) | エラー | 取得失敗の inline error + toast（スケジュール一覧） | GET /cron-schedules?project_id が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | | UNWANTED critical |
| SO01-006 | 自動スケジュール(cron) | 権限 | 403 拒否表示（スケジュール一覧） | 権限なしユーザ | 1. 権限なしで 自動スケジュール(cron) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | | セクションG 越境 |
| SO01-007 | 自動スケジュール(cron) | インタラクション | 「有効/無効トグル」を操作すると動作する（死にボタン検査） | - | 1. 「有効/無効トグル」をクリック | PATCH enabled(楽観トグル)（onClick/href/API が実在し、押すと副作用が起きる） | PASS | 死にボタン |
| SO01-008 | 自動スケジュール(cron) | 楽観更新 | 有効トグル: 成功で即時反映 | 対象が存在 | 1. 有効トグル を実行 | PATCH /cron-schedules/{id} 呼び出し前にUIへ楽観反映→成功で確定（別GET再取得で反映） | PASS | EVENT-DRIVEN |
| SO01-009 | 自動スケジュール(cron) | 楽観更新 | 有効トグル: 失敗でロールバック | サーバ 4xx/5xx | 1. 有効トグル を実行しAPIが失敗 | 楽観反映が元に戻り、inline error + toast を表示 | | EVENT-DRIVEN/rollback |
| SO01-010 | 自動スケジュール(cron) | エラー | 有効トグル: API 失敗時 inline error + toast | 接続失敗/レート | 1. 有効トグル を失敗させる | role=alert + toast、状態は不整合にならない | | UNWANTED critical |
| SO01-011 | 自動スケジュール(cron) | 受容差分 | 即時実行ボタンは非表示 | - | 1. 一覧確認 | run-now API 無しのため列非表示(honest) | | API欠如 |
| SO01-012 | 自動スケジュール(cron) | a11y | axe scan で 0 critical/serious | - | 1. axe を実行 | critical/serious 違反 0（semantic role/aria） | PASS | vitest-axe 実測済(該当時) |
| SO01-013 | 自動スケジュール(cron) | レスポンシブ | 320/768/1024/1440 で崩れない | - | 1. 各幅で開く | 横スクロール/はみ出し/縦折れが無い | PASS | Playwright responsive.e2e 26画面×4幅 実走 第8軸 |
| SO01-014 | 自動スケジュール(cron) | 状態永続 | F5 リロードでログアウトに飛ばない | ログイン済 | 1. 画面で F5 | 保護ガードで維持・状態復元（URL クエリ等） | | セクションG G4 |
| SO01-015 | 自動スケジュール(cron) | 到達性 | ナビ/リンクからUIで到達でき遷移先が正しい | - | 1. 導線から本画面へ到達→主要リンクを辿る | 死リンク無し・遷移先が正しい文脈(param)で開く | PASS | I4-S（全体で死リンク0確認済） |
