# SI02 タスク詳細(6タブ) 画面別テスト仕様（S-I02・human-grade-qa test-plan）

> 種別: detail / 結果列は空=planned（実機実行は環境要因で BLOCKED）。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SI02-001 | タスク詳細(6タブ) | 画面表示 | 画面が正常表示（全 UI 要素・モック準拠） | - | 1. タスク詳細(6タブ) を開く | モックの全要素（見出し/ボタン/一覧/フォーム）が表示される | PASS |  |
| SI02-002 | タスク詳細(6タブ) | 視覚忠実度 | 配色/角丸/タイポが design token 準拠 | - | 1. 画面を目視 | --color-* / --rounded-* / --text-* トークンで描画（ハードコード色なし） | PASS | 段1: token値=DESIGN-atelier.md 一致(済) |
| SI02-003 | タスク詳細(6タブ) | データ取得 | loading skeleton 表示（詳細） | 取得未完 | 1. タスク詳細(6タブ) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SI02-004 | タスク詳細(6タブ) | 空データ | 0 件時の空状態（詳細） | GET /tasks/{id} が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SI02-005 | タスク詳細(6タブ) | エラー | 取得失敗の inline error + toast（詳細） | GET /tasks/{id} が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SI02-006 | タスク詳細(6タブ) | 権限 | 403 拒否表示（詳細） | 権限なしユーザ | 1. 権限なしで タスク詳細(6タブ) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SI02-007 | タスク詳細(6タブ) | データ取得 | loading skeleton 表示（受入条件） | 取得未完 | 1. タスク詳細(6タブ) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SI02-008 | タスク詳細(6タブ) | 空データ | 0 件時の空状態（受入条件） | GET /tasks/{id}/acceptance-criteria が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SI02-009 | タスク詳細(6タブ) | エラー | 取得失敗の inline error + toast（受入条件） | GET /tasks/{id}/acceptance-criteria が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SI02-010 | タスク詳細(6タブ) | 権限 | 403 拒否表示（受入条件） | 権限なしユーザ | 1. 権限なしで タスク詳細(6タブ) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SI02-011 | タスク詳細(6タブ) | データ取得 | loading skeleton 表示（実行履歴） | 取得未完 | 1. タスク詳細(6タブ) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SI02-012 | タスク詳細(6タブ) | 空データ | 0 件時の空状態（実行履歴） | GET /tasks/{id}/executions が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SI02-013 | タスク詳細(6タブ) | エラー | 取得失敗の inline error + toast（実行履歴） | GET /tasks/{id}/executions が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SI02-014 | タスク詳細(6タブ) | 権限 | 403 拒否表示（実行履歴） | 権限なしユーザ | 1. 権限なしで タスク詳細(6タブ) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SI02-015 | タスク詳細(6タブ) | データ取得 | loading skeleton 表示（コメント） | 取得未完 | 1. タスク詳細(6タブ) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SI02-016 | タスク詳細(6タブ) | 空データ | 0 件時の空状態（コメント） | GET /comments?target=task が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SI02-017 | タスク詳細(6タブ) | エラー | 取得失敗の inline error + toast（コメント） | GET /comments?target=task が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SI02-018 | タスク詳細(6タブ) | 権限 | 403 拒否表示（コメント） | 権限なしユーザ | 1. 権限なしで タスク詳細(6タブ) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SI02-019 | タスク詳細(6タブ) | インタラクション | 「概要/仕様/入出力/実行履歴/添付/コメント タブ」を操作すると動作する（死にボタン検査） | - | 1. 「概要/仕様/入出力/実行履歴/添付/コメント タブ」を各クリック | 対応タブ内容を表示(aria-selected/controls)（onClick/href/API が実在し、押すと副作用が起きる） | PASS | 死にボタン |
| SI02-020 | タスク詳細(6タブ) | 受容差分 | 入出力/添付は情報なし表示 | - | 1. 各タブ | 単一の裏付けAPI無しのため『情報なし』(honest) | PASS | API欠如 |
| SI02-021 | タスク詳細(6タブ) | a11y | axe scan で 0 critical/serious | - | 1. axe を実行 | critical/serious 違反 0（semantic role/aria） | PASS | vitest-axe 実測済(該当時) |
| SI02-022 | タスク詳細(6タブ) | レスポンシブ | 320/768/1024/1440 で崩れない | - | 1. 各幅で開く | 横スクロール/はみ出し/縦折れが無い | PASS | Playwright responsive.e2e 26画面×4幅 実走 第8軸 |
| SI02-023 | タスク詳細(6タブ) | 状態永続 | F5 リロードでログアウトに飛ばない | ログイン済 | 1. 画面で F5 | 保護ガードで維持・状態復元（URL クエリ等） | PASS | セクションG G4 |
| SI02-024 | タスク詳細(6タブ) | 到達性 | ナビ/リンクからUIで到達でき遷移先が正しい | - | 1. 導線から本画面へ到達→主要リンクを辿る | 死リンク無し・遷移先が正しい文脈(param)で開く | PASS | I4-S（全体で死リンク0確認済） |
| SI02-307 | タスク詳細(6タブ) | 表示 | 書き込みが「誰のものか」名前で分かる | 社内メンバー 1 人 + クライアント窓口 1 人が同じ対象にコメント | 1. 両方からコメントする 2. 社内の画面で一覧を見る | **それぞれの表示名**が出る。「クライアント（招待）」「メンバー 8f3bbf48」のような **種別だけ・UUID の断片**にならない。クライアント発か社内発かも見分けられる | BLOCKED: 前提「クライアント窓口が同じタスクにコメント」が仕様上成立しない — POST /client/projects/{id}/comments は target_type ^(workflow_output|mock)$ で task は 422 / 解除条件: 正本の前提見直し (タスクはクライアントに見せない前提なら社内のみの行に直す) かタスクへのクライアントコメント機能の追加。社内メンバー分は S-I02 コメント欄に「QA成果物メンバー甲」と表示名で出ることを確認 (shots/SI02-307.png) | ai_required=no GAP-226

## design-audit v2（2026-08-01 実機再監査・全面再構築）

> 監査スクリプト: `apps/web/.audit-i02.mjs`（再実行可能・API/UI/DB 突合・3 連続 PASS 確認）。
> vitest: `tests/bundle-i/uc15-task-detail.test.tsx`（7 件）+ `tests/bundle-g/tasks-and-approvals.test.tsx`。
> モック基準: `06_mockups/task/S-I02-detail.html`（1440/768/390 撮影済）。

### 検出・是正した欠陥

| # | 欠陥 | 分類 | 是正 |
|---|---|---|---|
| 1 | かんばん (S-I01) からタスク詳細への導線ゼロ | 到達不能画面 | カードタイトル/リスト行を /tasks/detail?task= への実リンク化 |
| 2 | **操作バー未実装 — POST /tasks/{id}/approve・/reject・/retry は x-screen-ids: S-I02 と宣言済なのに UI 皆無** | UI断線 | 承認/差し戻し(理由 note 付)/再試行を 2 段階確認で実装。409 契約に従い awaiting/blocked のときだけ描画 (死にボタン禁止) |
| 3 | **API 契約違反: Task.dependencies/prerequisites/blocks が応答に無い** | 契約違反 | サービス層 SELECT+マッピング追加 (pytest 回帰追加)。依存タスクタブ (前提/後続チップ、タイトル・状態解決) を新設 |
| 4 | タブ構成がモックと乖離 (概要/入出力/添付 — 入出力・添付は裏付け API の無い死にタブ) | 死にタブ | モック準拠 + API 裏付けの 5 タブ (受入条件 3-tier/進捗・スコア/依存/実行履歴/コメント) に再構築 |
| 5 | 実行ステータス判定が `completed` 比較 — 実 enum は succeeded で**永遠に成功表示にならない実バグ** | 実バグ | succeeded 判定 + 日本語ラベル化 (running=実行中 等) |
| 6 | 担当 AI が生コード表示 (thor 等) | 鉄則5 | GET /ai-employees で表示名 (ソー) に解決 |
| 7 | コメントが読み取り専用 (POST /comments は対応済) | UI断線 | 追加コンポーザ実装 + 作成者ラベル化 (メンバー xxxxxxxx) |
| 8 | 進捗・スコアの可視化なし | モック乖離 | スコアサークル (conic-gradient) + AC 達成率バー + 再試行回数 (すべて実 execution データ) |
| 9 | 実行履歴から実行モニター (S-I03) への導線なし | UI断線 | 各行を /tasks/monitor?execution= への実リンク化 |
| 10 | モックの「あなたへの確認 (仕様変更 3 択)」「テスト結果」「関連資料」タブ・検証担当/経過時間/変更ファイル数メタ | ~~API不在~~ | ~~未描画 (Rule 10) → GAP-025 起票~~ → **GAP-025 是正で全て実装済 (2026-08-04、下記節)** |

### v2 追加テスト（.audit-i02.mjs 実測）

| ID | テスト項目 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| SI02-101 | かんばん→詳細の到達性 | カードに実リンク + 遷移成功 | PASS | TC6/TC7 |
| SI02-102 | 担当 AI 表示名解決 | 「ソー」表示・生コード thor 非表示 | PASS | TC8 |
| SI02-103 | 種別/優先度の日本語ラベル | 画面実装 / 優先度：高 | PASS | TC9 |
| SI02-104 | 受入条件 3-tier 描画 | 構造/機能/再発防止の tier 見出し | PASS | TC10 |
| SI02-105 | スコアサークル実データ | 実行 score 0.83 表示 | PASS | TC11 |
| SI02-106 | 依存チップのタイトル解決 | 前提/後続タスク名 + 完了スタイル | PASS | TC12（契約違反修正後に成立） |
| SI02-107 | 実行履歴→S-I03 実リンク | /tasks/monitor?execution={id} | PASS | TC13 |
| SI02-108 | コメント追加 UI→DB | target_type=task で永続 | PASS | TC14 |
| SI02-109 | 差し戻し (awaiting→blocked) | DB: blocked + note→blocked_reason + audit_logs | PASS | TC15/TC16 |
| SI02-110 | 再試行 (blocked→ready) | DB: ready + retry_count+1 | PASS | TC18 |
| SI02-111 | 承認 (awaiting→done) | DB: done + audit_logs task.approve | PASS | TC20/TC21 |
| SI02-112 | 操作バーの状態限定描画 | ready 等では非描画 (死にボタン禁止) | PASS | TC19 |
| SI02-113 | 390px 横スクロールなし + 目視 | 崩れなし | PASS | TC22 + スクリーンショット目視 |
| SI02-114 | 自作データ後片付け | 論理削除 + コメント削除 | PASS | TC23 |

### モック原文アンカー (CI Gate #6 照合台帳)

> Gate #6 (`scripts/ci/mock-impl-diff.py`) がモック `<button>` 原文で照合する台帳。
> 判定は本仕様書の監査記録の正確なモック文言への転記であり、新規判断ではない。

| モック原文ラベル | 判定 | 記録 |
|---|---|---|
| 最新仕様で実装し直す 所要 +4 時間。最新仕様で品質を担保します(推奨)。 | 実装済 (GAP-025 是正・文言差) | 実装は「最新仕様で実装し直す — 紐づくモックを最新バージョンに差し替えます（推奨）」(所要時間は実データが無いため推測表示しない) |
| 現状の実装で完了にする 追加対応は別タスクとして起票します。 | 実装済 (GAP-025 是正) | POST spec-changes/resolve (split) — 実フォロータスク起票 |
| 破棄して分解からやり直す 作業を破棄し、ピーターに再分解を依頼します。 | 実装済 (GAP-025 是正・文言差) | 実装は「破棄して分解からやり直す — 作業を破棄し、再分解待ち (blocked) にします」(自動再分解は未配線のため状態遷移で誠実に表現) |
| 再試行(条件 6・7 を追加) | 実装済 (文言差) | 実装は汎用「再試行」(POST /tasks/{id}/retry)。条件注記はモックのダミーデータ |

## GAP-025 是正 (2026-08-04) — あなたへの確認 / テスト結果 / 関連資料 / メタ行

> 監査スクリプト: `apps/web/.audit-i02-detail.mjs`（3 連続 ALL PASS・再実行可能）。
> pytest: `test_tasks.py::TestSpecChangesAndRelated` 5 件 + `test_executions.py::TestExecutionTests` 1 件。
> vitest: `tests/bundle-i/uc15-task-detail.test.tsx` +4 件 (11 passed)。
> migration `t-d-99zi`: tasks.verifier_employee_id + task_execution_tests (RLS: member select のみ)。

- **① あなたへの確認 (仕様変更 3 択)**: 実検知 = 紐づくモック (mock_id) と同一画面の新バージョンが
  後からアップロードされた状態のみ (`GET /tasks/{id}/spec-changes` — 推測イベントは生成しない)。
  3 択 (2 段階確認) = adopt (mock_id を最新へ差替) / split (実フォロータスク起票 — 仕様変更フォロー
  /triage/見積未実施 1h 明示) / discard (blocked + dispatch 解除 = 再分解待ち)。解決は
  metadata.spec_change_resolutions に記録し再表示しない。audit task.spec_change.*。
- **② テスト結果タブ**: `task_execution_tests` (Bridge complete の metadata.tests が記録) を
  `GET /executions/{id}/tests` で読む。pass/fail/skip 行 + タブカウント「合格 / 総数」。
  未記録は「まだ記録されていません」の誠実表示。
- **③ 関連資料タブ**: `GET /tasks/{id}/related` — mock (実バージョン)/spec_html_path/受入条件
  (実項目数)/ブランチ+変更ファイル数/参照ナレッジ (knowledge_references referrer_type='task')。
  **実リンクのみ** — 存在しない資料は返さない。
- **④ メタ行**: 検証担当 (tasks.verifier_employee_id — PATCH で設定、他 WS 社員 422、未設定は
  「未割当」) / 見積・経過 (経過 = 実 executions duration 合計 — TaskExecution に duration_seconds
  を追加) / 変更ファイル数 (tasks.files_changed — Bridge complete が列にも永続、0 は「記録なし」)。

| ID | テスト項目 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| SI02-301 | 仕様変更カード実描画 (mock v1→v2 実検知) | v1 → v2 表示・検知なしは非描画 | PASS | |
| SI02-302 | adopt → mock_id 差替 + 再表示なし | DB 突合 + metadata 記録 | PASS | |
| SI02-303 | split → 実フォロータスク起票 / discard → blocked | audit ×2 | PASS | pytest で discard も検証 |
| SI02-304 | テスト結果タブ (実 rows + カウント) | 1 / 2 表示・fail detail | PASS | complete 経由記録は pytest |
| SI02-305 | 関連資料タブ (実リンクのみ) | mock/branch/knowledge カード | PASS | AC/knowledge 未紐付け時は返さない (pytest) |
| SI02-306 | メタ行 (検証担当/見積・経過/変更ファイル数) | 実データ表示・未設定は誠実表示 | PASS | verifier 越境 422 (pytest) |
