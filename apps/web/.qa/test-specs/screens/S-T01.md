# ST01 運営ダッシュボード 画面別テスト仕様（S-T01・human-grade-qa design-audit）

> 種別: admin-dashboard / design-audit ラウンドで新規作成 (GAP-101 解消)。
> 結果列は 2026-07-19 の実操作 (`.audit-t.mjs` 24 TC) + vitest (uc30) の実測。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| ST01-001 | 運営ダッシュボード | 権限 | 非 admin は 403 拒否表示 | 一般ユーザー | 1. /admin を開く | 「権限がありません」(role=alert)・データ非表示 | PASS | TC1 実サインアップユーザーで実証 |
| ST01-002 | 運営ダッシュボード | 画面表示 | 管理シェル (ダークサイドバー) がモック準拠 | admin | 1. /admin を開く | ADMIN CONSOLE・運営タグ・6 項目ナビ | PASS | TC2。**監査前は共通シェルが無く 6 画面間の導線ゼロ** |
| ST01-003 | 運営ダッシュボード | 整合性 | KPI が API=DB (admin 所属 WS 範囲) と一致 | admin | 1. KPI と psql を突合 | 契約 (T-A-41) 通り所属 WS 範囲の実数 | PASS | TC3。全 DB 数との差は契約仕様 (備考に明記) |
| ST01-004 | 運営ダッシュボード | 画面表示 | KPI 4 種 (WS/プロジェクト/AI社員/監査イベント24h) | admin | 1. bento を目視 | 4 タイル表示 | PASS | TC4。**監査前は audit_log_count_24h が未表示 (API は返していた)** |
| ST01-005 | 運営ダッシュボード | 画面表示 | アクティビティ actor がメール表示 | admin | 1. 一覧を目視 | 生 UUID を出さない (/admin/users で解決、未解決時は短縮 ID) | PASS | TC5。**監査前は生 UUID 羅列 (鉄則5)** |
| ST01-006 | 運営ダッシュボード | 到達性 | ナビから他 5 画面へ遷移 | admin | 1. 各ナビをクリック | 全画面到達・active 表示 | PASS | TC6 |
| ST01-007 | 運営ダッシュボード | レスポンシブ | 390px 横スクロールなし・モバイルナビ操作可 | admin | 1. 390 で操作 | KPI 2×2・チップナビで遷移 | PASS | TC17-18 + 目視 |
| ST01-008 | 運営ダッシュボード | 受容差分 | ミッションヒーロー/トレンド/健全性/コスト等は未描画 | - | 1. 目視 | ダミー業務数値 (100社獲得/¥906 等) の虚偽表示をしない | PASS | GAP-019 起票 (実データ供給時に実装) |
| ST01-235 | 運営ダッシュボード | 秘密の保持 | 外部サービスの応答をそのまま利用者に見せない | 決済 (Stripe) が失敗する状態 | 1. 解約手続きの画面を開く 2. 応答本文を見る | 「決済サービスとのやり取りに失敗しました」とだけ出る。事業者の応答本文や HTTP の状態コードが本文に載らない | PASS | ai_required=no GAP-225 ／実測: billing portal の StripeApiError → 「決済サービスとのやり取りに失敗しました。時間をおいて…」のみ。生応答/HTTPコードは logger.error に回す (GAP-225 コード確認) 

### モック原文アンカー (CI Gate #6 照合台帳)

> Gate #6 (`scripts/ci/mock-impl-diff.py`) がモック `<button>` 原文で照合する台帳。
> 判定は本仕様書の監査記録の正確なモック文言への転記であり、新規判断ではない。

| モック原文ラベル | 判定 | 記録 |
|---|---|---|
| 累計 | 撤去 + GAP-019 | 今月/累計トグルはダミー業務数値の供給 API 不在 (ST01-008 の未描画群) |

### GAP-019 是正（2026-08-10 — 未描画セクション群の全実装）

> 監査スクリプト: `apps/web/.audit-t01-dashboard.mjs`（25 チェック・3 連続 ALL PASS・DB/audit 突合・再実行可能）。
> pytest: `tests/routes/test_admin_ops.py`（403 ゲート / mission / trends / channels / health / FB / costs の 7 件、7 passed）。
> vitest: `tests/bundle-j/uc30-admin-dashboard.test.tsx`（GAP-019 8 件追加、10 passed / bundle-j 84 passed）。
> migration: `supabase/migrations/t-d-99zm_gap019_admin_dashboard.sql`（admin_goals + beta_feedback + acquisition_records + admin_costs — RLS deny + service session）。

**設計根拠 (誠実表示)**: モックのダミー業務数値 (100 社獲得/¥906/99.97% 等) をそのまま
表示せず、**表示値の出所を 3 種に限定**した: ① 実カウント/実計測 (WS 数・30 日実増分・
タスク実行数・DB latency・Bridge presence) ② **運営が明示的に記録した値** (獲得目標・
取得チャネル・コスト — 記録フォーム付き、未記録は honest 空表示) ③ 実設定状態
(外部 API キーの設定有無)。推測の稼働率・創作の売上は表示しない。MRR は課金未導入の
実額 ¥0 と明示。ミッションの「必要ペース」は目標と実数からの実計算。

| ID | テスト項目 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| ST01-201 | ミッションヒーロー | 目標未設定 → honest 表示 + 記録フォーム。PUT /admin/goal → admin_goals 永続 | PASS | 数値はシステムが創作しない |
| ST01-202 | ミッション実計算 | あと N 社 = 目標 − 実 WS 数 / 必要ペース = 残 ÷ 残月数 / いまのペース = 30 日実増分 | PASS | DB 突合 |
| ST01-203 | KPI bento 拡張 | タスク実行/30日 (実 task_executions + 平均スコア)・ベータ FB 件数・稼働 Bridge 数・退会 30 日 | PASS | /admin/platform-stats |
| ST01-204 | トレンドグラフ | 週次実累計 (created_at 由来・単調非減少) の実 SVG。MRR は「¥0（課金未導入）」明示 | PASS | pytest で単調性検証 |
| ST01-205 | 取得チャネル別 | acquisition_records の実集計バー + 30 日/累計 seg + 「獲得を記録」フォーム + 記録削除 | PASS | 未記録は honest 空表示 |
| ST01-206 | プラットフォーム健全性 | 実計測のみ: DB roundtrip 実測 ms / DB サイズ・接続数 / Bridge presence / 外部 API 設定有無 | PASS | 推測の稼働率を出さない |
| ST01-207 | ベータ FB | POST /beta-feedback (認証ユーザー収集) → admin 一覧 (カテゴリタグ/未対応 seg) → 対応済み化 (open のみ・二重 404) | PASS | DB 突合 |
| ST01-208 | 運営コスト | 記録フォーム → admin_costs (月初正規化) → 実合計 → 削除。「運営が記録した実費のみ」明示 | PASS | |
| ST01-209 | admin ゲート | 全 GAP-019 エンドポイントは非 admin 403 | PASS | pytest 7 パス一括検証 |
| ST01-210 | audit 証跡 | admin.goal.set / admin.acquisition.record(delete) / beta.feedback.create(resolve) / admin.cost.record(delete) | PASS | 5 種突合 |
| ST01-211 | 自作データ後片付け | 目標/FB/記録の削除 (再実行可能) | PASS | |

### 2026-08-26 追記 — 最初の運営を作れるか (GAP-217)

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| ST01-220 | 運営ダッシュボード | 初期構築 | 新しい環境で最初の運営を作る手順が存在し、そのとおりにやれば運営画面に入れる | 誰も運営でない DB | 1. 手順書を読む 2. 書かれたとおりに実行する 3. 運営画面を開く | 手順書に沿って運営になれる。「登録もサインインもできるが運営画面には永久に入れない」状態にならない | PASS | ai_required=no GAP-217 ／実測: scripts/grant-admin.sh で運営になれる (test_grant_admin_script)。「登録もサインインもできるが運営に永久に入れない」にならない |
| ST01-221 | 運営ダッシュボード | 初期構築 | 昇格しても外部サインインの情報を壊さない | 外部サインイン済みの利用者 | 1. 昇格させる 2. `raw_app_meta_data` を見る | `role` が足されるだけで `provider` 等の既存の値が残る（jsonb を丸ごと置き換えていない） | PASS | ai_required=no GAP-217 ／実測: grant-admin は role を足すだけで provider 等の既存 jsonb を残す (test_grant_admin_script) |
| ST01-222 | 運営ダッシュボード | 初期構築 | 昇格後に入り直す必要があることが手順に書いてある | 昇格直後 | 1. 入り直さずに運営画面を開く 2. 手順書を読む | 入り直す前は 403 のまま。手順書にその理由（トークンはサインイン時の role を焼き込む）と対処が書かれている | PASS | ai_required=no GAP-217 ／実測: 再サインイン前は 403 のまま (トークンにサインイン時の role が焼き込まれる)。手順書に理由と対処 (test_grant_admin_script) |
| ST01-223 | 運営ダッシュボード | エラー | 居ない相手を昇格させようとしたら理由が出て止まる | 未登録のメールアドレス | 1. その相手を昇格させようとする | 「アカウントがありません」と出て終了する。黙って 0 件更新して成功に見せない | PASS | ai_required=no GAP-217 ／実測: 存在しないアカウントへの grant は「アカウントがありません」で終了・0 件更新を成功に見せない (test_grant_admin_script) |
| ST01-224 | 運営ダッシュボード | 表示 | 運営専用 API を非 admin が叩いたときの文言が日本語 | 一般利用者でサインイン | 1. 運営画面を開く 2. 応答本文を見る | `admin privilege required` 等の英語ではなく日本語で返る（例:「この操作は運営のみが行えます。」） | PASS | ai_required=no GAP-218 ／実測: 非 admin API は「この操作は運営のみが行えます。」日本語 (admin privilege required 等の英語でない) |
| ST01-225 | 運営ダッシュボード | 整合性 | 利用者に届く文言に英語・内部名が残っていない | - | 1. route 全体を機械で走査する | `HTTPException` の文言がすべて日本語で、snake_case の内部名（byok_key / cron_schedule 等）や社内の管理番号を含まない。**新しく英語で書いたらテストが落ちる** | PASS | ai_required=no GAP-218 ／実測: test_route_messages_are_japanese が src 全体の HTTPException 文言を日本語強制。新規英語/内部名は落ちる |
| ST01-226 | 運営ダッシュボード | 権限 | 運営でない人には運営コンソールを組み立てない | 一般利用者でサインイン | 1. `/admin` と配下の各画面を開く | 運営メニュー（7 項目）が 1 つも描画されず、運営専用 API を 1 本も呼ばない。開いた URL のまま「この画面は運営専用です」と戻り道が出る | PASS | ai_required=no GAP-219 ／実測: 非 admin の /admin は運営メニュー 0 描画・運営 API 0 呼び出し・URL そのまま「運営専用です」(AD-004/005/007 と同一実測) |
| ST01-227 | 運営ダッシュボード | 権限 | 門を付けたことで運営が入れなくならない | 運営でサインイン | 1. `/admin` と配下の各画面を開く | 従来どおり運営コンソールが描画され、権限メッセージは 0 件 | PASS | ai_required=no GAP-219 ／実測: admin は運営コンソール描画・権限メッセージ 0 件 (AD-008 と同一実測) |
| ST01-228 | 運営ダッシュボード | 秘密の保持 | 画面側の門を防御と取り違えない | 署名が正しくない JWT | 1. 偽のトークンで `/admin` を開く 2. 運営専用 API を直接叩く | 画面は通り抜けられても **API が 403 を返す**。画面側の門は表示の整理であって防御ではないことが、実装のコメントにも明記されている | PASS | ai_required=no GAP-219 ／実測: 署名不正 JWT でも API が 403/401 (AD-010 実測)。middleware は署名検証しない=防御は API 側、実装コメントに明記 |
| ST01-229 | 運営ダッシュボード | 初期構築 | 新しい環境の DB を最後まで作れる | 空の PostgreSQL | 1. bootstrap → migration → seed を順に流す | すべて成功で終わる。1 本も「適用できず」が残らず、seed も 0 エラー。途中で止まると **その先のゲートが本体を一度も実行できない** | PASS | ai_required=no GAP-220 ／実測: test_provision_scripts: 構築が all-or-nothing で全 migration 適用・seed 0 エラー (途中停止でゲートが本体を実行できない罠を回避) |
| ST01-230 | 運営ダッシュボード | 初期構築 | 適用できない migration を黙って飛ばさない | 依存が壊れた migration がある状態 | 1. migration を流す | 「skip した」で成功扱いにせず、残ったファイル名とエラーを出して失敗する（表の欠けた DB で緑にならない） | PASS | ai_required=no GAP-220 ／実測: test_provision_scripts: skip を成功扱いにせず残ファイル名+エラーで失敗 (表の欠けた DB で緑にしない) |
| ST01-231 | 運営ダッシュボード | 整合性 | 同じ表を seed と migration の両方が書いていない | - | 1. seed を走査する | migration が持ち主の表（法務文書など）を seed が書いていない。両方に書くと適用順で結果が変わる | PASS | ai_required=no GAP-220 ／実測: test_provision_scripts: migration が持ち主の表 (法務文書) を seed が二重に書かない (適用順で結果が変わらない) |
| ST01-232 | 運営ダッシュボード | 秘密の保持 | 運営専用の表を、一般利用者が消せない | 一般利用者のロール | 1. 運営のエラーログ等に対し `truncate` を試す | 権限エラーで拒否される。**TRUNCATE は RLS の対象外**なので、RLS だけでなく権限側でも閉じていること | PASS | ai_required=no GAP-221 ／実測: test_blanket_deny / provision: TRUNCATE は RLS 対象外なので権限側でも閉じる (権限エラーで拒否) |
| ST01-233 | 運営ダッシュボード | 整合性 | ローカルと本番でアプリ用ロールの権限が同じ | - | 1. ローカル構築と CI の grant を突き合わせる | ローカルだけ広い権限（TRUNCATE 等）になっていない。**ローカルで通ったことが本番で通る保証**になる | PASS | ai_required=no GAP-221 ／実測: ローカルだけ広い権限 (TRUNCATE 等) を持たない (ローカルで通ったことが本番で通る保証) |
| ST01-234 | 運営ダッシュボード | 整合性 | 門が「正しい設計」を違反と呼ばない | policy を意図的に置かない表がある状態 | 1. RLS 監査を走らせる | 理由つきで宣言された default deny は PASS。宣言が無い 0 本は FAIL。**赤が普通になった門は本物の穴を見逃す** | PASS | ai_required=no GAP-221 ／実測: test_blanket_deny: 理由つき宣言の default deny は PASS・宣言 0 本は FAIL (赤が普通の門にしない) |
