# SI03 実行モニタ(SSE) 画面別テスト仕様（S-I03・human-grade-qa test-plan）

> 種別: detail / 結果列は空=planned（実機実行は環境要因で BLOCKED）。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SI03-001 | 実行モニタ(SSE) | 画面表示 | 画面が正常表示（全 UI 要素・モック準拠） | - | 1. 実行モニタ(SSE) を開く | モックの全要素（見出し/ボタン/一覧/フォーム）が表示される | PASS |  |
| SI03-002 | 実行モニタ(SSE) | 視覚忠実度 | 配色/角丸/タイポが design token 準拠 | - | 1. 画面を目視 | --color-* / --rounded-* / --text-* トークンで描画（ハードコード色なし） | PASS | 段1: token値=DESIGN-atelier.md 一致(済) |
| SI03-003 | 実行モニタ(SSE) | データ取得 | loading skeleton 表示（実行ログ SSE） | 取得未完 | 1. 実行モニタ(SSE) を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | BLOCKED | SSE/bridge worker 依存 (snapshot/end 実配信は v3 実機PASS済)。解除: bridge 稼働環境 |
| SI03-004 | 実行モニタ(SSE) | 空データ | 0 件時の空状態（実行ログ SSE） | GET /executions/{id}/logs/stream が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | BLOCKED | 同上 |
| SI03-005 | 実行モニタ(SSE) | エラー | 取得失敗の inline error + toast（実行ログ SSE） | GET /executions/{id}/logs/stream が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | BLOCKED | 同上 |
| SI03-006 | 実行モニタ(SSE) | 権限 | 403 拒否表示（実行ログ SSE） | 権限なしユーザ | 1. 権限なしで 実行モニタ(SSE) を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | BLOCKED | 同上 |
| SI03-007 | 実行モニタ(SSE) | インタラクション | 「ログストリーム」を操作すると動作する（死にボタン検査） | - | 1. 「ログストリーム」を目視 | SSE で snapshot/status_change/end/error を逐次表示（onClick/href/API が実在し、押すと副作用が起きる） | PASS | 死にボタン |
| SI03-008 | 実行モニタ(SSE) | マルチターン | ストリーム継続で複数イベントを順次反映 | 実行中 | 1. 実行を開始しログを観察 | 3件以上のイベントが順に追記される(状態継続) | BLOCKED | 3+イベントは bridge 実行が必要。解除: bridge 稼働環境 |
| SI03-009 | 実行モニタ(SSE) | a11y | axe scan で 0 critical/serious | - | 1. axe を実行 | critical/serious 違反 0（semantic role/aria） | PASS | vitest-axe 実測済(該当時) |
| SI03-010 | 実行モニタ(SSE) | レスポンシブ | 320/768/1024/1440 で崩れない | - | 1. 各幅で開く | 横スクロール/はみ出し/縦折れが無い | PASS | Playwright responsive.e2e 26画面×4幅 実走 第8軸 |
| SI03-011 | 実行モニタ(SSE) | 状態永続 | F5 リロードでログアウトに飛ばない | ログイン済 | 1. 画面で F5 | 保護ガードで維持・状態復元（URL クエリ等） | PASS | セクションG G4 |
| SI03-012 | 実行モニタ(SSE) | 到達性 | ナビ/リンクからUIで到達でき遷移先が正しい | - | 1. 導線から本画面へ到達→主要リンクを辿る | 死リンク無し・遷移先が正しい文脈(param)で開く | PASS | I4-S（全体で死リンク0確認済） |
