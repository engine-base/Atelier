# SF02 フェーズ管理 画面別テスト仕様（S-F02・human-grade-qa test-plan）

> 種別: list / 結果列は空=planned（実機実行は環境要因で BLOCKED）。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SF02-001 | フェーズ管理 | 画面表示 | 画面が正常表示（全 UI 要素・モック準拠） | - | 1. フェーズ管理 を開く | モックの全要素（見出し/ボタン/一覧/フォーム）が表示される | PASS |  |
| SF02-002 | フェーズ管理 | 視覚忠実度 | 配色/角丸/タイポが design token 準拠 | - | 1. 画面を目視 | --color-* / --rounded-* / --text-* トークンで描画（ハードコード色なし） | PASS | 段1: token値=DESIGN-atelier.md 一致(済) |
| SF02-003 | フェーズ管理 | データ取得 | loading skeleton 表示（工程一覧） | 取得未完 | 1. フェーズ管理 を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SF02-004 | フェーズ管理 | 空データ | 0 件時の空状態（工程一覧） | GET /workflow/phases?project_id が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SF02-005 | フェーズ管理 | エラー | 取得失敗の inline error + toast（工程一覧） | GET /workflow/phases?project_id が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SF02-006 | フェーズ管理 | 権限 | 403 拒否表示（工程一覧） | 権限なしユーザ | 1. 権限なしで フェーズ管理 を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SF02-007 | フェーズ管理 | インタラクション | 「状態 select」を操作すると動作する（死にボタン検査） | - | 1. 「状態 select」を変更 | PATCH で状態遷移(done↔completed/blocked↔skipped 変換・楽観)（onClick/href/API が実在し、押すと副作用が起きる） | PASS | 死にボタン |
| SF02-008 | フェーズ管理 | 楽観更新 | 状態遷移(select): 成功で即時反映 | 対象が存在 | 1. 状態遷移(select) を実行 | PATCH /workflow/phases/{id} 呼び出し前にUIへ楽観反映→成功で確定（別GET再取得で反映） | PASS | EVENT-DRIVEN |
| SF02-009 | フェーズ管理 | 楽観更新 | 状態遷移(select): 失敗でロールバック | サーバ 4xx/5xx | 1. 状態遷移(select) を実行しAPIが失敗 | 楽観反映が元に戻り、inline error + toast を表示 | PASS | EVENT-DRIVEN/rollback |
| SF02-010 | フェーズ管理 | エラー | 状態遷移(select): API 失敗時 inline error + toast | 接続失敗/レート | 1. 状態遷移(select) を失敗させる | role=alert + toast、状態は不整合にならない | PASS | UNWANTED critical |
| SF02-011 | フェーズ管理 | a11y | axe scan で 0 critical/serious | - | 1. axe を実行 | critical/serious 違反 0（semantic role/aria） | PASS | vitest-axe 実測済(該当時) |
| SF02-012 | フェーズ管理 | レスポンシブ | 320/768/1024/1440 で崩れない | - | 1. 各幅で開く | 横スクロール/はみ出し/縦折れが無い | PASS | Playwright responsive.e2e 26画面×4幅 実走 第8軸 |
| SF02-013 | フェーズ管理 | 状態永続 | F5 リロードでログアウトに飛ばない | ログイン済 | 1. 画面で F5 | 保護ガードで維持・状態復元（URL クエリ等） | PASS | セクションG G4 |
| SF02-014 | フェーズ管理 | 到達性 | ナビ/リンクからUIで到達でき遷移先が正しい | - | 1. 導線から本画面へ到達→主要リンクを辿る | 死リンク無し・遷移先が正しい文脈(param)で開く | PASS | I4-S（全体で死リンク0確認済） |

---

## design-audit ラウンド v2 (2026-07-19) — 期間表示追加 + 状態遷移 DB 突合

> モック: `06_mockups/workflow/S-F02-phases.html` / 実装: `apps/web/app/workflow/s_f02/`
> 実操作: `.audit-f02.mjs` 7/7 PASS (状態遷移の DB 突合・往復込み)

### モック要素台帳 (Rule 10 全分類)

| モック要素 | 分類 | 対応 |
|---|---|---|
| フェーズタイムライン (カード + 番号 + 状態 pill) | 実装 (従来) | GET /workflow/phases の実データ。DB 件数一致を実操作で突合 |
| フェーズ期間 (日付範囲) | **実装 (v2 追加)** | API の started_at/completed_at が未使用だった → 「YYYY-MM-DD 〜 YYYY-MM-DD」表示 |
| 状態変更 select | 実装 (従来) | PATCH /workflow/phases/{id} を実操作で DB 突合 (往復) |
| 統計 (確定/進行中/未着手/ブロック) | 実装 (是正済) | 実カウント。モックの「F-IMP01 実行回数 12 回」「日次整合性 OK」は供給 API 不在で撤去 (GAP-022) |
| AI 提案フェーズカード (承認/却下/提案理由) | **撤去 + GAP-022** | フェーズ提案生成 API 不在。偽の承認ボタンを置かない |
| F-IMP01 影響範囲解析パネル | **撤去 + GAP-022** | 影響範囲解析 API 不在。偽の影響グラフを置かない |
| per-phase タスク数「8/24」 | **撤去 + GAP-022** | tasks GET は phase_id フィルタを持たない (project_id/lifecycle_stage のみ) |
| 運用ルールカード | 実装 (従来) | 静的説明文 (モック準拠) |

### v2 追加 TC (全て PASS)
| ID | 項目 | 結果 |
|---|---|---|
| SF02-v2-01 | タイムライン件数 = DB phases 数 | PASS |
| SF02-v2-02 | 期間 (YYYY-MM-DD 〜) 表示 | PASS |
| SF02-v2-03 | 統計「確定フェーズ数」= DB completed 数 | PASS |
| SF02-v2-04 | 状態遷移 select → PATCH → DB 反映 → 往復復元 | PASS |
| SF02-v2-05 | 390px 崩れなし | PASS |
