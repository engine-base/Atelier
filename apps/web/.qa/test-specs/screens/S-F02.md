# SF02 フェーズ管理 画面別テスト仕様（S-F02・human-grade-qa test-plan）

> 種別: list / 結果列は空=planned（実機実行は環境要因で BLOCKED）。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SF02-001 | フェーズ管理 | 画面表示 | 画面が正常表示（全 UI 要素・モック準拠） | - | 1. フェーズ管理 を開く | モックの全要素（見出し/ボタン/一覧/フォーム）が表示される | PASS |  |
| SF02-002 | フェーズ管理 | 視覚忠実度 | 配色/角丸/タイポが design token 準拠 | - | 1. 画面を目視 | --color-* / --rounded-* / --text-* トークンで描画（ハードコード色なし） | PASS | 段1: token値=DESIGN-atelier.md 一致(済) |
| SF02-003 | フェーズ管理 | データ取得 | loading skeleton 表示（工程一覧） | 取得未完 | 1. フェーズ管理 を開き取得完了前を観察 | <Loading>（role=status/aria-live）が出る | PASS |  |
| SF02-004 | フェーズ管理 | 空データ | 0 件時の空状態（工程一覧） | GET /workflow/phases?project_id が空配列 | 1. データ0件で開く | 空状態メッセージを表示（500/undefined 参照にならない） | PASS | セクションH |
| SF02-005 | フェーズ管理 | エラー | 取得失敗の inline error + toast（工程一覧） | GET /workflow/phases?project_id が 5xx | 1. API を 500 でモックし開く | role=alert の inline error 表示 かつ toast 発火 | PASS | UNWANTED critical |
| SF02-006 | フェーズ管理 | 権限 | 403 拒否表示（工程一覧） | 権限なしユーザ | 1. 権限なしで フェーズ管理 を開く | 「権限がありません」等の拒否表示（他人のデータは出さない=RLS） | PASS | セクションG 越境 |
| SF02-007 | フェーズ管理 | インタラクション | 「状態 select」を操作すると動作する（死にボタン検査） | - | 1. 「状態 select」を変更 | PATCH で状態遷移(done↔completed/blocked↔skipped 変換・楽観)（onClick/href/API が実在し、押すと副作用が起きる） | PASS | 死にボタン |
| SF02-008 | フェーズ管理 | 楽観更新 | 状態遷移(select): 成功で即時反映 | 対象が存在 | 1. 状態遷移(select) を実行 | PATCH /workflow/phases/{id} 呼び出し前にUIへ楽観反映→成功で確定（別GET再取得で反映） | PASS | EVENT-DRIVEN |
| SF02-009 | フェーズ管理 | 楽観更新 | 状態遷移(select): 失敗でロールバック | サーバ 4xx/5xx | 1. 状態遷移(select) を実行しAPIが失敗 | 楽観反映が元に戻り、inline error + toast を表示 | PASS | EVENT-DRIVEN/rollback |
| SF02-010 | フェーズ管理 | エラー | 状態遷移(select): API 失敗時 inline error + toast | 接続失敗/レート | 1. 状態遷移(select) を失敗させる | role=alert + toast、状態は不整合にならない | PASS | UNWANTED critical |
| SF02-011 | フェーズ管理 | a11y | axe scan で 0 critical/serious | - | 1. axe を実行 | critical/serious 違反 0（semantic role/aria） | PASS | vitest-axe 実測済(該当時) |
| SF02-012 | フェーズ管理 | レスポンシブ | 320/768/1024/1440 で崩れない | - | 1. 各幅で開く | 横スクロール/はみ出し/縦折れが無い | PASS | Playwright responsive.e2e 26画面×4幅 実走 第8軸 |
| SF02-013 | フェーズ管理 | 状態永続 | F5 リロードでログアウトに飛ばない | ログイン済 | 1. 画面で F5 | 保護ガードで維持・状態復元（URL クエリ等） | PASS | セクションG G4 |
| SF02-014 | フェーズ管理 | 到達性 | ナビ/リンクからUIで到達でき遷移先が正しい | - | 1. 導線から本画面へ到達→主要リンクを辿る | 死リンク無し・遷移先が正しい文脈(param)で開く | PASS | I4-S（全体で死リンク0確認済） |
| SF02-315 | フェーズ管理 | 表示 | 提案の重複・二重処理が日本語で伝わる | 保留中のフェーズ提案がある状態 | 1. もう一度提案する 2. 処理済みの提案を承認する | 日本語で理由と次の行動が出る。`a pending phase proposal already exists for this project` のような英語が出ない | BLOCKED: 前提の「保留中の提案」が作れない — POST /workflow/phase-proposals は本番で 503「お使いの PC の Bridge がオフラインです…」(pending 重複判定より前に LLM 生成で止まる) / 解除条件: Bridge ハーネスで pending 提案を 1 件作った状態で再提案・処理済み提案の承認を流す | ai_required=no GAP-225
| SF02-316 | フェーズ管理 | 整合性 | 確定は明示の承認が要り、確定後の成果物は変わらない | active フェーズにモックがある | 1. 確認なしで確定する 2. 承認つきで確定する 3. 凍結後にモックを変更・削除する | 確認なしは 403 で理由が出る。承認つきなら frozen になり次フェーズが active になる。凍結後の変更・削除は 409 で、DB の内容は 1 文字も変わらない | **PASS prod(pre-launch) (2026-09-03)**: freeze confirm 無し・confirm:false → 403「確定すると成果物が凍結され…承認してください。」で phase 不変 / confirm:true → 200 でフェーズ1 frozen(frozen_at 付与)+フェーズ2 active / 再確定 409 / 凍結後の PATCH・DELETE・discard は 409「確定済みのフェーズの成果物は変更できません…」で GET /mocks/{id} の JSON は before/after で byte 一致 (cmp IDENTICAL)。POST /mocks/{id}/versions のみ 201 で v2 がフェーズ2 に積まれる (凍結 v1 行は不変 = 設計どおり「次のフェーズで追加」) | ai_required=no GAP-152

---

## design-audit ラウンド v2 (2026-07-19) — 期間表示追加 + 状態遷移 DB 突合

> モック: `06_mockups/workflow/S-F02-phases.html` / 実装: `apps/web/app/workflow/s_f02/`
> 実操作: `.audit-f02.mjs` 7/7 PASS (状態遷移の DB 突合・往復込み)

### モック要素台帳 (Rule 10 全分類)

| モック要素 | 分類 | 対応 |
|---|---|---|
| フェーズタイムライン (カード + 番号 + 状態 pill) | 実装 (従来) | GET /workflow/phases の実データ。DB 件数一致を実操作で突合 |
| フェーズ期間 (日付範囲) | **実装 (v2 追加)** | API の started_at/completed_at が未使用だった → 「YYYY-MM-DD 〜 YYYY-MM-DD」表示 |
| 状態変更 select | 実装 (従来) | PATCH /workflow/phases/{id} を実操作で DB 突合 (往復) |
| 統計 (確定/進行中/未着手/ブロック) | 実装 (是正済) | 実カウント。モックの「F-IMP01 実行回数 12 回」「日次整合性 OK」は供給 API 不在で撤去 (GAP-022) |
| AI 提案フェーズカード (承認/却下/提案理由) | **撤去 + GAP-022** | フェーズ提案生成 API 不在。偽の承認ボタンを置かない |
| F-IMP01 影響範囲解析パネル | **撤去 + GAP-022** | 影響範囲解析 API 不在。偽の影響グラフを置かない |
| per-phase タスク数「8/24」 | **撤去 + GAP-022** | tasks GET は phase_id フィルタを持たない (project_id/lifecycle_stage のみ) |
| 運用ルールカード | 実装 (従来) | 静的説明文 (モック準拠) |

### v2 追加 TC (全て PASS)
| ID | 項目 | 結果 |
|---|---|---|
| SF02-v2-01 | タイムライン件数 = DB phases 数 | PASS |
| SF02-v2-02 | 期間 (YYYY-MM-DD 〜) 表示 | PASS |
| SF02-v2-03 | 統計「確定フェーズ数」= DB completed 数 | PASS |
| SF02-v2-04 | 状態遷移 select → PATCH → DB 反映 → 往復復元 | PASS |
| SF02-v2-05 | 390px 崩れなし | PASS |

### モック原文アンカー (CI Gate #6 照合台帳)

> Gate #6 (`scripts/ci/mock-impl-diff.py`) がモック `<button>` 原文で照合する台帳。
> 判定は本仕様書の監査記録の正確なモック文言への転記であり、新規判断ではない。

| モック原文ラベル | 判定 | 記録 |
|---|---|---|
| 提案理由を見る | 撤去 + GAP-022 | AI 提案フェーズカード一式 (承認/却下/提案理由) の一部として撤去済 |
| 解析結果を見る | 撤去 + GAP-022 | F-IMP01 影響範囲解析パネルの一部として撤去済 |

## GAP-004 是正 (2026-08-03) — 工程への担当 AI 社員割当

> 監査スクリプト: `apps/web/.audit-f02-assign.mjs`（再実行可能・実操作+DB 突合+実 JWT 422 検証・3 連続 ALL PASS）。
> pytest: `tests/routes/test_workflow.py::TestPhaseAssignees` 2 件 / vitest: uc11 に 1 件追加。

| ID | 項目 | 結果 | 備考 |
|---|---|---|---|
| SF02-201 | phases.assigned_employee_ids uuid[] (migration t-d-99zb、tasks.dependencies と同型) | PASS | 既定 '{}' |
| SF02-202 | PATCH /workflow/phases/{id} {assigned_employee_ids} — 丸ごと置換。他 WS 社員は 422 (同一 WS 検証) | PASS | pytest (割当/置換/クリア/越境 422) |
| SF02-203 | S-F02 割当 UI (チップ + × で外す + 追加 select、楽観更新) | PASS | 実操作往復 |
| SF02-204 | 社員候補は project → workspace で絞る | PASS | **監査が実バグ検出**: 無フィルタ /ai-employees が他 WS の同名社員を混ぜ、選択が 422 で弾かれていた → workspace_id フィルタ修正 |
| SF02-205 | S-F01 ヘッダーアバターは実割当を優先 (未割当はスレッド/確定事項集計にフォールバック) | PASS | WorkflowGraphContainer |

### GAP-022 是正（2026-08-10 — AI 提案フェーズ + F-IMP01 + phase 別集計の全実装）

> 監査スクリプト: `apps/web/.audit-f02-phase-ops.mjs`（22 チェック・3 連続 ALL PASS・専用プロジェクトシード・DB/audit 突合・再実行可能）。
> pytest: `tests/routes/test_workflow.py`（提案ライフサイクル / 503 / 影響解析+適用+統計 の 3 クラス追加、9 passed）。
> vitest: `tests/bundle-g/uc11-phase-list.test.tsx`（GAP-022 6 件追加、10 passed）。
> migration: `supabase/migrations/t-d-99zk_gap022_phase_ops.sql`（phase_proposals + impact_analyses + RLS）。

**設計根拠**: 運用ルール「フェーズ追加は AI 提案のみ」の提案主体は COO AI ジャービス
(モックの avatar-jarvis)。GAP-023/024 と同じ確定アーキ判断 — AI 生成は**人間の明示操作
起点** (「ジャービスに次フェーズを提案してもらう」ボタン) でのみ実行し、自動生成しない。
承認で実 phases 行が確定、却下は不変。LLM 未設定は 503 (偽の提案を出さない)。
モックの「日次整合性チェック」は日次バッチが存在しないため、**表示時に実計算する
「依存整合性チェック」** (dependencies の参照先実在検証) として誠実に実装した。

| ID | テスト項目 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| SF02-301 | phase 別タスク集計行 | 実 /workflow/phase-task-stats (done/total/awaiting + 最新実行スコア平均) をカードに描画。タスク 0 のフェーズは行を出さない | PASS | モックの「8/24 タスク完了」実体化 |
| SF02-302 | 提案依頼 (明示操作) | ボタン → POST /workflow/phase-proposals → pending カード (.proposed 準拠: ✦バッジ/承認待ち pill/ジャービスが提案 · 日時) | PASS | 1 プロジェクト 1 pending (409) |
| SF02-303 | 提案理由を見る | トグルで実 reason 表示 | PASS | |
| SF02-304 | 却下 | rejected + フェーズ数不変 | PASS | |
| SF02-305 | 承認 | 実 phases 行確定 (order=max+1 再計算) + approved_phase_id + タイムラインに実描画 | PASS | audit phase.proposal.* |
| SF02-306 | LLM 未設定 | POST 提案が 503 llm_unconfigured (偽の提案を出さない) | PASS | pytest |
| SF02-307 | F-IMP01 解析 | タスク+移動先を選択 → dependencies (uuid[]) の推移的走査 (recursive CTE) → 影響ノード + 「N タスクへの影響を検出（実装済み D / その他 P）」 | PASS | 推測イベントなし |
| SF02-308 | 解析の記録 | impact_analyses に実記録 → 統計「F-IMP01 実行回数（本日）」が実カウント更新 | PASS | |
| SF02-309 | 承認して移動 | 実移動 (tasks.phase_id) + 完了済影響のリファクタ自動起票 (F-CUC02: origin_type=refactor / triage / カテゴリ リファクタ / 暫定 1h 明示) | PASS | 二重適用 409 |
| SF02-310 | 別プロジェクト越境 | 移動先フェーズが別プロジェクト → 422 / R-T08 他 WS は 404 | PASS | pytest |
| SF02-311 | 依存整合性チェック | dangling 依存 0 = OK / N 件 = 「不整合 N 件」(表示時実計算 — 日次バッチを偽装しない) | PASS | |
| SF02-312 | 統計「提案中」 | pending 提案の実カウント | PASS | |
| SF02-313 | audit 証跡 | phase.proposal.propose/approve/reject + task.impact.analyze/apply | PASS | 6 行突合 |
| SF02-314 | 自作データ後片付け | 専用プロジェクトごと削除 (cascade — 再実行可能) | PASS | |
