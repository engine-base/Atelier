# ST03 AI 社員テンプレート 画面別テスト仕様（S-T03・human-grade-qa design-audit）

> 種別: admin-list + editor (GAP-031⑤ で T-A-42 契約を scope expand し編集対応) / design-audit ラウンドで新規作成 (GAP-101 解消)。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 | タスク | 実行条件 |
|---|---|---|---|---|---|---|---|---|---|---|
| ST03-001 | AI 社員テンプレ | 画面表示 | 一覧が実データ (DB 件数一致) | admin | 1. /admin/s_t03 | ai_employee_templates 全件 | PASS | TC9 DB 突合 (rows=14) | T-UC-32 | L1 |
| ST03-002 | AI 社員テンプレ | 権限 | 非 admin 403 | 一般 | 1. 開く | 拒否表示 | PASS | TC1 と同ガード (共通コンテナ)。PATCH/deployment も API 側 403 (pytest) | T-A-42, T-UC-32 | L1 |
| ST03-003 | AI 社員テンプレ | 到達性 | シェルナビから到達 | admin | 1. ナビ | 遷移 + active | PASS | TC6 | T-UC-32 | L1 |
| ST03-004 | AI 社員テンプレ | 受容差分 | ~~編集 UI なし (契約が read-only)~~ → **編集対応済 (GAP-031⑤)** | admin | 1. テンプレ選択 2. 編集 3. 保存して全 WS 反映 | 部分更新 + version 自動 increment + audit template.update | PASS | T-A-42 契約を tickets.json note で scope expand (2026-08-10)。実操作監査 .audit-t03-template-edit.mjs 3 連続 ALL PASS | T-UC-32 | L1 |
| ST03-005 | AI 社員テンプレ | レスポンシブ | 390px 横スクロールなし | admin | 1. 390 | 崩れなし | PASS | TC18 | T-UC-32 | L1 |

## 是正記録（GAP-031⑤ / 2026-08-10）

- **契約変更 (先行 PR 相当 commit)**: T-A-42 は read-only 契約だったため、仕様変更プロトコルに従い
  `07_tasks/tickets.json` の T-A-42 note で scope expand を先行宣言（validate.sh 216/216 PASS）。
- **実装**: PATCH `/admin/ai-employee-templates/{template_id}`（部分更新 — default_display_name /
  department / role / system_prompt / specialty / default_skills(uuid[]) / default_knowledge_cats、
  保存のたび version 自動 increment、audit `template.update`、空 body 422、admin gate + service
  session（RESTRICTIVE no_update のため）） + GET `.../deployment`（ai_employees.template_id 参照の
  実カウント — archived=false のみ）。
- **UI (モック S-T03-templates.html 忠実)**: 一覧 + エディタ 2 ペイン。基本情報（内部名は変更不可
  表示）/ System Prompt（ADMIN ONLY）/ 専門領域 / スキル pills（×で外す +「スキル追加」= 実
  /admin/skills 由来の候補 select）/ ナレッジカテゴリ pills（「カテゴリ追加」）/「保存して全 WS 反映」
  （ヘッダー）。展開先は「展開先：N WS（自動同期）」+ 注意文とも実 deployment カウント。変更なし
  保存は「変更がありません。」で honest ブロック（偽の成功を出さない）。
- **モックにあるが本 scope 外（死にボタンを出さない — 別途起票対象）**: テンプレ「追加」（新規作成）、
  「複製」、バージョン履歴と「復元」。tickets.json T-A-42 note に複製・バージョン復元は対象外である
  旨を記録済み。
- **検証**: pytest test_admin.py 19 passed（PATCH 401/403/422/404/部分更新+increment/audit/
  deployment 実カウント）+ vitest bundle-j 88 passed（TemplateEditor 4 本 + 既存改修）+
  実操作監査 `.audit-t03-template-edit.mjs` 3 連続 ALL PASS（DB 突合: 部分更新・version 1→2→3・
  uuid[] スキル・text[] カテゴリ・audit 2 件・展開先実カウント archived 除外）。
