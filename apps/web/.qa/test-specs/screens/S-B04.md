# SB04 プロジェクト・シークレット 画面別テスト仕様（S-B04・human-grade-qa design-audit）

> 種別: form+list / 本仕様書は design-audit ラウンドで新規作成 (GAP-101 の 1 枚を解消)。
> 結果列は 2026-07-19 の実操作 (`.audit-b04.mjs` 16 TC) + vitest (`tests/bundle-f/uc41-*` 9 件・新規) の実測。

| ID | 画面 | テスト観点 | テスト項目 | 前提条件 | 操作手順 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|
| SB04-001 | シークレット | 画面表示 | 主要領域 (注意書き/新規追加/一覧) がモック準拠で表示 | project 選択済 | 1. サイドバー「シークレット」から開く | secure-note (amber)・フォーム・一覧テーブルが揃う | PASS | TC1 |
| SB04-002 | シークレット | 登録・編集 | フォームから作成 → 一覧反映 | - | 1. 名称/種別/値を入力 2. 暗号化して保存 | POST 201 → 一覧に新行 (別 GET 再取得) | PASS | TC2 |
| SB04-003 | シークレット | 整合性 | DB は暗号化保存・平文を含まない | 作成直後 | 1. psql で encrypted_value を確認 | Fernet 暗号文のみ・平文部分文字列なし・last4 記録 | PASS | TC3 DB 突合 |
| SB04-004 | シークレット | 画面表示 | 一覧の値はマスク (••••••••last4) | - | 1. 一覧を目視 | 平文は出ない | PASS | TC4 |
| SB04-005 | シークレット | 画面表示 | 作成者列に実ユーザー名 | - | 1. 一覧を目視 | created_by → users.display_name (契約拡張 v2) | PASS | TC5。旧実装は列ごと欠落 |
| SB04-006 | シークレット | 画面表示 | 作成日が YYYY-MM-DD (生 ISO 露出なし) | - | 1. 一覧を目視 | 人間可読の日付 (鉄則5) | PASS | TC6。旧実装は生 ISO を表示 |
| SB04-007 | シークレット | インタラクション | 「表示」→ 復号平文が一時表示 | - | 1. 表示をクリック | reveal API → 平文 + コピー/隠す | PASS | TC7/TC9 |
| SB04-008 | シークレット | 整合性 | reveal が監査ログに記録される | - | 1. 表示 2. psql audit_logs | action=credential.reveal が +1 | PASS | TC8 DB 突合 |
| SB04-009 | シークレット | インタラクション | 「隠す」でマスクへ復帰 (平文を保持しない) | 表示中 | 1. 隠すをクリック | マスク表示へ戻る | PASS | TC10 |
| SB04-010 | シークレット | エラー | reveal 失敗時に明示エラー (握り潰さない) | API 5xx/403 | 1. 失敗させる | role=alert「復号に失敗しました…」 | PASS | vitest。旧実装は無言で終了 |
| SB04-011 | シークレット | 権限 | 他ユーザーは一覧不可・reveal 404 (RLS) | 別 WS ユーザー | 1. 実サインアップした別ユーザーで API | 一覧 0 件・reveal 404 | PASS | TC11-12 実ユーザーで越境=0 |
| SB04-012 | シークレット | インタラクション | 削除は 2 段階確認 | - | 1. ゴミ箱 → 削除する | 1 クリック目は確認のみ。確定で DELETE | PASS | TC13。旧実装は即削除 (機密で確認なしは危険) |
| SB04-013 | シークレット | 整合性 | 削除確定 → 一覧から消え DB soft-delete | - | 1. 削除確定 2. psql | deleted_at セット・一覧再取得で消滅 | PASS | TC14 DB 突合 |
| SB04-014 | シークレット | バリデーション | 名称/値の必須チェック | - | 1. 空のまま保存 | 「名前と値は必須です。」・POST 不発 | PASS | vitest |
| SB04-015 | シークレット | 入力フォーム | 既定種別=トークン (モック準拠)・成功後は全欄クリア | - | 1. 送信成功後を目視 | 値を画面に残さない (機密) | PASS | vitest。旧実装の既定は「その他」 |
| SB04-016 | シークレット | エラー | 保存失敗時に role=alert | POST 4xx/5xx | 1. 失敗させる | エラーメッセージ表示・入力は保持 | PASS | vitest |
| SB04-017 | シークレット | 空データ | 0 件時の空状態 | 0 件 | 1. 空の project で開く | 「まだ何も保管されていません。」 | PASS | vitest + 実機 (削除後の 390 スクショ) |
| SB04-018 | シークレット | レスポンシブ | 390/768/1440 で崩れず操作可能 | - | 1. 各幅で開き入力 | body 横スクロール無し (表は自前スクロール)・フォーム操作可 | PASS | TC15-16 + 390 目視 |
| SB04-019 | シークレット | エラー | project 未指定は明示メッセージ | ?project= なし | 1. /projects/vault 直接 | 「プロジェクトが指定されていません。」 | PASS | 実機確認済 (.b04-edge: メッセージ表示を実走) |
| SB04-020 | シークレット | 状態永続 | 401 で signin へ誘導 | 未ログイン | 1. cookie なしで開く | /signin?redirect=... へ | PASS | 実機確認済 (.b04-edge: cookie なし→ /signin 遷移を実走) |

---

## design-audit ラウンド v2 (2026-07-19) — 契約拡張 + 実操作検証

> モック: `06_mockups/project/S-B04-vault.html` / 実装: `apps/web/app/projects/s_b04/`
> 実操作: `.audit-b04.mjs` 16/16 PASS (暗号化 DB 突合・監査ログ・RLS 実ユーザー越境込み)

### モック要素台帳 (Rule 10 全分類)

| モック要素 | 分類 | 対応 |
|---|---|---|
| secure-note (amber) | 実装 (是正) | 配色が secondary-container に化けていた → モックの amber (#FFFBEB/#FDE68A) に |
| 新規追加フォーム (名称/種別 5 択/値 password/暗号化して保存) | 実装 (是正) | 既定種別が「その他」→ モック通り「トークン」に |
| 一覧テーブル 名称/種別/値/作成者/作成日/操作 | **実装 (契約拡張)** | **作成者列が欠落**していた。DB created_by は存在 → openapi に created_by_name を追加し users join で貫通 (pytest +1) |
| 値マスク ●●●●last4 | 実装 | 従来から |
| 表示 (reveal) | 実装 (是正) | **失敗時に何も起きない握り潰し** → role=alert 明示。監査ログ記録は実 DB 突合で実証 |
| 削除 (trash) | 実装 (是正) | **確認なし即削除** → 2 段階確認 (機密データで最重要) |
| フッター注意書き | 実装 | 従来から |

### 実装追加 (モックに無い改善)
- 「隠す」「コピー」ボタン (reveal 後の操作)
- 作成日の YYYY-MM-DD 整形 (旧実装は**生 ISO タイムスタンプを表示** — 鉄則5 違反の実バグ)

### テスト欠落の解消
- 本画面の web テストは**監査時点で 0 本**だった → `tests/bundle-f/uc41-project-vault.test.tsx` (9 件) を新設
- API 側も created_by_name の統合テストを追加 (`test_project_credentials.py` 実 Postgres)

### 教訓
- 機密操作 (削除・reveal) は「動く」だけでは不足 — **確認段階・失敗の可視化・監査記録**まで
  そろって初めて完成。監査ログは UI からは見えないため **DB 突合でしか検証できない** (鉄則1)。
