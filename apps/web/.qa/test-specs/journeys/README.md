# 通しフロー一覧（ユーザージャーニー正本）

> **これが「どの流れを通せば製品が成立していると言えるか」の正本。**
> `e2e-journey-walkthrough` スキルはここを分母にして実行する。
> 画面別仕様書（`../screens/*.md`）が「1 画面が正しいか」を見るのに対し、
> ここは「**役割をまたいだ流れが、正しい順序で、最後まで成立するか**」を見る。

## なぜ作ったか（2026-08-25）

**ここは今まで存在しなかった。** 画面別は 44 画面ぶんあったのに、通しの正本は 0 本。
その結果、2026-08-25 の通しは「その場で思いついた 16 項目」になり、45 画面中 10 画面しか
触れず、**クライアントポータル（R-T08 = 致命級）にも課金にも触れないまま「一周した」**と
報告する事故になった。

過去の実行記録（`.qa/e2e-journey/journey-2026*.xlsx`）は 3 回分あるが、
**`action` / `expected` 列が空**で何をやったか復元できず、**ロールは owner と client だけで
運営を一度も演じていない**。記録は残っても、次の人が使える正本にはなっていなかった。

## 段と実行条件（test-ladder L2 — 2026-09-02 追加）

ジャーニーは **L2（機能の流れ）** の正本。規約は `.claude/rules/common/test-ladder.md`。

- 各行は `runnable_after`（揃うべきタスク ID 群）を持つ。**流れは、依存タスクの最後の 1 つを
  merge する人が、merge 前に staging で流す**（`python3 scripts/ci/qa-ladder.py runnable --task T-x-y`
  がその人に「この流れが揃った」と示す）。
- 新しい流れは **task-decomposition の時点で**ここに足す（実装後に思い出して足さない）。
  機能 ↔ 流れの対応は feature-decomposition が出す。
- 揃っていない流れは「未実行（待ち）」として残す。黙って消さない。

## ロール（絶対原則4: 管理者を飛ばさない）

| ロール | 入口 | この人のゴール | 供給するもの | 消費するもの |
|---|---|---|---|---|
| **運営 (admin)** | 通常サインイン + 運営権限 | 全アカウントの土台を用意する | 既定テンプレ / AI 社員テンプレ / スキル / 法務文書 | 監査ログ / 健全性 / 混雑実績 |
| **オーナー (owner)** | 新規登録 | 案件を回して納品する | ワークスペース / 案件 / 成果物 / 招待 | 既定テンプレ / AI 社員 |
| **メンバー (member)** | 招待 | 割り当てられた作業をする | 作業結果 / コメント | 案件 / タスク |
| **クライアント (client)** | 招待リンク（別 cookie 系統） | 進捗を見て意見を返す | コメント | 限定ビュー（自分の案件のみ） |

**実行順の原則**: 運営が土台を用意 → オーナーが作る → メンバー/クライアントが消費。
土台が無い状態で消費側から始めた通しは、成立しないので無意味。

## 一覧（43 本 / 315 ステップ）

`場所` は実行場所。`cloud` = この環境で消化できる / `mac` = Bridge + 本人の Claude 契約が要る（1 本の中に両方あるものは `cloud+mac`）。
**行数は感覚で決めない。** `derive_surface.py` が数えた (画面, 操作) の下限（2026-09-03 時点 283）以上にする（GAP-248）。

### 第0群 — 土台（これが無いと他が始まらない）

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-00 | 空の状態から初回起動・最初の運営を作る | 運営 | — | cloud |
| J-01 | 運営が既定デザインテンプレを用意する → WS が継承・上書き・既定に戻す | 運営→オーナー | J-00, J-10 | cloud+mac |
| J-02 | 運営が AI 社員テンプレとスキルを整える（追加・装着・解除・削除・漏れない） | 運営 | J-00 | cloud |
| J-03 | 運営が法務文書を改訂し、現行版を切り替える → 公開ページに出る | 運営→未サインイン | J-00 | cloud |

### 第1群 — 入口（新規登録から使い始めるまで）

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-10 | 新規登録 → 同意 → ワークスペース作成 → 着地 | オーナー | J-03 | cloud |
| J-11 | サインアウト → 再サインイン → 状態が残っている | オーナー | J-10 | cloud |
| J-12 | 規約が新版になった → 帯が出る → 同意する | オーナー | J-03, J-10 | cloud |
| J-13 | パスワードを忘れた → 再設定 → 旧セッションが死ぬ → 5 回失敗ロック | オーナー | J-10 | cloud |
| J-14 | OAuth / Magic Link で入る → 拒否・二重使用が効く | オーナー | J-03 | cloud |
| J-15 | 横断画面（ウェルカム・通知・プロフィール・WS/案件切替・検索）が実データを映す | オーナー | J-10, J-30 | cloud |

### 第2群 — R-T08（致命級・クライアント分離）

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-20 | 招待を発行する → クライアントが招待リンクで入る → 再送・一覧 | オーナー→クライアント | J-30 | cloud |
| J-21 | クライアントが自分の案件だけ見える（最新版のみ・更新メール） | クライアント | J-20 | cloud |
| J-22 | **クライアントが他人の案件へ越境できない**（両系統のトークン） | クライアント | J-20 | cloud |
| J-23 | クライアントがコメントし、社内に届く → 返信 → 納品物を受け取る | クライアント→オーナー | J-21, J-30 | cloud |
| J-24 | 招待の期限切れ・失効・案件削除が即時に効く（G-12 / G-14） | オーナー→クライアント | J-20, J-38 | cloud |

### 第3群 — 案件を回す

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-30 | 案件を作る → 工程が用意される → 進める・スキップ・差戻・フェーズ確定 → 納品 | オーナー | J-10 | cloud |
| J-31 | メンバーを招待する → 権限どおりに使える → ロール変更・除名が効く | オーナー→メンバー | J-30 | cloud |
| J-32 | 承認待ちを承認する / 却下する / 一括 / 通知メール | オーナー | J-30 | cloud |
| J-33 | 既存案件を途中から取り込む | オーナー | J-30 | cloud+mac |
| J-34 | 議事録をアップロード → 文字起こし → 採用 → 次フェーズ提案 | オーナー | J-30 | cloud+mac |
| J-35 | ナレッジを貯める → 検索で引ける → 昇格する → 候補採用 → 書出 | オーナー | J-30 | cloud |
| J-36 | ワークスペース設定（名前・AI 学習・MCP トークン・削除） | オーナー | J-10 | cloud |
| J-37 | AI 社員を編集する → 能力として見える → 運営改訂と共存する | オーナー | J-10, J-02 | cloud |
| J-38 | 案件の設定・削除・30 日復元・アーカイブ | オーナー | J-30 | cloud |
| J-39 | 案件シークレットを登録 → reveal → 削除（平文を漏らさない） | オーナー | J-30 | cloud |
| J-44 | 自動実行スケジュールを作る → 有効/無効 → 今すぐ実行 → 履歴 | オーナー | J-30 | cloud+mac |
| J-45 | モックを作る → 修正依頼 → 版管理 → コメント（凍結が守られる） | オーナー | J-46 | cloud+mac |
| J-46 | 成果物を見る → コメント → AI 修正提案 → 版 → 共有リンクで納品 | オーナー→未サインイン | J-30 | cloud+mac |
| J-47 | 営業ドラフトを作る → PDF → dry_run/本送信 → 履歴 | オーナー | J-35 | cloud+mac |
| J-48 | タスクを作る → 再生 → スコア判定 → 承認/差戻 → 回収（G-15） | オーナー | J-30, J-40 | cloud+mac |
| J-49 | フェーズ提案 → 承認/却下 → タスク移動の影響解析 → 適用 | オーナー | J-30 | cloud+mac |

### 第4群 — AI が仕事をする（製品の中核）

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-40 | 本人の PC を繋ぐ → 未接続時の案内 → トークン失効・終了が即時に効く | オーナー | J-10 | cloud+mac |
| J-41 | チャットで相談 → 文脈が効く → 成果物になる → 追い足し・切替・信号なし終了 | オーナー | J-40, J-30 | mac |
| J-42 | 承認モードで危険な操作が止まる | オーナー | J-40 | mac |
| J-43 | 混雑しても断られず順番待ちになる → 一時停止/再開 → 実績が残る | オーナー | J-40 | mac |

### 第5群 — お金と出口

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-50 | 有料プランに申し込む（free 表示・Checkout・webhook 署名） | オーナー | J-10 | cloud |
| J-51 | 解約する → 期間末まで使える → 期間末で free に戻る | オーナー | J-50 | cloud |
| J-52 | 退会する → 30 日猶予 → 復元できる → トークン全失効 → ハード削除 | オーナー→未サインイン | J-10 | cloud |

### 第6群 — 運営が見る

| ID | フロー | ロール | 依存 | 場所 |
|---|---|---|---|---|
| J-60 | 運営が健全性・混雑実績・監視・エラー報告・ベータ FB を見て気づける | 運営 | J-43 | cloud |
| J-61 | 運営が監査ログで「誰が何をしたか」を追える → 絞る → CSV | 運営 | J-32 | cloud |
| J-62 | 運営が事業 KPI（目標・チャネル・コスト）を記録して見る | 運営 | J-00 | cloud |
| J-63 | 運営がユーザーを横断して見てサポート連絡を送る | 運営 | J-31 | cloud |
| J-64 | 運営デフォルト・ナレッジとキュレーション（匿名化）を回す | 運営 | J-00, J-35 | cloud |

## 成果物（正本と再生成）

| ファイル | 何か |
|---|---|
| `build_plan.py` | **ソース**。ここを直して plan.json を再生成する |
| `plan.json` | 315 ステップ / 43 フローの機械可読な正本。`discovered.role_meta` に各ロールの入口・ゴール・供給・消費を持つ |
| `render_xlsx.py` | **読める形**の Excel を生成する（フロー一覧 + 群ごとのタブ） |
| `../../e2e-journey/journey-plan.xlsx` | 生成した Excel（フロー一覧 + 群タブ + Plan/Roles/Summary） |
| `../../e2e-journey/surface.json` | `derive_surface.py` が数えた表面積（行数の下限の根拠） |

### Excel の構成（フラットな 1 シートで渡さない）

| シート | 中身 |
|---|---|
| **フロー一覧** | 最初に開くシート。**1 フロー = 1 行**。進捗と状態が一目で分かる |
| 第0群 土台 〜 第6群 運営が見る | 群ごとのタブ。フロー名の見出し → その配下にステップ |
| `Plan` | 機械可読（`journey_workbook.py` の update/status が読む） |
| `Roles` | ロール・入口・ゴール・供給/消費 |
| `Summary` | 全体 + ロール別 / 分岐別 / 実行場所別 の内訳と `DONE?` |

**完成した Excel を手で直さない**（鉄則3）。直すのは `build_plan.py` で、そこから再生成する。

```bash
# 再生成 (実行結果は --carry-status で引き継ぐので捨てない)
python3 apps/web/.qa/test-specs/journeys/build_plan.py > apps/web/.qa/test-specs/journeys/plan.json
python3 apps/web/.qa/test-specs/journeys/render_xlsx.py \
  --plan apps/web/.qa/test-specs/journeys/plan.json \
  --out apps/web/.qa/e2e-journey/journey-plan.xlsx --carry-status

# 計画ゲート (GAP-248): 表面積を数え直し、行数の下限・Roles・expected の実結果・outcome を機械検査する
python3 .claude/skills/e2e-journey-walkthrough/scripts/derive_surface.py . -o apps/web/.qa/e2e-journey/surface.json
bash .claude/skills/e2e-journey-walkthrough/scripts/plan_gate.sh \
  apps/web/.qa/test-specs/journeys/plan.json apps/web/.qa/e2e-journey/surface.json

# 1 行を実行したら結果を書き戻す
python3 .claude/skills/e2e-journey-walkthrough/scripts/journey_workbook.py update \
  --out .qa/e2e-journey/journey-<日付>.xlsx --id J22-01 --status PASS \
  --evidence .qa/gap-XXX/shots/xxx.png --note "..."

# 全行 PASS かを機械で見る (未達なら「完了」と書けない)
python3 .claude/skills/e2e-journey-walkthrough/scripts/journey_workbook.py status \
  --out .qa/e2e-journey/journey-<日付>.xlsx
```

## 使い方

1. 着手前に `python3 scripts/ci/qa-coverage.py` で分母を宣言する
2. 依存順（第0群 → 第1群 → …）に実行する。**土台を飛ばして消費側から始めない**
3. 各フローのファイル（`J-XX-*.md`）のステップを 1 つずつ実機で踏み、証拠を残す
4. `場所=mac` のものは、この環境では **BLOCKED として残す**（対象外にしない）
5. 完了系の語を書く前に完了ゲートを通す

## 現在の状態（2026-09-03 / GAP-248）

**43 フロー / 315 ステップ。全行 TODO（未実行）。** `plan_gate.sh` PASS（下限 283）。

> 2026-08-25 版は 28 フロー / 82 ステップで、`plan_gate.sh` に当てたら FAIL だった:
> ① role_meta が空 ② 82 行中 68 行の expected が「画面が出た」止まり ③ owner / member / client_portal に
> outcome 行が無い ④ `derive_surface.py` が Next.js app router / FastAPI を数えられず下限 55 が実態より低い。
> 4 点とも直し、行数は (画面,操作) 265 + 操作なし画面 10 + 分岐 8 = 283 以上にした。
> 数え直した根拠: `python3 -c "import json;print(len(json.load(open(...plan.json))['rows']))"` → 315 /
> フロー = `{r['id'].split('-')[0] for r in rows}` → 43。

| ロール | ステップ数 |
|---|---|
| オーナー | 237 |
| 運営 | 46 |
| クライアント | 16 |
| メンバー | 11 |
| 未サインイン | 5 |

| 分岐 | 数 |
|---|---|
| happy（正常系） | 198 |
| cancel（取り消し・拒否） | 34 |
| permission（権限） | 22 |
| validation（入力エラー） | 16 |
| empty（0 件・未設定） | 15 |
| conflict（競合・凍結） | 13 |
| limit（上限・期限） | 11 |
| isolation（越境の遮断・R-T08） | 6 |

実行場所: **この環境で消化できる 255 / AI 実行基盤が要る 60**。

過去の実行記録（`journey-2026080*.xlsx`）は内容が復元できないため、消化済みとして数えない。
**記録は残っていても、何を確かめたか分からないものは証拠ではない。**
