{
  "name": "@atelier/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "型安全 API クライアント — openapi-typescript で生成した @atelier/api-types を消費する fetch wrapper (T-US-04)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'placeholder — root ESLint で集約' && exit 0",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@atelier/api-types": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
