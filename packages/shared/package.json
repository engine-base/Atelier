{
  "name": "@atelier/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Atelier 共有型・Zod スキーマ・定数（apps/web と apps/api で参照）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'placeholder — T-F-25 で OpenAPI → TS 型生成パイプライン投入' && exit 0",
    "lint": "echo 'placeholder' && exit 0",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
