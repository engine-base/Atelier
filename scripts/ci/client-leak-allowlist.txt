# GAP-204: サーバーと画面の両方に「意図的に」同じ日本語がある場合だけ、
# ここに 1 行 1 件で書く。形式:  <文字列><TAB><理由>
#
# ルール:
#   - **もう流出していない項目はエラーになる** (惰性で膨らませない)
#   - 理由は「なぜ画面側にあってよいのか」を書く。「暫定」「あとで」は不可
#   - プロンプト本文・スキル本文・テンプレ本文は **絶対にここへ書かない**
#     (それらは画面側に出てはいけないもの。出ていたら実装を直す)
AI 応答の取得に失敗しました。時間をおいて再試行してください。	画面が出す汎用エラー文言。サーバー側にも同じ文言があるだけで、中身(プロンプト)ではない (ChatContainer.tsx)
※ 本ドラフトは AI 補助で作成されています。最終版は人間レビュー後に確定されます。	生成物の末尾に必ず入る利用者向けの但し書き。画面もこれを表示・判定に使う (SalesDocDraft.tsx)。プロンプト本文そのものは流出していない
この成果物には改訂対象の HTML がありません	画面が出す利用者向けエラー文言 (OutputViewerContainer.tsx)
この種類の運営既定デザインはまだ設定されていません	画面が出す利用者向けエラー文言 (DesignTemplateStudio.tsx)
※ 本ドラフトは AI 補助で作成されています	上の但し書きを句点で割った断片。同じ理由で共有 (SalesDocDraft.tsx)
最終版は人間レビュー後に確定されます	上の但し書きを句点で割った断片。同じ理由で共有 (SalesDocDraft.tsx)
あなたの PC の Claude Code で実行します	定期実行の費用説明 (cost_note)。プロンプトではなく画面に出す利用者向けの説明 (CronSchedule.tsx)
